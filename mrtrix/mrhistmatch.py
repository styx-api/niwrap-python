# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRHISTMATCH_METADATA = Metadata(
    id="mrhistmatch",
    name="mrhistmatch",
    package="mrtrix",
)

class MrhistmatchConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrhistmatch = typing.TypedDict(
    "Mrhistmatch",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrhistmatch"]],
        "mask_input": typing.NotRequired[InputPathType],
        "mask_target": typing.NotRequired[InputPathType],
        "bins": typing.NotRequired[int],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrhistmatchConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "type": str,
        "input": InputPathType,
        "target": InputPathType,
        "output": str,
    },
)

@dataclasses.dataclass
class MrhistmatchOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType
    """the output image"""

def mrhistmatch_params(
    type_: str,
    input_: InputPathType,
    target: InputPathType,
    output: str,
    mask_input: InputPathType | None = None,
    mask_target: InputPathType | None = None,
    bins: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrhistmatchConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Mrhistmatch:
    """
    Build parameters.

    Args:
        type_: type of histogram matching to perform; options are:\
            scale,linear,nonlinear
        input_: the input image to be modified
        target: the input image from which to derive the target histogram
        output: the output image
        mask_input: only generate input histogram based on a specified binary mask\
            image
        mask_target: only generate target histogram based on a specified binary\
            mask image
        bins: the number of bins to use to generate the histograms
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Mrhistmatch = {
        "@type": "mrtrix/mrhistmatch",
        "type": type_,
        "input": input_,
        "target": target,
        "output": output,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if mask_input is not None:
        params["mask_input"] = mask_input
    if mask_target is not None:
        params["mask_target"] = mask_target
    if bins is not None:
        params["bins"] = bins
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def mrhistmatch_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrhistmatch."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask_input", None) is not None:
        if not isinstance(params["mask_input"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_input` has the wrong type: Received `{type(params["mask_input"])}` expected `InputPathType`')
    if params.get("mask_target", None) is not None:
        if not isinstance(params["mask_target"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_target` has the wrong type: Received `{type(params["mask_target"])}` expected `InputPathType`')
    if params.get("bins", None) is not None:
        if not isinstance(params["bins"], int):
            raise StyxValidationError(f'`bins` has the wrong type: Received `{type(params["bins"])}` expected `int`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrhistmatchConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("type", None) is None:
        raise StyxValidationError("`type` must not be None")
    if not isinstance(params["type"], str):
        raise StyxValidationError(f'`type` has the wrong type: Received `{type(params["type"])}` expected `str`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("target", None) is None:
        raise StyxValidationError("`target` must not be None")
    if not isinstance(params["target"], (pathlib.Path, str)):
        raise StyxValidationError(f'`target` has the wrong type: Received `{type(params["target"])}` expected `InputPathType`')
    if params.get("output", None) is None:
        raise StyxValidationError("`output` must not be None")
    if not isinstance(params["output"], str):
        raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mrhistmatch_cargs(params: Mrhistmatch, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrhistmatch")
    v_0 = params.get("mask_input")
    if v_0 is not None:
        cargs.append("-mask_input")
        cargs.append(execution.input_file(v_0))
    v_1 = params.get("mask_target")
    if v_1 is not None:
        cargs.append("-mask_target")
        cargs.append(execution.input_file(v_1))
    v_2 = params.get("bins")
    if v_2 is not None:
        cargs.append("-bins")
        cargs.append(str(v_2))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(params["type"])
    cargs.append(execution.input_file(params["input"]))
    cargs.append(execution.input_file(params["target"]))
    cargs.append(params["output"])
    return cargs

def mrhistmatch_outputs(params: Mrhistmatch, execution: Execution) -> MrhistmatchOutputs:
    """Build the MrhistmatchOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    output_v: OutputPathType = execution.output_file(params["output"])
    return MrhistmatchOutputs(
        root=root_v,
        output=output_v,
    )

def mrhistmatch_execute(params: Mrhistmatch, runner: Runner | None = None) -> MrhistmatchOutputs:
    """
    mrhistmatch

    Modify the intensities of one image to match the histogram of another.



References:

* If using inverse contrast normalization for inter-modal (DWI - T1) registration:
Bhushan, C.; Haldar, J. P.; Choi, S.; Joshi, A. A.; Shattuck, D. W. & Leahy, R. M. Co-registration and distortion correction of diffusion and anatomical images based on inverse contrast normalization. NeuroImage, 2015, 115, 269-280

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrhistmatch_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRHISTMATCH_METADATA)
    execution.params(params)
    args = mrhistmatch_cargs(params, execution)
    out = mrhistmatch_outputs(params, execution)
    execution.run(args)
    return out

def mrhistmatch(
    type_: str,
    input_: InputPathType,
    target: InputPathType,
    output: str,
    mask_input: InputPathType | None = None,
    mask_target: InputPathType | None = None,
    bins: int | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrhistmatchConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> MrhistmatchOutputs:
    """
    mrhistmatch

    Modify the intensities of one image to match the histogram of another.



References:

* If using inverse contrast normalization for inter-modal (DWI - T1) registration:
Bhushan, C.; Haldar, J. P.; Choi, S.; Joshi, A. A.; Shattuck, D. W. & Leahy, R. M. Co-registration and distortion correction of diffusion and anatomical images based on inverse contrast normalization. NeuroImage, 2015, 115, 269-280

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        type_: type of histogram matching to perform; options are:\
            scale,linear,nonlinear
        input_: the input image to be modified
        target: the input image from which to derive the target histogram
        output: the output image
        mask_input: only generate input histogram based on a specified binary mask\
            image
        mask_target: only generate target histogram based on a specified binary\
            mask image
        bins: the number of bins to use to generate the histograms
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrhistmatch_params(
        type_=type_,
        input_=input_,
        target=target,
        output=output,
        mask_input=mask_input,
        mask_target=mask_target,
        bins=bins,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return mrhistmatch_execute(params, runner)

__all__ = [
    "Mrhistmatch",
    "MrhistmatchOutputs",
    "MRHISTMATCH_METADATA",
    "mrhistmatch_cargs",
    "mrhistmatch_outputs",
    "mrhistmatch_params",
    "mrhistmatch_execute",
    "mrhistmatch_validate",
    "mrhistmatch",
]