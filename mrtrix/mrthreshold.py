# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRTHRESHOLD_METADATA = Metadata(
    id="mrthreshold",
    name="mrthreshold",
    package="mrtrix",
)

class MrthresholdConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrthreshold = typing.TypedDict(
    "Mrthreshold",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrthreshold"]],
        "abs": typing.NotRequired[float],
        "percentile": typing.NotRequired[float],
        "top": typing.NotRequired[int],
        "bottom": typing.NotRequired[int],
        "allvolumes": typing.NotRequired[bool],
        "ignorezero": typing.NotRequired[bool],
        "mask": typing.NotRequired[InputPathType],
        "comparison": typing.NotRequired[str],
        "invert": typing.NotRequired[bool],
        "out_masked": typing.NotRequired[bool],
        "nan": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrthresholdConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "output": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MrthresholdOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType | None
    """the (optional) output binary image mask"""

def mrthreshold_params(
    input_: InputPathType,
    abs: float | None = None,
    percentile: float | None = None,
    top: int | None = None,
    bottom: int | None = None,
    allvolumes: bool = False,
    ignorezero: bool = False,
    mask: InputPathType | None = None,
    comparison: str | None = None,
    invert: bool = False,
    out_masked: bool = False,
    nan: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrthresholdConfig] | None = None,
    help: bool = False,
    version: bool = False,
    output: str | None = None,
) -> Mrthreshold:
    """
    Build parameters.

    Args:
        input_: the input image to be thresholded
        abs: specify threshold value as absolute intensity
        percentile: determine threshold based on some percentile of the image\
            intensity distribution
        top: determine threshold that will result in selection of some number of\
            top-valued voxels
        bottom: determine & apply threshold resulting in selection of some number\
            of bottom-valued voxels (note: implies threshold application operator\
            of "le" unless otherwise specified)
        allvolumes: compute a single threshold for all image volumes, rather than\
            an individual threshold per volume
        ignorezero: ignore zero-valued input values during threshold determination
        mask: compute the threshold based only on values within an input mask image
        comparison: comparison operator to use when applying the threshold; options\
            are: lt,le,ge,gt (default = "le" for -bottom; "ge" otherwise)
        invert: invert the output binary mask (equivalent to flipping the operator;\
            provided for backwards compatibility)
        out_masked: mask the output image based on the provided input mask image
        nan: set voxels that fail the threshold to NaN rather than zero (output\
            image will be floating-point rather than binary)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        output: the (optional) output binary image mask

    Returns:
        Parameter dictionary.
    """
    params: Mrthreshold = {
        "@type": "mrtrix/mrthreshold",
        "input": input_,
        "allvolumes": allvolumes,
        "ignorezero": ignorezero,
        "invert": invert,
        "out_masked": out_masked,
        "nan": nan,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if abs is not None:
        params["abs"] = abs
    if percentile is not None:
        params["percentile"] = percentile
    if top is not None:
        params["top"] = top
    if bottom is not None:
        params["bottom"] = bottom
    if mask is not None:
        params["mask"] = mask
    if comparison is not None:
        params["comparison"] = comparison
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    if output is not None:
        params["output"] = output
    return params

def mrthreshold_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrthreshold."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("abs", None) is not None:
        if not isinstance(params["abs"], (float, int)):
            raise StyxValidationError(f'`abs` has the wrong type: Received `{type(params["abs"])}` expected `float`')
    if params.get("percentile", None) is not None:
        if not isinstance(params["percentile"], (float, int)):
            raise StyxValidationError(f'`percentile` has the wrong type: Received `{type(params["percentile"])}` expected `float`')
    if params.get("top", None) is not None:
        if not isinstance(params["top"], int):
            raise StyxValidationError(f'`top` has the wrong type: Received `{type(params["top"])}` expected `int`')
    if params.get("bottom", None) is not None:
        if not isinstance(params["bottom"], int):
            raise StyxValidationError(f'`bottom` has the wrong type: Received `{type(params["bottom"])}` expected `int`')
    if params.get("allvolumes", None) is not None:
        if not isinstance(params["allvolumes"], bool):
            raise StyxValidationError(f'`allvolumes` has the wrong type: Received `{type(params["allvolumes"])}` expected `bool`')
    if params.get("ignorezero", None) is not None:
        if not isinstance(params["ignorezero"], bool):
            raise StyxValidationError(f'`ignorezero` has the wrong type: Received `{type(params["ignorezero"])}` expected `bool`')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("comparison", None) is not None:
        if not isinstance(params["comparison"], str):
            raise StyxValidationError(f'`comparison` has the wrong type: Received `{type(params["comparison"])}` expected `str`')
    if params.get("invert", None) is not None:
        if not isinstance(params["invert"], bool):
            raise StyxValidationError(f'`invert` has the wrong type: Received `{type(params["invert"])}` expected `bool`')
    if params.get("out_masked", None) is not None:
        if not isinstance(params["out_masked"], bool):
            raise StyxValidationError(f'`out_masked` has the wrong type: Received `{type(params["out_masked"])}` expected `bool`')
    if params.get("nan", None) is not None:
        if not isinstance(params["nan"], bool):
            raise StyxValidationError(f'`nan` has the wrong type: Received `{type(params["nan"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrthresholdConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mrthreshold_cargs(params: Mrthreshold, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrthreshold")
    v_0 = params.get("abs")
    if v_0 is not None:
        cargs.append("-abs")
        cargs.append(str(v_0))
    v_1 = params.get("percentile")
    if v_1 is not None:
        cargs.append("-percentile")
        cargs.append(str(v_1))
    v_2 = params.get("top")
    if v_2 is not None:
        cargs.append("-top")
        cargs.append(str(v_2))
    v_3 = params.get("bottom")
    if v_3 is not None:
        cargs.append("-bottom")
        cargs.append(str(v_3))
    if params.get("allvolumes"):
        cargs.append("-allvolumes")
    if params.get("ignorezero"):
        cargs.append("-ignorezero")
    v_4 = params.get("mask")
    if v_4 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_4))
    v_5 = params.get("comparison")
    if v_5 is not None:
        cargs.append("-comparison")
        cargs.append(v_5)
    if params.get("invert"):
        cargs.append("-invert")
    if params.get("out_masked"):
        cargs.append("-out_masked")
    if params.get("nan"):
        cargs.append("-nan")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_6 = params.get("nthreads")
    if v_6 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_6))
    v_7 = params.get("config")
    if v_7 is not None:
        for item0 in v_7:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    v_8 = params.get("output")
    if v_8 is not None:
        cargs.append(v_8)
    return cargs

def mrthreshold_outputs(params: Mrthreshold, execution: Execution) -> MrthresholdOutputs:
    """Build the MrthresholdOutputs object for this tool."""
    output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output")
    if __v0 is not None:
        output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MrthresholdOutputs(
        root=root_v,
        output=output_v,
    )

def mrthreshold_execute(params: Mrthreshold, runner: Runner | None = None) -> MrthresholdOutputs:
    """
    mrthreshold

    Create bitwise image by thresholding image intensity.

The threshold value to be applied can be determined in one of a number of ways:

- If no relevant command-line option is used, the command will automatically determine an optimal threshold;

- The -abs option provides the threshold value explicitly;

- The -percentile, -top and -bottom options enable more fine-grained control over how the threshold value is determined.

The -mask option only influences those image values that contribute toward the determination of the threshold value; once the threshold is determined, it is applied to the entire image, irrespective of use of the -mask option. If you wish for the voxels outside of the specified mask to additionally be excluded from the output mask, this can be achieved by providing the -out_masked option.

The four operators available through the "-comparison" option ("lt", "le", "ge" and "gt") correspond to "less-than" (<), "less-than-or-equal" (<=), "greater-than-or-equal" (>=) and "greater-than" (>). This offers fine-grained control over how the thresholding operation will behave in the presence of values equivalent to the threshold. By default, the command will select voxels with values greater than or equal to the determined threshold ("ge"); unless the -bottom option is used, in which case after a threshold is determined from the relevant lowest-valued image voxels, those voxels with values less than or equal to that threshold ("le") are selected. This provides more fine-grained control than the -invert option; the latter is provided for backwards compatibility, but is equivalent to selection of the opposite comparison within this selection.

If no output image path is specified, the command will instead write to standard output the determined threshold value.

References:

* If not using any explicit thresholding mechanism: 
Ridgway, G. R.; Omar, R.; Ourselin, S.; Hill, D. L.; Warren, J. D. & Fox, N. C. Issues with threshold masking in voxel-based morphometry of atrophied brains. NeuroImage, 2009, 44, 99-111

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrthreshold_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRTHRESHOLD_METADATA)
    execution.params(params)
    args = mrthreshold_cargs(params, execution)
    out = mrthreshold_outputs(params, execution)
    execution.run(args)
    return out

def mrthreshold(
    input_: InputPathType,
    abs: float | None = None,
    percentile: float | None = None,
    top: int | None = None,
    bottom: int | None = None,
    allvolumes: bool = False,
    ignorezero: bool = False,
    mask: InputPathType | None = None,
    comparison: str | None = None,
    invert: bool = False,
    out_masked: bool = False,
    nan: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrthresholdConfig] | None = None,
    help: bool = False,
    version: bool = False,
    output: str | None = None,
    runner: Runner | None = None,
) -> MrthresholdOutputs:
    """
    mrthreshold

    Create bitwise image by thresholding image intensity.

The threshold value to be applied can be determined in one of a number of ways:

- If no relevant command-line option is used, the command will automatically determine an optimal threshold;

- The -abs option provides the threshold value explicitly;

- The -percentile, -top and -bottom options enable more fine-grained control over how the threshold value is determined.

The -mask option only influences those image values that contribute toward the determination of the threshold value; once the threshold is determined, it is applied to the entire image, irrespective of use of the -mask option. If you wish for the voxels outside of the specified mask to additionally be excluded from the output mask, this can be achieved by providing the -out_masked option.

The four operators available through the "-comparison" option ("lt", "le", "ge" and "gt") correspond to "less-than" (<), "less-than-or-equal" (<=), "greater-than-or-equal" (>=) and "greater-than" (>). This offers fine-grained control over how the thresholding operation will behave in the presence of values equivalent to the threshold. By default, the command will select voxels with values greater than or equal to the determined threshold ("ge"); unless the -bottom option is used, in which case after a threshold is determined from the relevant lowest-valued image voxels, those voxels with values less than or equal to that threshold ("le") are selected. This provides more fine-grained control than the -invert option; the latter is provided for backwards compatibility, but is equivalent to selection of the opposite comparison within this selection.

If no output image path is specified, the command will instead write to standard output the determined threshold value.

References:

* If not using any explicit thresholding mechanism: 
Ridgway, G. R.; Omar, R.; Ourselin, S.; Hill, D. L.; Warren, J. D. & Fox, N. C. Issues with threshold masking in voxel-based morphometry of atrophied brains. NeuroImage, 2009, 44, 99-111

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input image to be thresholded
        abs: specify threshold value as absolute intensity
        percentile: determine threshold based on some percentile of the image\
            intensity distribution
        top: determine threshold that will result in selection of some number of\
            top-valued voxels
        bottom: determine & apply threshold resulting in selection of some number\
            of bottom-valued voxels (note: implies threshold application operator\
            of "le" unless otherwise specified)
        allvolumes: compute a single threshold for all image volumes, rather than\
            an individual threshold per volume
        ignorezero: ignore zero-valued input values during threshold determination
        mask: compute the threshold based only on values within an input mask image
        comparison: comparison operator to use when applying the threshold; options\
            are: lt,le,ge,gt (default = "le" for -bottom; "ge" otherwise)
        invert: invert the output binary mask (equivalent to flipping the operator;\
            provided for backwards compatibility)
        out_masked: mask the output image based on the provided input mask image
        nan: set voxels that fail the threshold to NaN rather than zero (output\
            image will be floating-point rather than binary)
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        output: the (optional) output binary image mask
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrthreshold_params(
        input_=input_,
        abs=abs,
        percentile=percentile,
        top=top,
        bottom=bottom,
        allvolumes=allvolumes,
        ignorezero=ignorezero,
        mask=mask,
        comparison=comparison,
        invert=invert,
        out_masked=out_masked,
        nan=nan,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
        output=output,
    )
    return mrthreshold_execute(params, runner)

__all__ = [
    "Mrthreshold",
    "MrthresholdOutputs",
    "MRTHRESHOLD_METADATA",
    "mrthreshold_cargs",
    "mrthreshold_outputs",
    "mrthreshold_params",
    "mrthreshold_execute",
    "mrthreshold_validate",
    "mrthreshold",
]