# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXEL2VOXEL_METADATA = Metadata(
    id="fixel2voxel",
    name="fixel2voxel",
    package="mrtrix",
)

class Fixel2voxelConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Fixel2voxel = typing.TypedDict(
    "Fixel2voxel",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixel2voxel"]],
        "number": typing.NotRequired[int],
        "fill": typing.NotRequired[float],
        "weighted": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Fixel2voxelConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "fixel_in": InputPathType,
        "operation": str,
        "image_out": str,
    },
)

@dataclasses.dataclass
class Fixel2voxelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    image_out: OutputPathType
    """the output scalar image."""

def fixel2voxel_params(
    fixel_in: InputPathType,
    operation: str,
    image_out: str,
    number: int | None = None,
    fill: float | None = None,
    weighted: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2voxelConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixel2voxel:
    """
    Build parameters.

    Args:
        fixel_in: the input fixel data file
        operation: the operation to apply, one of: mean, sum, product, min, max,\
            absmax, magmax, count, complexity, sf, dec_unit, dec_scaled, none.
        image_out: the output scalar image.
        number: use only the largest N fixels in calculation of the voxel-wise\
            statistic; in the case of operation "none", output only the largest N\
            fixels in each voxel.
        fill: for "none" operation, specify the value to fill when number of fixels\
            is fewer than the maximum (default: 0.0)
        weighted: weight the contribution of each fixel to the per-voxel result\
            according to its volume.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixel2voxel = {
        "@type": "mrtrix/fixel2voxel",
        "fixel_in": fixel_in,
        "operation": operation,
        "image_out": image_out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if number is not None:
        params["number"] = number
    if fill is not None:
        params["fill"] = fill
    if weighted is not None:
        params["weighted"] = weighted
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixel2voxel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixel2voxel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("number", None) is not None:
        if not isinstance(params["number"], int):
            raise StyxValidationError(f'`number` has the wrong type: Received `{type(params["number"])}` expected `int`')
    if params.get("fill", None) is not None:
        if not isinstance(params["fill"], (float, int)):
            raise StyxValidationError(f'`fill` has the wrong type: Received `{type(params["fill"])}` expected `float`')
    if params.get("weighted", None) is not None:
        if not isinstance(params["weighted"], (pathlib.Path, str)):
            raise StyxValidationError(f'`weighted` has the wrong type: Received `{type(params["weighted"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Fixel2voxelConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("fixel_in", None) is None:
        raise StyxValidationError("`fixel_in` must not be None")
    if not isinstance(params["fixel_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`fixel_in` has the wrong type: Received `{type(params["fixel_in"])}` expected `InputPathType`')
    if params.get("operation", None) is None:
        raise StyxValidationError("`operation` must not be None")
    if not isinstance(params["operation"], str):
        raise StyxValidationError(f'`operation` has the wrong type: Received `{type(params["operation"])}` expected `str`')
    if params.get("image_out", None) is None:
        raise StyxValidationError("`image_out` must not be None")
    if not isinstance(params["image_out"], str):
        raise StyxValidationError(f'`image_out` has the wrong type: Received `{type(params["image_out"])}` expected `str`')

def fixel2voxel_cargs(params: Fixel2voxel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixel2voxel")
    v_0 = params.get("number")
    if v_0 is not None:
        cargs.append("-number")
        cargs.append(str(v_0))
    v_1 = params.get("fill")
    if v_1 is not None:
        cargs.append("-fill")
        cargs.append(str(v_1))
    v_2 = params.get("weighted")
    if v_2 is not None:
        cargs.append("-weighted")
        cargs.append(execution.input_file(v_2))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["fixel_in"]))
    cargs.append(params["operation"])
    cargs.append(params["image_out"])
    return cargs

def fixel2voxel_outputs(params: Fixel2voxel, execution: Execution) -> Fixel2voxelOutputs:
    """Build the Fixel2voxelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    image_out_v: OutputPathType = execution.output_file(params["image_out"])
    return Fixel2voxelOutputs(
        root=root_v,
        image_out=image_out_v,
    )

def fixel2voxel_execute(params: Fixel2voxel, runner: Runner | None = None) -> Fixel2voxelOutputs:
    """
    fixel2voxel

    Convert a fixel-based sparse-data image into some form of scalar image.

Fixel data can be reduced to voxel data in a number of ways:

- Some statistic computed across all fixel values within a voxel: mean, sum, product, min, max, absmax, magmax

- The number of fixels in each voxel: count

- Some measure of crossing-fibre organisation: complexity, sf ('single-fibre')

- A 4D directionally-encoded colour image: dec_unit, dec_scaled

- A 4D image containing all fixel data values in each voxel unmodified: none

The -weighted option deals with the case where there is some per-fixel metric of interest that you wish to collapse into a single scalar measure per voxel, but each fixel possesses a different volume, and you wish for those fixels with greater volume to have a greater influence on the calculation than fixels with lesser volume. For instance, when estimating a voxel-based measure of mean axon diameter from per-fixel mean axon diameters, a fixel's mean axon diameter should be weigthed by its relative volume within the voxel in the calculation of that voxel mean.

References:

* Reference for 'complexity' operation:
Riffert, T. W.; Schreiber, J.; Anwander, A. & Knosche, T. R. Beyond Fractional Anisotropy: Extraction of bundle-specific structural metrics from crossing fibre models. NeuroImage, 2014, 100, 176-191

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixel2voxel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXEL2VOXEL_METADATA)
    execution.params(params)
    args = fixel2voxel_cargs(params, execution)
    out = fixel2voxel_outputs(params, execution)
    execution.run(args)
    return out

def fixel2voxel(
    fixel_in: InputPathType,
    operation: str,
    image_out: str,
    number: int | None = None,
    fill: float | None = None,
    weighted: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Fixel2voxelConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Fixel2voxelOutputs:
    """
    fixel2voxel

    Convert a fixel-based sparse-data image into some form of scalar image.

Fixel data can be reduced to voxel data in a number of ways:

- Some statistic computed across all fixel values within a voxel: mean, sum, product, min, max, absmax, magmax

- The number of fixels in each voxel: count

- Some measure of crossing-fibre organisation: complexity, sf ('single-fibre')

- A 4D directionally-encoded colour image: dec_unit, dec_scaled

- A 4D image containing all fixel data values in each voxel unmodified: none

The -weighted option deals with the case where there is some per-fixel metric of interest that you wish to collapse into a single scalar measure per voxel, but each fixel possesses a different volume, and you wish for those fixels with greater volume to have a greater influence on the calculation than fixels with lesser volume. For instance, when estimating a voxel-based measure of mean axon diameter from per-fixel mean axon diameters, a fixel's mean axon diameter should be weigthed by its relative volume within the voxel in the calculation of that voxel mean.

References:

* Reference for 'complexity' operation:
Riffert, T. W.; Schreiber, J.; Anwander, A. & Knosche, T. R. Beyond Fractional Anisotropy: Extraction of bundle-specific structural metrics from crossing fibre models. NeuroImage, 2014, 100, 176-191

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        fixel_in: the input fixel data file
        operation: the operation to apply, one of: mean, sum, product, min, max,\
            absmax, magmax, count, complexity, sf, dec_unit, dec_scaled, none.
        image_out: the output scalar image.
        number: use only the largest N fixels in calculation of the voxel-wise\
            statistic; in the case of operation "none", output only the largest N\
            fixels in each voxel.
        fill: for "none" operation, specify the value to fill when number of fixels\
            is fewer than the maximum (default: 0.0)
        weighted: weight the contribution of each fixel to the per-voxel result\
            according to its volume.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixel2voxel_params(
        fixel_in=fixel_in,
        operation=operation,
        image_out=image_out,
        number=number,
        fill=fill,
        weighted=weighted,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixel2voxel_execute(params, runner)

__all__ = [
    "Fixel2voxel",
    "Fixel2voxelOutputs",
    "FIXEL2VOXEL_METADATA",
    "fixel2voxel_cargs",
    "fixel2voxel_outputs",
    "fixel2voxel_params",
    "fixel2voxel_execute",
    "fixel2voxel_validate",
    "fixel2voxel",
]