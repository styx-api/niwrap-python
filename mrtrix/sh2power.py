# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

SH2POWER_METADATA = Metadata(
    id="sh2power",
    name="sh2power",
    package="mrtrix",
)

class Sh2powerConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Sh2power = typing.TypedDict(
    "Sh2power",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/sh2power"]],
        "spectrum": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Sh2powerConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "SH": InputPathType,
        "power": str,
    },
)

@dataclasses.dataclass
class Sh2powerOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    power: OutputPathType
    """the output power image."""

def sh2power_params(
    SH: InputPathType,
    power: str,
    spectrum: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Sh2powerConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Sh2power:
    """
    Build parameters.

    Args:
        SH: the input spherical harmonics coefficients image.
        power: the output power image.
        spectrum: output the power spectrum, i.e., the power contained within each\
            harmonic degree (l=0, 2, 4, ...) as a 4-D image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Sh2power = {
        "@type": "mrtrix/sh2power",
        "SH": SH,
        "power": power,
        "spectrum": spectrum,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def sh2power_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Sh2power."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("spectrum", None) is not None:
        if not isinstance(params["spectrum"], bool):
            raise StyxValidationError(f'`spectrum` has the wrong type: Received `{type(params["spectrum"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Sh2powerConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("SH", None) is None:
        raise StyxValidationError("`SH` must not be None")
    if not isinstance(params["SH"], (pathlib.Path, str)):
        raise StyxValidationError(f'`SH` has the wrong type: Received `{type(params["SH"])}` expected `InputPathType`')
    if params.get("power", None) is None:
        raise StyxValidationError("`power` must not be None")
    if not isinstance(params["power"], str):
        raise StyxValidationError(f'`power` has the wrong type: Received `{type(params["power"])}` expected `str`')

def sh2power_cargs(params: Sh2power, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("sh2power")
    if params.get("spectrum"):
        cargs.append("-spectrum")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_0 = params.get("nthreads")
    if v_0 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_0))
    v_1 = params.get("config")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["SH"]))
    cargs.append(params["power"])
    return cargs

def sh2power_outputs(params: Sh2power, execution: Execution) -> Sh2powerOutputs:
    """Build the Sh2powerOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    power_v: OutputPathType = execution.output_file(params["power"])
    return Sh2powerOutputs(
        root=root_v,
        power=power_v,
    )

def sh2power_execute(params: Sh2power, runner: Runner | None = None) -> Sh2powerOutputs:
    """
    sh2power

    Compute the total power of a spherical harmonics image.

This command computes the sum of squared SH coefficients, which equals the mean-squared amplitude of the spherical function it represents.

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    sh2power_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(SH2POWER_METADATA)
    execution.params(params)
    args = sh2power_cargs(params, execution)
    out = sh2power_outputs(params, execution)
    execution.run(args)
    return out

def sh2power(
    SH: InputPathType,
    power: str,
    spectrum: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Sh2powerConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Sh2powerOutputs:
    """
    sh2power

    Compute the total power of a spherical harmonics image.

This command computes the sum of squared SH coefficients, which equals the mean-squared amplitude of the spherical function it represents.

The spherical harmonic coefficients are stored according the conventions described the main documentation, which can be found at the following link: 
https://mrtrix.readthedocs.io/en/3.0.4/concepts/spherical_harmonics.html

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        SH: the input spherical harmonics coefficients image.
        power: the output power image.
        spectrum: output the power spectrum, i.e., the power contained within each\
            harmonic degree (l=0, 2, 4, ...) as a 4-D image.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = sh2power_params(
        SH=SH,
        power=power,
        spectrum=spectrum,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return sh2power_execute(params, runner)

__all__ = [
    "Sh2power",
    "Sh2powerOutputs",
    "SH2POWER_METADATA",
    "sh2power_cargs",
    "sh2power_outputs",
    "sh2power_params",
    "sh2power_execute",
    "sh2power_validate",
    "sh2power",
]