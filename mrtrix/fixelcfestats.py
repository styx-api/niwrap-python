# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

FIXELCFESTATS_METADATA = Metadata(
    id="fixelcfestats",
    name="fixelcfestats",
    package="mrtrix",
)

class FixelcfestatsConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

FixelcfestatsVariousString = typing.TypedDict(
    "FixelcfestatsVariousString",
    {
        "@type": typing.Literal["VariousString"],
        "obj": str,
    },
)

FixelcfestatsVariousFile = typing.TypedDict(
    "FixelcfestatsVariousFile",
    {
        "@type": typing.Literal["VariousFile"],
        "obj": InputPathType,
    },
)

FixelcfestatsConnectivity = FixelcfestatsVariousString | FixelcfestatsVariousFile

Fixelcfestats = typing.TypedDict(
    "Fixelcfestats",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/fixelcfestats"]],
        "mask": typing.NotRequired[InputPathType],
        "notest": typing.NotRequired[bool],
        "errors": typing.NotRequired[str],
        "exchange_within": typing.NotRequired[InputPathType],
        "exchange_whole": typing.NotRequired[InputPathType],
        "strong": typing.NotRequired[bool],
        "nshuffles": typing.NotRequired[int],
        "permutations": typing.NotRequired[InputPathType],
        "nonstationarity": typing.NotRequired[bool],
        "skew_nonstationarity": typing.NotRequired[float],
        "nshuffles_nonstationarity": typing.NotRequired[int],
        "permutations_nonstationarity": typing.NotRequired[InputPathType],
        "cfe_dh": typing.NotRequired[float],
        "cfe_e": typing.NotRequired[float],
        "cfe_h": typing.NotRequired[float],
        "cfe_c": typing.NotRequired[float],
        "cfe_legacy": typing.NotRequired[bool],
        "variance": typing.NotRequired[InputPathType],
        "ftests": typing.NotRequired[InputPathType],
        "fonly": typing.NotRequired[bool],
        "column": typing.NotRequired[list[InputPathType]],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[FixelcfestatsConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in_fixel_directory": InputPathType,
        "subjects": InputPathType,
        "design": InputPathType,
        "contrast": InputPathType,
        "connectivity": FixelcfestatsConnectivity,
        "out_fixel_directory": str,
    },
)

@dataclasses.dataclass
class FixelcfestatsOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def fixelcfestats_params(
    in_fixel_directory: InputPathType,
    subjects: InputPathType,
    design: InputPathType,
    contrast: InputPathType,
    connectivity: FixelcfestatsConnectivity,
    out_fixel_directory: str,
    mask: InputPathType | None = None,
    notest: bool = False,
    errors: str | None = None,
    exchange_within: InputPathType | None = None,
    exchange_whole: InputPathType | None = None,
    strong: bool = False,
    nshuffles: int | None = None,
    permutations: InputPathType | None = None,
    nonstationarity: bool = False,
    skew_nonstationarity: float | None = None,
    nshuffles_nonstationarity: int | None = None,
    permutations_nonstationarity: InputPathType | None = None,
    cfe_dh: float | None = None,
    cfe_e: float | None = None,
    cfe_h: float | None = None,
    cfe_c: float | None = None,
    cfe_legacy: bool = False,
    variance: InputPathType | None = None,
    ftests: InputPathType | None = None,
    fonly: bool = False,
    column: list[InputPathType] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelcfestatsConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Fixelcfestats:
    """
    Build parameters.

    Args:
        in_fixel_directory: the fixel directory containing the data files for each\
            subject (after obtaining fixel correspondence
        subjects: a text file listing the subject identifiers (one per line). This\
            should correspond with the filenames in the fixel directory (including\
            the file extension), and be listed in the same order as the rows of the\
            design matrix.
        design: the design matrix
        contrast: the contrast matrix, specified as rows of weights
        connectivity: the fixel-fixel connectivity matrix
        out_fixel_directory: the output directory where results will be saved. Will\
            be created if it does not exist
        mask: provide a fixel data file containing a mask of those fixels to be\
            used during processing
        notest: don't perform statistical inference; only output population\
            statistics (effect size, stdev etc)
        errors: specify nature of errors for shuffling; options are: ee,ise,both\
            (default: ee)
        exchange_within: specify blocks of observations within each of which data\
            may undergo restricted exchange
        exchange_whole: specify blocks of observations that may be exchanged with\
            one another (for independent and symmetric errors, sign-flipping will\
            occur block-wise)
        strong: use strong familywise error control across multiple hypotheses
        nshuffles: the number of shuffles (default: 5000)
        permutations: manually define the permutations (relabelling). The input\
            should be a text file defining a m x n matrix, where each relabelling\
            is defined as a column vector of size m, and the number of columns, n,\
            defines the number of permutations. Can be generated with the\
            palm_quickperms function in PALM\
            (http://fsl.fmrib.ox.ac.uk/fsl/fslwiki/PALM). Overrides the -nshuffles\
            option.
        nonstationarity: perform non-stationarity correction
        skew_nonstationarity: specify the skew parameter for empirical statistic\
            calculation (default for this command is 1)
        nshuffles_nonstationarity: the number of shuffles to use when precomputing\
            the empirical statistic image for non-stationarity correction (default:\
            5000)
        permutations_nonstationarity: manually define the permutations\
            (relabelling) for computing the emprical statistics for\
            non-stationarity correction. The input should be a text file defining a\
            m x n matrix, where each relabelling is defined as a column vector of\
            size m, and the number of columns, n, defines the number of\
            permutations. Can be generated with the palm_quickperms function in\
            PALM (http://fsl.fmrib.ox.ac.uk/fsl/fslwiki/PALM) Overrides the\
            -nshuffles_nonstationarity option.
        cfe_dh: the height increment used in the cfe integration (default: 0.1)
        cfe_e: cfe extent exponent (default: 2)
        cfe_h: cfe height exponent (default: 3)
        cfe_c: cfe connectivity exponent (default: 0.5)
        cfe_legacy: use the legacy (non-normalised) form of the cfe equation
        variance: define variance groups for the G-statistic; measurements for\
            which the expected variance is equivalent should contain the same index
        ftests: perform F-tests; input text file should contain, for each F-test, a\
            row containing ones and zeros, where ones indicate the rows of the\
            contrast matrix to be included in the F-test.
        fonly: only assess F-tests; do not perform statistical inference on entries\
            in the contrast matrix
        column: add a column to the design matrix corresponding to subject\
            fixel-wise values (note that the contrast matrix must include an\
            additional column for each use of this option); the text file provided\
            via this option should contain a file name for each subject
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Fixelcfestats = {
        "@type": "mrtrix/fixelcfestats",
        "in_fixel_directory": in_fixel_directory,
        "subjects": subjects,
        "design": design,
        "contrast": contrast,
        "connectivity": connectivity,
        "out_fixel_directory": out_fixel_directory,
        "notest": notest,
        "strong": strong,
        "nonstationarity": nonstationarity,
        "cfe_legacy": cfe_legacy,
        "fonly": fonly,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if mask is not None:
        params["mask"] = mask
    if errors is not None:
        params["errors"] = errors
    if exchange_within is not None:
        params["exchange_within"] = exchange_within
    if exchange_whole is not None:
        params["exchange_whole"] = exchange_whole
    if nshuffles is not None:
        params["nshuffles"] = nshuffles
    if permutations is not None:
        params["permutations"] = permutations
    if skew_nonstationarity is not None:
        params["skew_nonstationarity"] = skew_nonstationarity
    if nshuffles_nonstationarity is not None:
        params["nshuffles_nonstationarity"] = nshuffles_nonstationarity
    if permutations_nonstationarity is not None:
        params["permutations_nonstationarity"] = permutations_nonstationarity
    if cfe_dh is not None:
        params["cfe_dh"] = cfe_dh
    if cfe_e is not None:
        params["cfe_e"] = cfe_e
    if cfe_h is not None:
        params["cfe_h"] = cfe_h
    if cfe_c is not None:
        params["cfe_c"] = cfe_c
    if variance is not None:
        params["variance"] = variance
    if ftests is not None:
        params["ftests"] = ftests
    if column is not None:
        params["column"] = column
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def fixelcfestats_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Fixelcfestats."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask", None) is not None:
        if not isinstance(params["mask"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask` has the wrong type: Received `{type(params["mask"])}` expected `InputPathType`')
    if params.get("notest", None) is not None:
        if not isinstance(params["notest"], bool):
            raise StyxValidationError(f'`notest` has the wrong type: Received `{type(params["notest"])}` expected `bool`')
    if params.get("errors", None) is not None:
        if not isinstance(params["errors"], str):
            raise StyxValidationError(f'`errors` has the wrong type: Received `{type(params["errors"])}` expected `str`')
    if params.get("exchange_within", None) is not None:
        if not isinstance(params["exchange_within"], (pathlib.Path, str)):
            raise StyxValidationError(f'`exchange_within` has the wrong type: Received `{type(params["exchange_within"])}` expected `InputPathType`')
    if params.get("exchange_whole", None) is not None:
        if not isinstance(params["exchange_whole"], (pathlib.Path, str)):
            raise StyxValidationError(f'`exchange_whole` has the wrong type: Received `{type(params["exchange_whole"])}` expected `InputPathType`')
    if params.get("strong", None) is not None:
        if not isinstance(params["strong"], bool):
            raise StyxValidationError(f'`strong` has the wrong type: Received `{type(params["strong"])}` expected `bool`')
    if params.get("nshuffles", None) is not None:
        if not isinstance(params["nshuffles"], int):
            raise StyxValidationError(f'`nshuffles` has the wrong type: Received `{type(params["nshuffles"])}` expected `int`')
    if params.get("permutations", None) is not None:
        if not isinstance(params["permutations"], (pathlib.Path, str)):
            raise StyxValidationError(f'`permutations` has the wrong type: Received `{type(params["permutations"])}` expected `InputPathType`')
    if params.get("nonstationarity", None) is not None:
        if not isinstance(params["nonstationarity"], bool):
            raise StyxValidationError(f'`nonstationarity` has the wrong type: Received `{type(params["nonstationarity"])}` expected `bool`')
    if params.get("skew_nonstationarity", None) is not None:
        if not isinstance(params["skew_nonstationarity"], (float, int)):
            raise StyxValidationError(f'`skew_nonstationarity` has the wrong type: Received `{type(params["skew_nonstationarity"])}` expected `float`')
    if params.get("nshuffles_nonstationarity", None) is not None:
        if not isinstance(params["nshuffles_nonstationarity"], int):
            raise StyxValidationError(f'`nshuffles_nonstationarity` has the wrong type: Received `{type(params["nshuffles_nonstationarity"])}` expected `int`')
    if params.get("permutations_nonstationarity", None) is not None:
        if not isinstance(params["permutations_nonstationarity"], (pathlib.Path, str)):
            raise StyxValidationError(f'`permutations_nonstationarity` has the wrong type: Received `{type(params["permutations_nonstationarity"])}` expected `InputPathType`')
    if params.get("cfe_dh", None) is not None:
        if not isinstance(params["cfe_dh"], (float, int)):
            raise StyxValidationError(f'`cfe_dh` has the wrong type: Received `{type(params["cfe_dh"])}` expected `float`')
    if params.get("cfe_e", None) is not None:
        if not isinstance(params["cfe_e"], (float, int)):
            raise StyxValidationError(f'`cfe_e` has the wrong type: Received `{type(params["cfe_e"])}` expected `float`')
    if params.get("cfe_h", None) is not None:
        if not isinstance(params["cfe_h"], (float, int)):
            raise StyxValidationError(f'`cfe_h` has the wrong type: Received `{type(params["cfe_h"])}` expected `float`')
    if params.get("cfe_c", None) is not None:
        if not isinstance(params["cfe_c"], (float, int)):
            raise StyxValidationError(f'`cfe_c` has the wrong type: Received `{type(params["cfe_c"])}` expected `float`')
    if params.get("cfe_legacy", None) is not None:
        if not isinstance(params["cfe_legacy"], bool):
            raise StyxValidationError(f'`cfe_legacy` has the wrong type: Received `{type(params["cfe_legacy"])}` expected `bool`')
    if params.get("variance", None) is not None:
        if not isinstance(params["variance"], (pathlib.Path, str)):
            raise StyxValidationError(f'`variance` has the wrong type: Received `{type(params["variance"])}` expected `InputPathType`')
    if params.get("ftests", None) is not None:
        if not isinstance(params["ftests"], (pathlib.Path, str)):
            raise StyxValidationError(f'`ftests` has the wrong type: Received `{type(params["ftests"])}` expected `InputPathType`')
    if params.get("fonly", None) is not None:
        if not isinstance(params["fonly"], bool):
            raise StyxValidationError(f'`fonly` has the wrong type: Received `{type(params["fonly"])}` expected `bool`')
    if params.get("column", None) is not None:
        if not isinstance(params["column"], list):
            raise StyxValidationError(f'`column` has the wrong type: Received `{type(params["column"])}` expected `list[InputPathType]`')
        for e in params["column"]:
            if not isinstance(e, (pathlib.Path, str)):
                raise StyxValidationError(f'`column` has the wrong type: Received `{type(e)}` expected `list[InputPathType]`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[FixelcfestatsConfig]`')
        for e_2 in params["config"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_2["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_2["key"])}` expected `str`')
            if e_2.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_2["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_2["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in_fixel_directory", None) is None:
        raise StyxValidationError("`in_fixel_directory` must not be None")
    if not isinstance(params["in_fixel_directory"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in_fixel_directory` has the wrong type: Received `{type(params["in_fixel_directory"])}` expected `InputPathType`')
    if params.get("subjects", None) is None:
        raise StyxValidationError("`subjects` must not be None")
    if not isinstance(params["subjects"], (pathlib.Path, str)):
        raise StyxValidationError(f'`subjects` has the wrong type: Received `{type(params["subjects"])}` expected `InputPathType`')
    if params.get("design", None) is None:
        raise StyxValidationError("`design` must not be None")
    if not isinstance(params["design"], (pathlib.Path, str)):
        raise StyxValidationError(f'`design` has the wrong type: Received `{type(params["design"])}` expected `InputPathType`')
    if params.get("contrast", None) is None:
        raise StyxValidationError("`contrast` must not be None")
    if not isinstance(params["contrast"], (pathlib.Path, str)):
        raise StyxValidationError(f'`contrast` has the wrong type: Received `{type(params["contrast"])}` expected `InputPathType`')
    if params.get("connectivity", None) is None:
        raise StyxValidationError("`connectivity` must not be None")
    if not isinstance(params["connectivity"], dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params["connectivity"])}\'')
    if "@type" not in params["connectivity"]:
        raise StyxValidationError("Params object is missing `@type`")
    if params["connectivity"]["@type"] not in ["VariousString", "VariousFile"]:
        raise StyxValidationError("Parameter `connectivity`s `@type` must be one of [\"VariousString\", \"VariousFile\"]")
    if params["connectivity"]["@type"] == "VariousString":
        if params["connectivity"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["connectivity"]["obj"], str):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["connectivity"]["obj"])}` expected `str`')
    elif params["connectivity"]["@type"] == "VariousFile":
        if params["connectivity"].get("obj", None) is None:
            raise StyxValidationError("`obj` must not be None")
        if not isinstance(params["connectivity"]["obj"], (pathlib.Path, str)):
            raise StyxValidationError(f'`obj` has the wrong type: Received `{type(params["connectivity"]["obj"])}` expected `InputPathType`')
    if params.get("out_fixel_directory", None) is None:
        raise StyxValidationError("`out_fixel_directory` must not be None")
    if not isinstance(params["out_fixel_directory"], str):
        raise StyxValidationError(f'`out_fixel_directory` has the wrong type: Received `{type(params["out_fixel_directory"])}` expected `str`')

def fixelcfestats_cargs(params: Fixelcfestats, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("fixelcfestats")
    v_0 = params.get("mask")
    if v_0 is not None:
        cargs.append("-mask")
        cargs.append(execution.input_file(v_0))
    if params.get("notest"):
        cargs.append("-notest")
    v_1 = params.get("errors")
    if v_1 is not None:
        cargs.append("-errors")
        cargs.append(v_1)
    v_2 = params.get("exchange_within")
    if v_2 is not None:
        cargs.append("-exchange_within")
        cargs.append(execution.input_file(v_2))
    v_3 = params.get("exchange_whole")
    if v_3 is not None:
        cargs.append("-exchange_whole")
        cargs.append(execution.input_file(v_3))
    if params.get("strong"):
        cargs.append("-strong")
    v_4 = params.get("nshuffles")
    if v_4 is not None:
        cargs.append("-nshuffles")
        cargs.append(str(v_4))
    v_5 = params.get("permutations")
    if v_5 is not None:
        cargs.append("-permutations")
        cargs.append(execution.input_file(v_5))
    if params.get("nonstationarity"):
        cargs.append("-nonstationarity")
    v_6 = params.get("skew_nonstationarity")
    if v_6 is not None:
        cargs.append("-skew_nonstationarity")
        cargs.append(str(v_6))
    v_7 = params.get("nshuffles_nonstationarity")
    if v_7 is not None:
        cargs.append("-nshuffles_nonstationarity")
        cargs.append(str(v_7))
    v_8 = params.get("permutations_nonstationarity")
    if v_8 is not None:
        cargs.append("-permutations_nonstationarity")
        cargs.append(execution.input_file(v_8))
    v_9 = params.get("cfe_dh")
    if v_9 is not None:
        cargs.append("-cfe_dh")
        cargs.append(str(v_9))
    v_10 = params.get("cfe_e")
    if v_10 is not None:
        cargs.append("-cfe_e")
        cargs.append(str(v_10))
    v_11 = params.get("cfe_h")
    if v_11 is not None:
        cargs.append("-cfe_h")
        cargs.append(str(v_11))
    v_12 = params.get("cfe_c")
    if v_12 is not None:
        cargs.append("-cfe_c")
        cargs.append(str(v_12))
    if params.get("cfe_legacy"):
        cargs.append("-cfe_legacy")
    v_13 = params.get("variance")
    if v_13 is not None:
        cargs.append("-variance")
        cargs.append(execution.input_file(v_13))
    v_14 = params.get("ftests")
    if v_14 is not None:
        cargs.append("-ftests")
        cargs.append(execution.input_file(v_14))
    if params.get("fonly"):
        cargs.append("-fonly")
    v_15 = params.get("column")
    if v_15 is not None:
        for item0 in v_15:
            cargs.append("-column")
            cargs.append(execution.input_file(item0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_16 = params.get("nthreads")
    if v_16 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_16))
    v_17 = params.get("config")
    if v_17 is not None:
        for item1 in v_17:
            cargs.append("-config")
            cargs.append(item1["key"])
            cargs.append(item1["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["in_fixel_directory"]))
    cargs.append(execution.input_file(params["subjects"]))
    cargs.append(execution.input_file(params["design"]))
    cargs.append(execution.input_file(params["contrast"]))
    if params["connectivity"]["@type"] == "VariousString":
        cargs.append(str(params["connectivity"]["obj"]))
    elif params["connectivity"]["@type"] == "VariousFile":
        cargs.append(str(params["connectivity"]["obj"]))
    cargs.append(params["out_fixel_directory"])
    return cargs

def fixelcfestats_outputs(params: Fixelcfestats, execution: Execution) -> FixelcfestatsOutputs:
    """Build the FixelcfestatsOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return FixelcfestatsOutputs(
        root=root_v,
    )

def fixelcfestats_execute(params: Fixelcfestats, runner: Runner | None = None) -> FixelcfestatsOutputs:
    """
    fixelcfestats

    Fixel-based analysis using connectivity-based fixel enhancement and non-parametric permutation testing.

Unlike previous versions of this command, where a whole-brain tractogram file would be provided as input in order to generate the fixel-fixel connectivity matrix and smooth fixel data, this version expects to be provided with the directory path to a pre-calculated fixel-fixel connectivity matrix (likely generated using the MRtrix3 command fixelconnectivity), and for the input fixel data to have already been smoothed (likely using the MRtrix3 command fixelfilter).

Note that if the -mask option is used, the output fixel directory will still contain the same set of fixels as that present in the input fixel template, in order to retain fixel correspondence. However a consequence of this is that all fixels in the template will be initialy visible when the output fixel directory is loaded in mrview. Those fixels outside the processing mask will immediately disappear from view as soon as any data-file-based fixel colouring or thresholding is applied.

In some software packages, a column of ones is automatically added to the GLM design matrix; the purpose of this column is to estimate the "global intercept", which is the predicted value of the observed variable if all explanatory variables were to be zero. However there are rare situations where including such a column would not be appropriate for a particular experimental design. Hence, in MRtrix3 statistical inference commands, it is up to the user to determine whether or not this column of ones should be included in their design matrix, and add it explicitly if necessary. The contrast matrix must also reflect the presence of this additional column.

References:

Raffelt, D.; Smith, RE.; Ridgway, GR.; Tournier, JD.; Vaughan, DN.; Rose, S.; Henderson, R.; Connelly, A. Connectivity-based fixel enhancement: Whole-brain statistical analysis of diffusion MRI measures in the presence of crossing fibres.Neuroimage, 2015, 15(117):40-55

* If not using the -cfe_legacy option: 
Smith, RE.; Dimond, D; Vaughan, D.; Parker, D.; Dhollander, T.; Jackson, G.; Connelly, A. Intrinsic non-stationarity correction for Fixel-Based Analysis. In Proc OHBM 2019 M789

* If using the -nonstationary option: 
Salimi-Khorshidi, G. Smith, S.M. Nichols, T.E. Adjusting the effect of nonstationarity in cluster-based and TFCE inference. NeuroImage, 2011, 54(3), 2006-19

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    fixelcfestats_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(FIXELCFESTATS_METADATA)
    execution.params(params)
    args = fixelcfestats_cargs(params, execution)
    out = fixelcfestats_outputs(params, execution)
    execution.run(args)
    return out

def fixelcfestats(
    in_fixel_directory: InputPathType,
    subjects: InputPathType,
    design: InputPathType,
    contrast: InputPathType,
    connectivity: FixelcfestatsConnectivity,
    out_fixel_directory: str,
    mask: InputPathType | None = None,
    notest: bool = False,
    errors: str | None = None,
    exchange_within: InputPathType | None = None,
    exchange_whole: InputPathType | None = None,
    strong: bool = False,
    nshuffles: int | None = None,
    permutations: InputPathType | None = None,
    nonstationarity: bool = False,
    skew_nonstationarity: float | None = None,
    nshuffles_nonstationarity: int | None = None,
    permutations_nonstationarity: InputPathType | None = None,
    cfe_dh: float | None = None,
    cfe_e: float | None = None,
    cfe_h: float | None = None,
    cfe_c: float | None = None,
    cfe_legacy: bool = False,
    variance: InputPathType | None = None,
    ftests: InputPathType | None = None,
    fonly: bool = False,
    column: list[InputPathType] | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[FixelcfestatsConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> FixelcfestatsOutputs:
    """
    fixelcfestats

    Fixel-based analysis using connectivity-based fixel enhancement and non-parametric permutation testing.

Unlike previous versions of this command, where a whole-brain tractogram file would be provided as input in order to generate the fixel-fixel connectivity matrix and smooth fixel data, this version expects to be provided with the directory path to a pre-calculated fixel-fixel connectivity matrix (likely generated using the MRtrix3 command fixelconnectivity), and for the input fixel data to have already been smoothed (likely using the MRtrix3 command fixelfilter).

Note that if the -mask option is used, the output fixel directory will still contain the same set of fixels as that present in the input fixel template, in order to retain fixel correspondence. However a consequence of this is that all fixels in the template will be initialy visible when the output fixel directory is loaded in mrview. Those fixels outside the processing mask will immediately disappear from view as soon as any data-file-based fixel colouring or thresholding is applied.

In some software packages, a column of ones is automatically added to the GLM design matrix; the purpose of this column is to estimate the "global intercept", which is the predicted value of the observed variable if all explanatory variables were to be zero. However there are rare situations where including such a column would not be appropriate for a particular experimental design. Hence, in MRtrix3 statistical inference commands, it is up to the user to determine whether or not this column of ones should be included in their design matrix, and add it explicitly if necessary. The contrast matrix must also reflect the presence of this additional column.

References:

Raffelt, D.; Smith, RE.; Ridgway, GR.; Tournier, JD.; Vaughan, DN.; Rose, S.; Henderson, R.; Connelly, A. Connectivity-based fixel enhancement: Whole-brain statistical analysis of diffusion MRI measures in the presence of crossing fibres.Neuroimage, 2015, 15(117):40-55

* If not using the -cfe_legacy option: 
Smith, RE.; Dimond, D; Vaughan, D.; Parker, D.; Dhollander, T.; Jackson, G.; Connelly, A. Intrinsic non-stationarity correction for Fixel-Based Analysis. In Proc OHBM 2019 M789

* If using the -nonstationary option: 
Salimi-Khorshidi, G. Smith, S.M. Nichols, T.E. Adjusting the effect of nonstationarity in cluster-based and TFCE inference. NeuroImage, 2011, 54(3), 2006-19

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_fixel_directory: the fixel directory containing the data files for each\
            subject (after obtaining fixel correspondence
        subjects: a text file listing the subject identifiers (one per line). This\
            should correspond with the filenames in the fixel directory (including\
            the file extension), and be listed in the same order as the rows of the\
            design matrix.
        design: the design matrix
        contrast: the contrast matrix, specified as rows of weights
        connectivity: the fixel-fixel connectivity matrix
        out_fixel_directory: the output directory where results will be saved. Will\
            be created if it does not exist
        mask: provide a fixel data file containing a mask of those fixels to be\
            used during processing
        notest: don't perform statistical inference; only output population\
            statistics (effect size, stdev etc)
        errors: specify nature of errors for shuffling; options are: ee,ise,both\
            (default: ee)
        exchange_within: specify blocks of observations within each of which data\
            may undergo restricted exchange
        exchange_whole: specify blocks of observations that may be exchanged with\
            one another (for independent and symmetric errors, sign-flipping will\
            occur block-wise)
        strong: use strong familywise error control across multiple hypotheses
        nshuffles: the number of shuffles (default: 5000)
        permutations: manually define the permutations (relabelling). The input\
            should be a text file defining a m x n matrix, where each relabelling\
            is defined as a column vector of size m, and the number of columns, n,\
            defines the number of permutations. Can be generated with the\
            palm_quickperms function in PALM\
            (http://fsl.fmrib.ox.ac.uk/fsl/fslwiki/PALM). Overrides the -nshuffles\
            option.
        nonstationarity: perform non-stationarity correction
        skew_nonstationarity: specify the skew parameter for empirical statistic\
            calculation (default for this command is 1)
        nshuffles_nonstationarity: the number of shuffles to use when precomputing\
            the empirical statistic image for non-stationarity correction (default:\
            5000)
        permutations_nonstationarity: manually define the permutations\
            (relabelling) for computing the emprical statistics for\
            non-stationarity correction. The input should be a text file defining a\
            m x n matrix, where each relabelling is defined as a column vector of\
            size m, and the number of columns, n, defines the number of\
            permutations. Can be generated with the palm_quickperms function in\
            PALM (http://fsl.fmrib.ox.ac.uk/fsl/fslwiki/PALM) Overrides the\
            -nshuffles_nonstationarity option.
        cfe_dh: the height increment used in the cfe integration (default: 0.1)
        cfe_e: cfe extent exponent (default: 2)
        cfe_h: cfe height exponent (default: 3)
        cfe_c: cfe connectivity exponent (default: 0.5)
        cfe_legacy: use the legacy (non-normalised) form of the cfe equation
        variance: define variance groups for the G-statistic; measurements for\
            which the expected variance is equivalent should contain the same index
        ftests: perform F-tests; input text file should contain, for each F-test, a\
            row containing ones and zeros, where ones indicate the rows of the\
            contrast matrix to be included in the F-test.
        fonly: only assess F-tests; do not perform statistical inference on entries\
            in the contrast matrix
        column: add a column to the design matrix corresponding to subject\
            fixel-wise values (note that the contrast matrix must include an\
            additional column for each use of this option); the text file provided\
            via this option should contain a file name for each subject
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = fixelcfestats_params(
        in_fixel_directory=in_fixel_directory,
        subjects=subjects,
        design=design,
        contrast=contrast,
        connectivity=connectivity,
        out_fixel_directory=out_fixel_directory,
        mask=mask,
        notest=notest,
        errors=errors,
        exchange_within=exchange_within,
        exchange_whole=exchange_whole,
        strong=strong,
        nshuffles=nshuffles,
        permutations=permutations,
        nonstationarity=nonstationarity,
        skew_nonstationarity=skew_nonstationarity,
        nshuffles_nonstationarity=nshuffles_nonstationarity,
        permutations_nonstationarity=permutations_nonstationarity,
        cfe_dh=cfe_dh,
        cfe_e=cfe_e,
        cfe_h=cfe_h,
        cfe_c=cfe_c,
        cfe_legacy=cfe_legacy,
        variance=variance,
        ftests=ftests,
        fonly=fonly,
        column=column,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return fixelcfestats_execute(params, runner)

__all__ = [
    "Fixelcfestats",
    "FixelcfestatsOutputs",
    "FIXELCFESTATS_METADATA",
    "fixelcfestats_cargs",
    "fixelcfestats_outputs",
    "fixelcfestats_params",
    "fixelcfestats_execute",
    "fixelcfestats_validate",
    "fixelcfestats",
]