# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

V_5TT2GMWMI_METADATA = Metadata(
    id="5tt2gmwmi",
    name="5tt2gmwmi",
    package="mrtrix",
)

class V5tt2gmwmiConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

V5tt2gmwmi = typing.TypedDict(
    "V5tt2gmwmi",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/5tt2gmwmi"]],
        "mask_in": typing.NotRequired[InputPathType],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[V5tt2gmwmiConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "5tt_in": InputPathType,
        "mask_out": str,
    },
)

@dataclasses.dataclass
class V5tt2gmwmiOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    mask_out: OutputPathType
    """the output mask image"""

def v_5tt2gmwmi_params(
    v_5tt_in: InputPathType,
    mask_out: str,
    mask_in: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[V5tt2gmwmiConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> V5tt2gmwmi:
    """
    Build parameters.

    Args:
        v_5tt_in: the input 5TT segmented anatomical image
        mask_out: the output mask image
        mask_in: Filter an input mask image according to those voxels that lie upon\
            the grey matter - white matter boundary. If no input mask is provided,\
            the output will be a whole-brain mask image calculated using the\
            anatomical image only.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: V5tt2gmwmi = {
        "@type": "mrtrix/5tt2gmwmi",
        "5tt_in": v_5tt_in,
        "mask_out": mask_out,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if mask_in is not None:
        params["mask_in"] = mask_in
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def v_5tt2gmwmi_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid V5tt2gmwmi."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("mask_in", None) is not None:
        if not isinstance(params["mask_in"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_in` has the wrong type: Received `{type(params["mask_in"])}` expected `InputPathType`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[V5tt2gmwmiConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("5tt_in", None) is None:
        raise StyxValidationError("`5tt_in` must not be None")
    if not isinstance(params["5tt_in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`5tt_in` has the wrong type: Received `{type(params["5tt_in"])}` expected `InputPathType`')
    if params.get("mask_out", None) is None:
        raise StyxValidationError("`mask_out` must not be None")
    if not isinstance(params["mask_out"], str):
        raise StyxValidationError(f'`mask_out` has the wrong type: Received `{type(params["mask_out"])}` expected `str`')

def v_5tt2gmwmi_cargs(params: V5tt2gmwmi, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("5tt2gmwmi")
    v_0 = params.get("mask_in")
    if v_0 is not None:
        cargs.append("-mask_in")
        cargs.append(execution.input_file(v_0))
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["5tt_in"]))
    cargs.append(params["mask_out"])
    return cargs

def v_5tt2gmwmi_outputs(params: V5tt2gmwmi, execution: Execution) -> V5tt2gmwmiOutputs:
    """Build the V5tt2gmwmiOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    mask_out_v: OutputPathType = execution.output_file(params["mask_out"])
    return V5tt2gmwmiOutputs(
        root=root_v,
        mask_out=mask_out_v,
    )

def v_5tt2gmwmi_execute(params: V5tt2gmwmi, runner: Runner | None = None) -> V5tt2gmwmiOutputs:
    """
    5tt2gmwmi

    Generate a mask image appropriate for seeding streamlines on the grey matter-white matter interface.



References:

Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. Anatomically-constrained tractography:Improved diffusion MRI streamlines tractography through effective use of anatomical information. NeuroImage, 2012, 62, 1924-1938

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    v_5tt2gmwmi_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(V_5TT2GMWMI_METADATA)
    execution.params(params)
    args = v_5tt2gmwmi_cargs(params, execution)
    out = v_5tt2gmwmi_outputs(params, execution)
    execution.run(args)
    return out

def v_5tt2gmwmi(
    v_5tt_in: InputPathType,
    mask_out: str,
    mask_in: InputPathType | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[V5tt2gmwmiConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> V5tt2gmwmiOutputs:
    """
    5tt2gmwmi

    Generate a mask image appropriate for seeding streamlines on the grey matter-white matter interface.



References:

Smith, R. E.; Tournier, J.-D.; Calamante, F. & Connelly, A. Anatomically-constrained tractography:Improved diffusion MRI streamlines tractography through effective use of anatomical information. NeuroImage, 2012, 62, 1924-1938

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        v_5tt_in: the input 5TT segmented anatomical image
        mask_out: the output mask image
        mask_in: Filter an input mask image according to those voxels that lie upon\
            the grey matter - white matter boundary. If no input mask is provided,\
            the output will be a whole-brain mask image calculated using the\
            anatomical image only.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = v_5tt2gmwmi_params(
        v_5tt_in=v_5tt_in,
        mask_out=mask_out,
        mask_in=mask_in,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return v_5tt2gmwmi_execute(params, runner)

__all__ = [
    "V5tt2gmwmi",
    "V5tt2gmwmiOutputs",
    "V_5TT2GMWMI_METADATA",
    "v_5tt2gmwmi_cargs",
    "v_5tt2gmwmi_outputs",
    "v_5tt2gmwmi_params",
    "v_5tt2gmwmi_execute",
    "v_5tt2gmwmi_validate",
    "v_5tt2gmwmi",
]