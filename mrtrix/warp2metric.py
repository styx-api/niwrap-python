# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

WARP2METRIC_METADATA = Metadata(
    id="warp2metric",
    name="warp2metric",
    package="mrtrix",
)

class Warp2metricFc(typing.TypedDict):
    template_fixel_directory: InputPathType
    """use an input template fixel image to define fibre orientations and output a fixel image describing the change in fibre cross-section (FC) in the perpendicular plane to the fixel orientation. e.g. warp2metric warp.mif -fc fixel_template_directory output_fixel_directory fc.mif"""
    output_fixel_directory: str
    """use an input template fixel image to define fibre orientations and output a fixel image describing the change in fibre cross-section (FC) in the perpendicular plane to the fixel orientation. e.g. warp2metric warp.mif -fc fixel_template_directory output_fixel_directory fc.mif"""
    output_fixel_data: str
    """use an input template fixel image to define fibre orientations and output a fixel image describing the change in fibre cross-section (FC) in the perpendicular plane to the fixel orientation. e.g. warp2metric warp.mif -fc fixel_template_directory output_fixel_directory fc.mif"""

class Warp2metricConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Warp2metric = typing.TypedDict(
    "Warp2metric",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/warp2metric"]],
        "fc": typing.NotRequired[Warp2metricFc],
        "jmat": typing.NotRequired[str],
        "jdet": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Warp2metricConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "in": InputPathType,
    },
)

@dataclasses.dataclass
class Warp2metricOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    jmat: OutputPathType | None
    """output a Jacobian matrix image stored in column-major order along the 4th dimension.Note the output jacobian describes the warp gradient w.r.t the scanner space coordinate system """
    jdet: OutputPathType | None
    """output the Jacobian determinant instead of the full matrix """

def warp2metric_params(
    in_: InputPathType,
    fc: Warp2metricFc | None = None,
    jmat: str | None = None,
    jdet: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Warp2metricConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Warp2metric:
    """
    Build parameters.

    Args:
        in_: the input deformation field
        fc: use an input template fixel image to define fibre orientations and\
            output a fixel image describing the change in fibre cross-section (FC)\
            in the perpendicular plane to the fixel orientation. e.g. warp2metric\
            warp.mif -fc fixel_template_directory output_fixel_directory fc.mif
        jmat: output a Jacobian matrix image stored in column-major order along the\
            4th dimension.Note the output jacobian describes the warp gradient\
            w.r.t the scanner space coordinate system
        jdet: output the Jacobian determinant instead of the full matrix
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Warp2metric = {
        "@type": "mrtrix/warp2metric",
        "in": in_,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if fc is not None:
        params["fc"] = fc
    if jmat is not None:
        params["jmat"] = jmat
    if jdet is not None:
        params["jdet"] = jdet
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def warp2metric_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Warp2metric."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("fc", None) is not None:
        if not isinstance(params["fc"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["fc"])}\'')
        if params["fc"].get("template_fixel_directory", None) is None:
            raise StyxValidationError("`template_fixel_directory` must not be None")
        if not isinstance(params["fc"]["template_fixel_directory"], (pathlib.Path, str)):
            raise StyxValidationError(f'`template_fixel_directory` has the wrong type: Received `{type(params["fc"]["template_fixel_directory"])}` expected `InputPathType`')
        if params["fc"].get("output_fixel_directory", None) is None:
            raise StyxValidationError("`output_fixel_directory` must not be None")
        if not isinstance(params["fc"]["output_fixel_directory"], str):
            raise StyxValidationError(f'`output_fixel_directory` has the wrong type: Received `{type(params["fc"]["output_fixel_directory"])}` expected `str`')
        if params["fc"].get("output_fixel_data", None) is None:
            raise StyxValidationError("`output_fixel_data` must not be None")
        if not isinstance(params["fc"]["output_fixel_data"], str):
            raise StyxValidationError(f'`output_fixel_data` has the wrong type: Received `{type(params["fc"]["output_fixel_data"])}` expected `str`')
    if params.get("jmat", None) is not None:
        if not isinstance(params["jmat"], str):
            raise StyxValidationError(f'`jmat` has the wrong type: Received `{type(params["jmat"])}` expected `str`')
    if params.get("jdet", None) is not None:
        if not isinstance(params["jdet"], str):
            raise StyxValidationError(f'`jdet` has the wrong type: Received `{type(params["jdet"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Warp2metricConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("in", None) is None:
        raise StyxValidationError("`in` must not be None")
    if not isinstance(params["in"], (pathlib.Path, str)):
        raise StyxValidationError(f'`in` has the wrong type: Received `{type(params["in"])}` expected `InputPathType`')

def warp2metric_cargs(params: Warp2metric, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("warp2metric")
    v_0 = params.get("fc")
    if v_0 is not None:
        cargs.append("-fc")
        cargs.append(execution.input_file(v_0["template_fixel_directory"]))
        cargs.append(v_0["output_fixel_directory"])
        cargs.append(v_0["output_fixel_data"])
    v_1 = params.get("jmat")
    if v_1 is not None:
        cargs.append("-jmat")
        cargs.append(v_1)
    v_2 = params.get("jdet")
    if v_2 is not None:
        cargs.append("-jdet")
        cargs.append(v_2)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item0 in v_4:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["in"]))
    return cargs

def warp2metric_outputs(params: Warp2metric, execution: Execution) -> Warp2metricOutputs:
    """Build the Warp2metricOutputs object for this tool."""
    jmat_v: OutputPathType | None = None
    jdet_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("jmat")
    if __v0 is not None:
        jmat_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("jdet")
    if __v1 is not None:
        jdet_v = execution.output_file((__v1 if __v1 is not None else ""))
    return Warp2metricOutputs(
        root=root_v,
        jmat=jmat_v,
        jdet=jdet_v,
    )

def warp2metric_execute(params: Warp2metric, runner: Runner | None = None) -> Warp2metricOutputs:
    """
    warp2metric

    Compute fixel-wise or voxel-wise metrics from a 4D deformation field.



References:

Raffelt, D.; Tournier, JD/; Smith, RE.; Vaughan, DN.; Jackson, G.; Ridgway, GR. Connelly, A.Investigating White Matter Fibre Density and Morphology using Fixel-Based Analysis. Neuroimage, 2017, 144, 58-73, doi: 10.1016/j.neuroimage.2016.09.029

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    warp2metric_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(WARP2METRIC_METADATA)
    execution.params(params)
    args = warp2metric_cargs(params, execution)
    out = warp2metric_outputs(params, execution)
    execution.run(args)
    return out

def warp2metric(
    in_: InputPathType,
    fc: Warp2metricFc | None = None,
    jmat: str | None = None,
    jdet: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Warp2metricConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Warp2metricOutputs:
    """
    warp2metric

    Compute fixel-wise or voxel-wise metrics from a 4D deformation field.



References:

Raffelt, D.; Tournier, JD/; Smith, RE.; Vaughan, DN.; Jackson, G.; Ridgway, GR. Connelly, A.Investigating White Matter Fibre Density and Morphology using Fixel-Based Analysis. Neuroimage, 2017, 144, 58-73, doi: 10.1016/j.neuroimage.2016.09.029

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        in_: the input deformation field
        fc: use an input template fixel image to define fibre orientations and\
            output a fixel image describing the change in fibre cross-section (FC)\
            in the perpendicular plane to the fixel orientation. e.g. warp2metric\
            warp.mif -fc fixel_template_directory output_fixel_directory fc.mif
        jmat: output a Jacobian matrix image stored in column-major order along the\
            4th dimension.Note the output jacobian describes the warp gradient\
            w.r.t the scanner space coordinate system
        jdet: output the Jacobian determinant instead of the full matrix
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = warp2metric_params(
        in_=in_,
        fc=fc,
        jmat=jmat,
        jdet=jdet,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return warp2metric_execute(params, runner)

__all__ = [
    "Warp2metric",
    "Warp2metricOutputs",
    "WARP2METRIC_METADATA",
    "warp2metric_cargs",
    "warp2metric_outputs",
    "warp2metric_params",
    "warp2metric_execute",
    "warp2metric_validate",
    "warp2metric",
]