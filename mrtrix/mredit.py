# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MREDIT_METADATA = Metadata(
    id="mredit",
    name="mredit",
    package="mrtrix",
)

class MreditPlane(typing.TypedDict):
    axis: int
    """fill one or more planes on a particular image axis"""
    coord: list[int]
    """fill one or more planes on a particular image axis"""
    value: float
    """fill one or more planes on a particular image axis"""

class MreditSphere(typing.TypedDict):
    position: list[float]
    """draw a sphere with radius in mm"""
    radius: float
    """draw a sphere with radius in mm"""
    value: float
    """draw a sphere with radius in mm"""

class MreditVoxel(typing.TypedDict):
    position: list[float]
    """change the image value within a single voxel"""
    value: float
    """change the image value within a single voxel"""

class MreditConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mredit = typing.TypedDict(
    "Mredit",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mredit"]],
        "plane": typing.NotRequired[list[MreditPlane]],
        "sphere": typing.NotRequired[list[MreditSphere]],
        "voxel": typing.NotRequired[list[MreditVoxel]],
        "scanner": typing.NotRequired[bool],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MreditConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "input": InputPathType,
        "output": typing.NotRequired[str],
    },
)

@dataclasses.dataclass
class MreditOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    output: OutputPathType | None
    """the (optional) output image"""

def mredit_params(
    input_: InputPathType,
    plane: list[MreditPlane] | None = None,
    sphere: list[MreditSphere] | None = None,
    voxel: list[MreditVoxel] | None = None,
    scanner: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MreditConfig] | None = None,
    help: bool = False,
    version: bool = False,
    output: str | None = None,
) -> Mredit:
    """
    Build parameters.

    Args:
        input_: the input image
        plane: fill one or more planes on a particular image axis
        sphere: draw a sphere with radius in mm
        voxel: change the image value within a single voxel
        scanner: indicate that coordinates are specified in scanner space, rather\
            than as voxel coordinates
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        output: the (optional) output image

    Returns:
        Parameter dictionary.
    """
    params: Mredit = {
        "@type": "mrtrix/mredit",
        "input": input_,
        "scanner": scanner,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if plane is not None:
        params["plane"] = plane
    if sphere is not None:
        params["sphere"] = sphere
    if voxel is not None:
        params["voxel"] = voxel
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    if output is not None:
        params["output"] = output
    return params

def mredit_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mredit."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("plane", None) is not None:
        if not isinstance(params["plane"], list):
            raise StyxValidationError(f'`plane` has the wrong type: Received `{type(params["plane"])}` expected `list[MreditPlane]`')
        for e in params["plane"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("axis", None) is None:
                raise StyxValidationError("`axis` must not be None")
            if not isinstance(e["axis"], int):
                raise StyxValidationError(f'`axis` has the wrong type: Received `{type(e["axis"])}` expected `int`')
            if e.get("coord", None) is None:
                raise StyxValidationError("`coord` must not be None")
            if not isinstance(e["coord"], list):
                raise StyxValidationError(f'`coord` has the wrong type: Received `{type(e["coord"])}` expected `list[int]`')
            for e_2 in e["coord"]:
                if not isinstance(e_2, int):
                    raise StyxValidationError(f'`coord` has the wrong type: Received `{type(e_2)}` expected `list[int]`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], (float, int)):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `float`')
    if params.get("sphere", None) is not None:
        if not isinstance(params["sphere"], list):
            raise StyxValidationError(f'`sphere` has the wrong type: Received `{type(params["sphere"])}` expected `list[MreditSphere]`')
        for e_3 in params["sphere"]:
            if not isinstance(e_3, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_3)}\'')
            if e_3.get("position", None) is None:
                raise StyxValidationError("`position` must not be None")
            if not isinstance(e_3["position"], list):
                raise StyxValidationError(f'`position` has the wrong type: Received `{type(e_3["position"])}` expected `list[float]`')
            for e_4 in e_3["position"]:
                if not isinstance(e_4, (float, int)):
                    raise StyxValidationError(f'`position` has the wrong type: Received `{type(e_4)}` expected `list[float]`')
            if e_3.get("radius", None) is None:
                raise StyxValidationError("`radius` must not be None")
            if not isinstance(e_3["radius"], (float, int)):
                raise StyxValidationError(f'`radius` has the wrong type: Received `{type(e_3["radius"])}` expected `float`')
            if e_3.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_3["value"], (float, int)):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_3["value"])}` expected `float`')
    if params.get("voxel", None) is not None:
        if not isinstance(params["voxel"], list):
            raise StyxValidationError(f'`voxel` has the wrong type: Received `{type(params["voxel"])}` expected `list[MreditVoxel]`')
        for e_5 in params["voxel"]:
            if not isinstance(e_5, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_5)}\'')
            if e_5.get("position", None) is None:
                raise StyxValidationError("`position` must not be None")
            if not isinstance(e_5["position"], list):
                raise StyxValidationError(f'`position` has the wrong type: Received `{type(e_5["position"])}` expected `list[float]`')
            for e_6 in e_5["position"]:
                if not isinstance(e_6, (float, int)):
                    raise StyxValidationError(f'`position` has the wrong type: Received `{type(e_6)}` expected `list[float]`')
            if e_5.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_5["value"], (float, int)):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_5["value"])}` expected `float`')
    if params.get("scanner", None) is not None:
        if not isinstance(params["scanner"], bool):
            raise StyxValidationError(f'`scanner` has the wrong type: Received `{type(params["scanner"])}` expected `bool`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MreditConfig]`')
        for e_7 in params["config"]:
            if not isinstance(e_7, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_7)}\'')
            if e_7.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_7["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_7["key"])}` expected `str`')
            if e_7.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_7["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_7["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("input", None) is None:
        raise StyxValidationError("`input` must not be None")
    if not isinstance(params["input"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input` has the wrong type: Received `{type(params["input"])}` expected `InputPathType`')
    if params.get("output", None) is not None:
        if not isinstance(params["output"], str):
            raise StyxValidationError(f'`output` has the wrong type: Received `{type(params["output"])}` expected `str`')

def mredit_cargs(params: Mredit, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mredit")
    v_0 = params.get("plane")
    if v_0 is not None:
        for item0 in v_0:
            cargs.append("-plane")
            cargs.append(str(item0["axis"]))
            for item1 in item0["coord"]:
                cargs.append(str(item1))
            cargs.append(str(item0["value"]))
    v_1 = params.get("sphere")
    if v_1 is not None:
        for item2 in v_1:
            cargs.append("-sphere")
            for item3 in item2["position"]:
                cargs.append(str(item3))
            cargs.append(str(item2["radius"]))
            cargs.append(str(item2["value"]))
    v_2 = params.get("voxel")
    if v_2 is not None:
        for item4 in v_2:
            cargs.append("-voxel")
            for item5 in item4["position"]:
                cargs.append(str(item5))
            cargs.append(str(item4["value"]))
    if params.get("scanner"):
        cargs.append("-scanner")
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_3 = params.get("nthreads")
    if v_3 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_3))
    v_4 = params.get("config")
    if v_4 is not None:
        for item6 in v_4:
            cargs.append("-config")
            cargs.append(item6["key"])
            cargs.append(item6["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["input"]))
    v_5 = params.get("output")
    if v_5 is not None:
        cargs.append(v_5)
    return cargs

def mredit_outputs(params: Mredit, execution: Execution) -> MreditOutputs:
    """Build the MreditOutputs object for this tool."""
    output_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("output")
    if __v0 is not None:
        output_v = execution.output_file((__v0 if __v0 is not None else ""))
    return MreditOutputs(
        root=root_v,
        output=output_v,
    )

def mredit_execute(params: Mredit, runner: Runner | None = None) -> MreditOutputs:
    """
    mredit

    Directly edit the intensities within an image from the command-line.

A range of options are provided to enable direct editing of voxel intensities based on voxel / real-space coordinates. If only one image path is provided, the image will be edited in-place (use at own risk); if input and output image paths are provided, the output will contain the edited image, and the original image will not be modified in any way.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mredit_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MREDIT_METADATA)
    execution.params(params)
    args = mredit_cargs(params, execution)
    out = mredit_outputs(params, execution)
    execution.run(args)
    return out

def mredit(
    input_: InputPathType,
    plane: list[MreditPlane] | None = None,
    sphere: list[MreditSphere] | None = None,
    voxel: list[MreditVoxel] | None = None,
    scanner: bool = False,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MreditConfig] | None = None,
    help: bool = False,
    version: bool = False,
    output: str | None = None,
    runner: Runner | None = None,
) -> MreditOutputs:
    """
    mredit

    Directly edit the intensities within an image from the command-line.

A range of options are provided to enable direct editing of voxel intensities based on voxel / real-space coordinates. If only one image path is provided, the image will be edited in-place (use at own risk); if input and output image paths are provided, the output will contain the edited image, and the original image will not be modified in any way.

References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        input_: the input image
        plane: fill one or more planes on a particular image axis
        sphere: draw a sphere with radius in mm
        voxel: change the image value within a single voxel
        scanner: indicate that coordinates are specified in scanner space, rather\
            than as voxel coordinates
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        output: the (optional) output image
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mredit_params(
        input_=input_,
        plane=plane,
        sphere=sphere,
        voxel=voxel,
        scanner=scanner,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
        output=output,
    )
    return mredit_execute(params, runner)

__all__ = [
    "Mredit",
    "MreditOutputs",
    "MREDIT_METADATA",
    "mredit_cargs",
    "mredit_outputs",
    "mredit_params",
    "mredit_execute",
    "mredit_validate",
    "mredit",
]