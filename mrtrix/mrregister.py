# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

MRREGISTER_METADATA = Metadata(
    id="mrregister",
    name="mrregister",
    package="mrtrix",
)

class MrregisterTransformedMidway(typing.TypedDict):
    image1_transformed: str
    """image1 and image2 after registration transformed and regridded to the midway space. Note that -transformed_midway needs to be repeated for each contrast if multi-contrast registration is used."""
    image2_transformed: str
    """image1 and image2 after registration transformed and regridded to the midway space. Note that -transformed_midway needs to be repeated for each contrast if multi-contrast registration is used."""

class MrregisterNlWarp(typing.TypedDict):
    warp1: str
    """the non-linear warp output defined as two deformation fields, where warp1 can be used to transform image1->image2 and warp2 to transform image2->image1. The deformation fields also encapsulate any linear transformation estimated prior to non-linear registration."""
    warp2: str
    """the non-linear warp output defined as two deformation fields, where warp1 can be used to transform image1->image2 and warp2 to transform image2->image1. The deformation fields also encapsulate any linear transformation estimated prior to non-linear registration."""

class MrregisterConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Mrregister = typing.TypedDict(
    "Mrregister",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/mrregister"]],
        "type": typing.NotRequired[str],
        "transformed": typing.NotRequired[list[str]],
        "transformed_midway": typing.NotRequired[list[MrregisterTransformedMidway]],
        "mask1": typing.NotRequired[InputPathType],
        "mask2": typing.NotRequired[InputPathType],
        "nan": typing.NotRequired[bool],
        "rigid": typing.NotRequired[str],
        "rigid_1tomidway": typing.NotRequired[str],
        "rigid_2tomidway": typing.NotRequired[str],
        "rigid_init_translation": typing.NotRequired[str],
        "rigid_init_rotation": typing.NotRequired[str],
        "rigid_init_matrix": typing.NotRequired[InputPathType],
        "rigid_scale": typing.NotRequired[list[float]],
        "rigid_niter": typing.NotRequired[list[int]],
        "rigid_metric": typing.NotRequired[str],
        "rigid_metric_diff_estimator": typing.NotRequired[str],
        "rigid_lmax": typing.NotRequired[list[int]],
        "rigid_log": typing.NotRequired[str],
        "affine": typing.NotRequired[str],
        "affine_1tomidway": typing.NotRequired[str],
        "affine_2tomidway": typing.NotRequired[str],
        "affine_init_translation": typing.NotRequired[str],
        "affine_init_rotation": typing.NotRequired[str],
        "affine_init_matrix": typing.NotRequired[InputPathType],
        "affine_scale": typing.NotRequired[list[float]],
        "affine_niter": typing.NotRequired[list[int]],
        "affine_metric": typing.NotRequired[str],
        "affine_metric_diff_estimator": typing.NotRequired[str],
        "affine_lmax": typing.NotRequired[list[int]],
        "affine_log": typing.NotRequired[str],
        "init_translation_unmasked1": typing.NotRequired[bool],
        "init_translation_unmasked2": typing.NotRequired[bool],
        "init_rotation_unmasked1": typing.NotRequired[bool],
        "init_rotation_unmasked2": typing.NotRequired[bool],
        "init_rotation_search_angles": typing.NotRequired[list[float]],
        "init_rotation_search_scale": typing.NotRequired[float],
        "init_rotation_search_directions": typing.NotRequired[int],
        "init_rotation_search_run_global": typing.NotRequired[bool],
        "init_rotation_search_global_iterations": typing.NotRequired[int],
        "linstage_iterations": typing.NotRequired[list[int]],
        "linstage_optimiser_first": typing.NotRequired[str],
        "linstage_optimiser_last": typing.NotRequired[str],
        "linstage_optimiser_default": typing.NotRequired[str],
        "linstage_diagnostics_prefix": typing.NotRequired[str],
        "nl_warp": typing.NotRequired[MrregisterNlWarp],
        "nl_warp_full": typing.NotRequired[str],
        "nl_init": typing.NotRequired[InputPathType],
        "nl_scale": typing.NotRequired[list[float]],
        "nl_niter": typing.NotRequired[list[int]],
        "nl_update_smooth": typing.NotRequired[float],
        "nl_disp_smooth": typing.NotRequired[float],
        "nl_grad_step": typing.NotRequired[float],
        "nl_lmax": typing.NotRequired[list[int]],
        "diagnostics_image": typing.NotRequired[str],
        "directions": typing.NotRequired[InputPathType],
        "noreorientation": typing.NotRequired[bool],
        "mc_weights": typing.NotRequired[list[float]],
        "datatype": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[MrregisterConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "image1_image2": InputPathType,
        "contrast1_contrast2": typing.NotRequired[list[InputPathType]],
    },
)

@dataclasses.dataclass
class MrregisterOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    rigid: OutputPathType | None
    """the output text file containing the rigid transformation as a 4x4 matrix """
    rigid_1tomidway: OutputPathType | None
    """the output text file containing the rigid transformation that aligns image1 to image2 in their common midway space as a 4x4 matrix """
    rigid_2tomidway: OutputPathType | None
    """the output text file containing the rigid transformation that aligns image2 to image1 in their common midway space as a 4x4 matrix """
    rigid_log: OutputPathType | None
    """write gradient descent parameter evolution to log file """
    affine: OutputPathType | None
    """the output text file containing the affine transformation as a 4x4 matrix """
    affine_1tomidway: OutputPathType | None
    """the output text file containing the affine transformation that aligns image1 to image2 in their common midway space as a 4x4 matrix """
    affine_2tomidway: OutputPathType | None
    """the output text file containing the affine transformation that aligns image2 to image1 in their common midway space as a 4x4 matrix """
    affine_log: OutputPathType | None
    """write gradient descent parameter evolution to log file """
    nl_warp_full: OutputPathType | None
    """output all warps used during registration. This saves four different warps that map each image to a midway space and their inverses in a single 5D image file. The 4th image dimension indexes the x,y,z component of the deformation vector and the 5th dimension indexes the field in this order: image1->midway, midway->image1, image2->midway, midway->image2. Where image1->midway defines the field that maps image1 onto the midway space using the reverse convention When linear registration is performed first, the estimated linear transform will be included in the comments of the image header, and therefore the entire linear and non-linear transform can be applied (in either direction) using this output warp file with mrtransform """

def mrregister_params(
    image1_image2: InputPathType,
    type_: str | None = None,
    transformed: list[str] | None = None,
    transformed_midway: list[MrregisterTransformedMidway] | None = None,
    mask1: InputPathType | None = None,
    mask2: InputPathType | None = None,
    nan: bool = False,
    rigid: str | None = None,
    rigid_1tomidway: str | None = None,
    rigid_2tomidway: str | None = None,
    rigid_init_translation: str | None = None,
    rigid_init_rotation: str | None = None,
    rigid_init_matrix: InputPathType | None = None,
    rigid_scale: list[float] | None = None,
    rigid_niter: list[int] | None = None,
    rigid_metric: str | None = None,
    rigid_metric_diff_estimator: str | None = None,
    rigid_lmax: list[int] | None = None,
    rigid_log: str | None = None,
    affine: str | None = None,
    affine_1tomidway: str | None = None,
    affine_2tomidway: str | None = None,
    affine_init_translation: str | None = None,
    affine_init_rotation: str | None = None,
    affine_init_matrix: InputPathType | None = None,
    affine_scale: list[float] | None = None,
    affine_niter: list[int] | None = None,
    affine_metric: str | None = None,
    affine_metric_diff_estimator: str | None = None,
    affine_lmax: list[int] | None = None,
    affine_log: str | None = None,
    init_translation_unmasked1: bool = False,
    init_translation_unmasked2: bool = False,
    init_rotation_unmasked1: bool = False,
    init_rotation_unmasked2: bool = False,
    init_rotation_search_angles: list[float] | None = None,
    init_rotation_search_scale: float | None = None,
    init_rotation_search_directions: int | None = None,
    init_rotation_search_run_global: bool = False,
    init_rotation_search_global_iterations: int | None = None,
    linstage_iterations: list[int] | None = None,
    linstage_optimiser_first: str | None = None,
    linstage_optimiser_last: str | None = None,
    linstage_optimiser_default: str | None = None,
    linstage_diagnostics_prefix: str | None = None,
    nl_warp: MrregisterNlWarp | None = None,
    nl_warp_full: str | None = None,
    nl_init: InputPathType | None = None,
    nl_scale: list[float] | None = None,
    nl_niter: list[int] | None = None,
    nl_update_smooth: float | None = None,
    nl_disp_smooth: float | None = None,
    nl_grad_step: float | None = None,
    nl_lmax: list[int] | None = None,
    diagnostics_image: str | None = None,
    directions: InputPathType | None = None,
    noreorientation: bool = False,
    mc_weights: list[float] | None = None,
    datatype: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrregisterConfig] | None = None,
    help: bool = False,
    version: bool = False,
    contrast1_contrast2: list[InputPathType] | None = None,
) -> Mrregister:
    """
    Build parameters.

    Args:
        image1_image2: input image 1 ('moving') and input image 2 ('template')
        type_: the registration type. Valid choices are: rigid, affine, nonlinear,\
            rigid_affine, rigid_nonlinear, affine_nonlinear, rigid_affine_nonlinear\
            (Default: affine_nonlinear)
        transformed: image1 after registration transformed and regridded to the\
            space of image2. Note that -transformed needs to be repeated for each\
            contrast if multi-contrast registration is used.
        transformed_midway: image1 and image2 after registration transformed and\
            regridded to the midway space. Note that -transformed_midway needs to\
            be repeated for each contrast if multi-contrast registration is used.
        mask1: a mask to define the region of image1 to use for optimisation.
        mask2: a mask to define the region of image2 to use for optimisation.
        nan: use NaN as out of bounds value. (Default: 0.0)
        rigid: the output text file containing the rigid transformation as a 4x4\
            matrix
        rigid_1tomidway: the output text file containing the rigid transformation\
            that aligns image1 to image2 in their common midway space as a 4x4\
            matrix
        rigid_2tomidway: the output text file containing the rigid transformation\
            that aligns image2 to image1 in their common midway space as a 4x4\
            matrix
        rigid_init_translation: initialise the translation and centre of rotation\
            Valid choices are: mass (aligns the centers of mass of both images,\
            default), geometric (aligns geometric image centres) and none.
        rigid_init_rotation: initialise the rotation Valid choices are: search\
            (search for the best rotation using mean squared residuals), moments\
            (rotation based on directions of intensity variance with respect to\
            centre of mass), none (default).
        rigid_init_matrix: initialise either the rigid, affine, or syn registration\
            with the supplied rigid transformation (as a 4x4 matrix in scanner\
            coordinates). Note that this overrides rigid_init_translation and\
            rigid_init_rotation initialisation
        rigid_scale: use a multi-resolution scheme by defining a scale factor for\
            each level using comma separated values (Default: 0.25,0.5,1.0)
        rigid_niter: the maximum number of gradient descent iterations per stage.\
            This can be specified either as a single number for all\
            multi-resolution levels, or a single value for each level. (Default:\
            1000)
        rigid_metric: valid choices are: diff (intensity differences), Default:\
            diff
        rigid_metric_diff_estimator: Valid choices are: l1 (least absolute: |x|),\
            l2 (ordinary least squares), lp (least powers: |x|^1.2), Default: l2
        rigid_lmax: explicitly set the lmax to be used per scale factor in rigid\
            FOD registration. By default FOD registration will use lmax 0,2,4 with\
            default scale factors 0.25,0.5,1.0 respectively. Note that no\
            reorientation will be performed with lmax = 0.
        rigid_log: write gradient descent parameter evolution to log file
        affine: the output text file containing the affine transformation as a 4x4\
            matrix
        affine_1tomidway: the output text file containing the affine transformation\
            that aligns image1 to image2 in their common midway space as a 4x4\
            matrix
        affine_2tomidway: the output text file containing the affine transformation\
            that aligns image2 to image1 in their common midway space as a 4x4\
            matrix
        affine_init_translation: initialise the translation and centre of rotation\
            Valid choices are: mass (aligns the centers of mass of both images),\
            geometric (aligns geometric image centres) and none. (Default: mass)
        affine_init_rotation: initialise the rotation Valid choices are: search\
            (search for the best rotation using mean squared residuals), moments\
            (rotation based on directions of intensity variance with respect to\
            centre of mass), none (Default: none).
        affine_init_matrix: initialise either the affine, or syn registration with\
            the supplied affine transformation (as a 4x4 matrix in scanner\
            coordinates). Note that this overrides affine_init_translation and\
            affine_init_rotation initialisation
        affine_scale: use a multi-resolution scheme by defining a scale factor for\
            each level using comma separated values (Default: 0.25,0.5,1.0)
        affine_niter: the maximum number of gradient descent iterations per stage.\
            This can be specified either as a single number for all\
            multi-resolution levels, or a single value for each level. (Default:\
            1000)
        affine_metric: valid choices are: diff (intensity differences), Default:\
            diff
        affine_metric_diff_estimator: Valid choices are: l1 (least absolute: |x|),\
            l2 (ordinary least squares), lp (least powers: |x|^1.2), Default: l2
        affine_lmax: explicitly set the lmax to be used per scale factor in affine\
            FOD registration. By default FOD registration will use lmax 0,2,4 with\
            default scale factors 0.25,0.5,1.0 respectively. Note that no\
            reorientation will be performed with lmax = 0.
        affine_log: write gradient descent parameter evolution to log file
        init_translation_unmasked1: disregard mask1 for the translation\
            initialisation (affects 'mass')
        init_translation_unmasked2: disregard mask2 for the translation\
            initialisation (affects 'mass')
        init_rotation_unmasked1: disregard mask1 for the rotation initialisation\
            (affects 'search' and 'moments')
        init_rotation_unmasked2: disregard mask2 for the rotation initialisation\
            (affects 'search' and 'moments')
        init_rotation_search_angles: rotation angles for the local search in\
            degrees between 0 and 180. (Default: 2,5,10,15,20)
        init_rotation_search_scale: relative size of the images used for the\
            rotation search. (Default: 0.15)
        init_rotation_search_directions: number of rotation axis for local search.\
            (Default: 250)
        init_rotation_search_run_global: perform a global search. (Default: local)
        init_rotation_search_global_iterations: number of rotations to investigate\
            (Default: 10000)
        linstage_iterations: number of iterations for each registration stage, not\
            to be confused with -rigid_niter or -affine_niter. This can be used to\
            generate intermediate diagnostics images (-linstage.diagnostics.prefix)\
            or to change the cost function optimiser during registration (without\
            the need to repeatedly resize the images). (Default: 1 == no\
            repetition)
        linstage_optimiser_first: Cost function optimisation algorithm to use at\
            first iteration of all stages. Valid choices: bbgd (Barzilai-Borwein\
            gradient descent) or gd (simple gradient descent). (Default: bbgd)
        linstage_optimiser_last: Cost function optimisation algorithm to use at\
            last iteration of all stages (if there are more than one). Valid\
            choices: bbgd (Barzilai-Borwein gradient descent) or gd (simple\
            gradient descent). (Default: bbgd)
        linstage_optimiser_default: Cost function optimisation algorithm to use at\
            any stage iteration other than first or last iteration. Valid choices:\
            bbgd (Barzilai-Borwein gradient descent) or gd (simple gradient\
            descent). (Default: bbgd)
        linstage_diagnostics_prefix: generate diagnostics images after every\
            registration stage
        nl_warp: the non-linear warp output defined as two deformation fields,\
            where warp1 can be used to transform image1->image2 and warp2 to\
            transform image2->image1. The deformation fields also encapsulate any\
            linear transformation estimated prior to non-linear registration.
        nl_warp_full: output all warps used during registration. This saves four\
            different warps that map each image to a midway space and their\
            inverses in a single 5D image file. The 4th image dimension indexes the\
            x,y,z component of the deformation vector and the 5th dimension indexes\
            the field in this order: image1->midway, midway->image1,\
            image2->midway, midway->image2. Where image1->midway defines the field\
            that maps image1 onto the midway space using the reverse convention\
            When linear registration is performed first, the estimated linear\
            transform will be included in the comments of the image header, and\
            therefore the entire linear and non-linear transform can be applied (in\
            either direction) using this output warp file with mrtransform
        nl_init: initialise the non-linear registration with the supplied warp\
            image. The supplied warp must be in the same format as output using the\
            -nl_warp_full option (i.e. have 4 deformation fields with the linear\
            transforms in the image header)
        nl_scale: use a multi-resolution scheme by defining a scale factor for each\
            level using comma separated values (Default: 0.25,0.5,1.0)
        nl_niter: the maximum number of iterations. This can be specified either as\
            a single number for all multi-resolution levels, or a single value for\
            each level. (Default: 50)
        nl_update_smooth: regularise the gradient update field with Gaussian\
            smoothing (standard deviation in voxel units, Default 2.0)
        nl_disp_smooth: regularise the displacement field with Gaussian smoothing\
            (standard deviation in voxel units, Default 1.0)
        nl_grad_step: the gradient step size for non-linear registration (Default:\
            0.5)
        nl_lmax: explicitly set the lmax to be used per scale factor in non-linear\
            FOD registration. By default FOD registration will use lmax 0,2,4 with\
            default scale factors 0.25,0.5,1.0 respectively. Note that no\
            reorientation will be performed with lmax = 0.
        diagnostics_image: write intermediate images for diagnostics purposes
        directions: the directions used for FOD reorientation using apodised point\
            spread functions (Default: 60 directions)
        noreorientation: turn off FOD reorientation. Reorientation is on by default\
            if the number of volumes in the 4th dimension corresponds to the number\
            of coefficients in an antipodally symmetric spherical harmonic series\
            (i.e. 6, 15, 28, 45, 66 etc)
        mc_weights: relative weight of images used for multi-contrast registration.\
            Default: 1.0 (equal weighting)
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        contrast1_contrast2: optional list of additional input images used as\
            additional contrasts. Can be used multiple times. contrastX and imageX\
            must share the same coordinate system.

    Returns:
        Parameter dictionary.
    """
    params: Mrregister = {
        "@type": "mrtrix/mrregister",
        "image1_image2": image1_image2,
        "nan": nan,
        "init_translation_unmasked1": init_translation_unmasked1,
        "init_translation_unmasked2": init_translation_unmasked2,
        "init_rotation_unmasked1": init_rotation_unmasked1,
        "init_rotation_unmasked2": init_rotation_unmasked2,
        "init_rotation_search_run_global": init_rotation_search_run_global,
        "noreorientation": noreorientation,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if type_ is not None:
        params["type"] = type_
    if transformed is not None:
        params["transformed"] = transformed
    if transformed_midway is not None:
        params["transformed_midway"] = transformed_midway
    if mask1 is not None:
        params["mask1"] = mask1
    if mask2 is not None:
        params["mask2"] = mask2
    if rigid is not None:
        params["rigid"] = rigid
    if rigid_1tomidway is not None:
        params["rigid_1tomidway"] = rigid_1tomidway
    if rigid_2tomidway is not None:
        params["rigid_2tomidway"] = rigid_2tomidway
    if rigid_init_translation is not None:
        params["rigid_init_translation"] = rigid_init_translation
    if rigid_init_rotation is not None:
        params["rigid_init_rotation"] = rigid_init_rotation
    if rigid_init_matrix is not None:
        params["rigid_init_matrix"] = rigid_init_matrix
    if rigid_scale is not None:
        params["rigid_scale"] = rigid_scale
    if rigid_niter is not None:
        params["rigid_niter"] = rigid_niter
    if rigid_metric is not None:
        params["rigid_metric"] = rigid_metric
    if rigid_metric_diff_estimator is not None:
        params["rigid_metric_diff_estimator"] = rigid_metric_diff_estimator
    if rigid_lmax is not None:
        params["rigid_lmax"] = rigid_lmax
    if rigid_log is not None:
        params["rigid_log"] = rigid_log
    if affine is not None:
        params["affine"] = affine
    if affine_1tomidway is not None:
        params["affine_1tomidway"] = affine_1tomidway
    if affine_2tomidway is not None:
        params["affine_2tomidway"] = affine_2tomidway
    if affine_init_translation is not None:
        params["affine_init_translation"] = affine_init_translation
    if affine_init_rotation is not None:
        params["affine_init_rotation"] = affine_init_rotation
    if affine_init_matrix is not None:
        params["affine_init_matrix"] = affine_init_matrix
    if affine_scale is not None:
        params["affine_scale"] = affine_scale
    if affine_niter is not None:
        params["affine_niter"] = affine_niter
    if affine_metric is not None:
        params["affine_metric"] = affine_metric
    if affine_metric_diff_estimator is not None:
        params["affine_metric_diff_estimator"] = affine_metric_diff_estimator
    if affine_lmax is not None:
        params["affine_lmax"] = affine_lmax
    if affine_log is not None:
        params["affine_log"] = affine_log
    if init_rotation_search_angles is not None:
        params["init_rotation_search_angles"] = init_rotation_search_angles
    if init_rotation_search_scale is not None:
        params["init_rotation_search_scale"] = init_rotation_search_scale
    if init_rotation_search_directions is not None:
        params["init_rotation_search_directions"] = init_rotation_search_directions
    if init_rotation_search_global_iterations is not None:
        params["init_rotation_search_global_iterations"] = init_rotation_search_global_iterations
    if linstage_iterations is not None:
        params["linstage_iterations"] = linstage_iterations
    if linstage_optimiser_first is not None:
        params["linstage_optimiser_first"] = linstage_optimiser_first
    if linstage_optimiser_last is not None:
        params["linstage_optimiser_last"] = linstage_optimiser_last
    if linstage_optimiser_default is not None:
        params["linstage_optimiser_default"] = linstage_optimiser_default
    if linstage_diagnostics_prefix is not None:
        params["linstage_diagnostics_prefix"] = linstage_diagnostics_prefix
    if nl_warp is not None:
        params["nl_warp"] = nl_warp
    if nl_warp_full is not None:
        params["nl_warp_full"] = nl_warp_full
    if nl_init is not None:
        params["nl_init"] = nl_init
    if nl_scale is not None:
        params["nl_scale"] = nl_scale
    if nl_niter is not None:
        params["nl_niter"] = nl_niter
    if nl_update_smooth is not None:
        params["nl_update_smooth"] = nl_update_smooth
    if nl_disp_smooth is not None:
        params["nl_disp_smooth"] = nl_disp_smooth
    if nl_grad_step is not None:
        params["nl_grad_step"] = nl_grad_step
    if nl_lmax is not None:
        params["nl_lmax"] = nl_lmax
    if diagnostics_image is not None:
        params["diagnostics_image"] = diagnostics_image
    if directions is not None:
        params["directions"] = directions
    if mc_weights is not None:
        params["mc_weights"] = mc_weights
    if datatype is not None:
        params["datatype"] = datatype
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    if contrast1_contrast2 is not None:
        params["contrast1_contrast2"] = contrast1_contrast2
    return params

def mrregister_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Mrregister."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("type", None) is not None:
        if not isinstance(params["type"], str):
            raise StyxValidationError(f'`type` has the wrong type: Received `{type(params["type"])}` expected `str`')
    if params.get("transformed", None) is not None:
        if not isinstance(params["transformed"], list):
            raise StyxValidationError(f'`transformed` has the wrong type: Received `{type(params["transformed"])}` expected `list[str]`')
        for e in params["transformed"]:
            if not isinstance(e, str):
                raise StyxValidationError(f'`transformed` has the wrong type: Received `{type(e)}` expected `list[str]`')
    if params.get("transformed_midway", None) is not None:
        if not isinstance(params["transformed_midway"], list):
            raise StyxValidationError(f'`transformed_midway` has the wrong type: Received `{type(params["transformed_midway"])}` expected `list[MrregisterTransformedMidway]`')
        for e_2 in params["transformed_midway"]:
            if not isinstance(e_2, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_2)}\'')
            if e_2.get("image1_transformed", None) is None:
                raise StyxValidationError("`image1_transformed` must not be None")
            if not isinstance(e_2["image1_transformed"], str):
                raise StyxValidationError(f'`image1_transformed` has the wrong type: Received `{type(e_2["image1_transformed"])}` expected `str`')
            if e_2.get("image2_transformed", None) is None:
                raise StyxValidationError("`image2_transformed` must not be None")
            if not isinstance(e_2["image2_transformed"], str):
                raise StyxValidationError(f'`image2_transformed` has the wrong type: Received `{type(e_2["image2_transformed"])}` expected `str`')
    if params.get("mask1", None) is not None:
        if not isinstance(params["mask1"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask1` has the wrong type: Received `{type(params["mask1"])}` expected `InputPathType`')
    if params.get("mask2", None) is not None:
        if not isinstance(params["mask2"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask2` has the wrong type: Received `{type(params["mask2"])}` expected `InputPathType`')
    if params.get("nan", None) is not None:
        if not isinstance(params["nan"], bool):
            raise StyxValidationError(f'`nan` has the wrong type: Received `{type(params["nan"])}` expected `bool`')
    if params.get("rigid", None) is not None:
        if not isinstance(params["rigid"], str):
            raise StyxValidationError(f'`rigid` has the wrong type: Received `{type(params["rigid"])}` expected `str`')
    if params.get("rigid_1tomidway", None) is not None:
        if not isinstance(params["rigid_1tomidway"], str):
            raise StyxValidationError(f'`rigid_1tomidway` has the wrong type: Received `{type(params["rigid_1tomidway"])}` expected `str`')
    if params.get("rigid_2tomidway", None) is not None:
        if not isinstance(params["rigid_2tomidway"], str):
            raise StyxValidationError(f'`rigid_2tomidway` has the wrong type: Received `{type(params["rigid_2tomidway"])}` expected `str`')
    if params.get("rigid_init_translation", None) is not None:
        if not isinstance(params["rigid_init_translation"], str):
            raise StyxValidationError(f'`rigid_init_translation` has the wrong type: Received `{type(params["rigid_init_translation"])}` expected `str`')
    if params.get("rigid_init_rotation", None) is not None:
        if not isinstance(params["rigid_init_rotation"], str):
            raise StyxValidationError(f'`rigid_init_rotation` has the wrong type: Received `{type(params["rigid_init_rotation"])}` expected `str`')
    if params.get("rigid_init_matrix", None) is not None:
        if not isinstance(params["rigid_init_matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`rigid_init_matrix` has the wrong type: Received `{type(params["rigid_init_matrix"])}` expected `InputPathType`')
    if params.get("rigid_scale", None) is not None:
        if not isinstance(params["rigid_scale"], list):
            raise StyxValidationError(f'`rigid_scale` has the wrong type: Received `{type(params["rigid_scale"])}` expected `list[float]`')
        for e_3 in params["rigid_scale"]:
            if not isinstance(e_3, (float, int)):
                raise StyxValidationError(f'`rigid_scale` has the wrong type: Received `{type(e_3)}` expected `list[float]`')
    if params.get("rigid_niter", None) is not None:
        if not isinstance(params["rigid_niter"], list):
            raise StyxValidationError(f'`rigid_niter` has the wrong type: Received `{type(params["rigid_niter"])}` expected `list[int]`')
        for e_4 in params["rigid_niter"]:
            if not isinstance(e_4, int):
                raise StyxValidationError(f'`rigid_niter` has the wrong type: Received `{type(e_4)}` expected `list[int]`')
    if params.get("rigid_metric", None) is not None:
        if not isinstance(params["rigid_metric"], str):
            raise StyxValidationError(f'`rigid_metric` has the wrong type: Received `{type(params["rigid_metric"])}` expected `str`')
    if params.get("rigid_metric_diff_estimator", None) is not None:
        if not isinstance(params["rigid_metric_diff_estimator"], str):
            raise StyxValidationError(f'`rigid_metric_diff_estimator` has the wrong type: Received `{type(params["rigid_metric_diff_estimator"])}` expected `str`')
    if params.get("rigid_lmax", None) is not None:
        if not isinstance(params["rigid_lmax"], list):
            raise StyxValidationError(f'`rigid_lmax` has the wrong type: Received `{type(params["rigid_lmax"])}` expected `list[int]`')
        for e_5 in params["rigid_lmax"]:
            if not isinstance(e_5, int):
                raise StyxValidationError(f'`rigid_lmax` has the wrong type: Received `{type(e_5)}` expected `list[int]`')
    if params.get("rigid_log", None) is not None:
        if not isinstance(params["rigid_log"], str):
            raise StyxValidationError(f'`rigid_log` has the wrong type: Received `{type(params["rigid_log"])}` expected `str`')
    if params.get("affine", None) is not None:
        if not isinstance(params["affine"], str):
            raise StyxValidationError(f'`affine` has the wrong type: Received `{type(params["affine"])}` expected `str`')
    if params.get("affine_1tomidway", None) is not None:
        if not isinstance(params["affine_1tomidway"], str):
            raise StyxValidationError(f'`affine_1tomidway` has the wrong type: Received `{type(params["affine_1tomidway"])}` expected `str`')
    if params.get("affine_2tomidway", None) is not None:
        if not isinstance(params["affine_2tomidway"], str):
            raise StyxValidationError(f'`affine_2tomidway` has the wrong type: Received `{type(params["affine_2tomidway"])}` expected `str`')
    if params.get("affine_init_translation", None) is not None:
        if not isinstance(params["affine_init_translation"], str):
            raise StyxValidationError(f'`affine_init_translation` has the wrong type: Received `{type(params["affine_init_translation"])}` expected `str`')
    if params.get("affine_init_rotation", None) is not None:
        if not isinstance(params["affine_init_rotation"], str):
            raise StyxValidationError(f'`affine_init_rotation` has the wrong type: Received `{type(params["affine_init_rotation"])}` expected `str`')
    if params.get("affine_init_matrix", None) is not None:
        if not isinstance(params["affine_init_matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`affine_init_matrix` has the wrong type: Received `{type(params["affine_init_matrix"])}` expected `InputPathType`')
    if params.get("affine_scale", None) is not None:
        if not isinstance(params["affine_scale"], list):
            raise StyxValidationError(f'`affine_scale` has the wrong type: Received `{type(params["affine_scale"])}` expected `list[float]`')
        for e_6 in params["affine_scale"]:
            if not isinstance(e_6, (float, int)):
                raise StyxValidationError(f'`affine_scale` has the wrong type: Received `{type(e_6)}` expected `list[float]`')
    if params.get("affine_niter", None) is not None:
        if not isinstance(params["affine_niter"], list):
            raise StyxValidationError(f'`affine_niter` has the wrong type: Received `{type(params["affine_niter"])}` expected `list[int]`')
        for e_7 in params["affine_niter"]:
            if not isinstance(e_7, int):
                raise StyxValidationError(f'`affine_niter` has the wrong type: Received `{type(e_7)}` expected `list[int]`')
    if params.get("affine_metric", None) is not None:
        if not isinstance(params["affine_metric"], str):
            raise StyxValidationError(f'`affine_metric` has the wrong type: Received `{type(params["affine_metric"])}` expected `str`')
    if params.get("affine_metric_diff_estimator", None) is not None:
        if not isinstance(params["affine_metric_diff_estimator"], str):
            raise StyxValidationError(f'`affine_metric_diff_estimator` has the wrong type: Received `{type(params["affine_metric_diff_estimator"])}` expected `str`')
    if params.get("affine_lmax", None) is not None:
        if not isinstance(params["affine_lmax"], list):
            raise StyxValidationError(f'`affine_lmax` has the wrong type: Received `{type(params["affine_lmax"])}` expected `list[int]`')
        for e_8 in params["affine_lmax"]:
            if not isinstance(e_8, int):
                raise StyxValidationError(f'`affine_lmax` has the wrong type: Received `{type(e_8)}` expected `list[int]`')
    if params.get("affine_log", None) is not None:
        if not isinstance(params["affine_log"], str):
            raise StyxValidationError(f'`affine_log` has the wrong type: Received `{type(params["affine_log"])}` expected `str`')
    if params.get("init_translation_unmasked1", None) is not None:
        if not isinstance(params["init_translation_unmasked1"], bool):
            raise StyxValidationError(f'`init_translation_unmasked1` has the wrong type: Received `{type(params["init_translation_unmasked1"])}` expected `bool`')
    if params.get("init_translation_unmasked2", None) is not None:
        if not isinstance(params["init_translation_unmasked2"], bool):
            raise StyxValidationError(f'`init_translation_unmasked2` has the wrong type: Received `{type(params["init_translation_unmasked2"])}` expected `bool`')
    if params.get("init_rotation_unmasked1", None) is not None:
        if not isinstance(params["init_rotation_unmasked1"], bool):
            raise StyxValidationError(f'`init_rotation_unmasked1` has the wrong type: Received `{type(params["init_rotation_unmasked1"])}` expected `bool`')
    if params.get("init_rotation_unmasked2", None) is not None:
        if not isinstance(params["init_rotation_unmasked2"], bool):
            raise StyxValidationError(f'`init_rotation_unmasked2` has the wrong type: Received `{type(params["init_rotation_unmasked2"])}` expected `bool`')
    if params.get("init_rotation_search_angles", None) is not None:
        if not isinstance(params["init_rotation_search_angles"], list):
            raise StyxValidationError(f'`init_rotation_search_angles` has the wrong type: Received `{type(params["init_rotation_search_angles"])}` expected `list[float]`')
        for e_9 in params["init_rotation_search_angles"]:
            if not isinstance(e_9, (float, int)):
                raise StyxValidationError(f'`init_rotation_search_angles` has the wrong type: Received `{type(e_9)}` expected `list[float]`')
    if params.get("init_rotation_search_scale", None) is not None:
        if not isinstance(params["init_rotation_search_scale"], (float, int)):
            raise StyxValidationError(f'`init_rotation_search_scale` has the wrong type: Received `{type(params["init_rotation_search_scale"])}` expected `float`')
    if params.get("init_rotation_search_directions", None) is not None:
        if not isinstance(params["init_rotation_search_directions"], int):
            raise StyxValidationError(f'`init_rotation_search_directions` has the wrong type: Received `{type(params["init_rotation_search_directions"])}` expected `int`')
    if params.get("init_rotation_search_run_global", None) is not None:
        if not isinstance(params["init_rotation_search_run_global"], bool):
            raise StyxValidationError(f'`init_rotation_search_run_global` has the wrong type: Received `{type(params["init_rotation_search_run_global"])}` expected `bool`')
    if params.get("init_rotation_search_global_iterations", None) is not None:
        if not isinstance(params["init_rotation_search_global_iterations"], int):
            raise StyxValidationError(f'`init_rotation_search_global_iterations` has the wrong type: Received `{type(params["init_rotation_search_global_iterations"])}` expected `int`')
    if params.get("linstage_iterations", None) is not None:
        if not isinstance(params["linstage_iterations"], list):
            raise StyxValidationError(f'`linstage_iterations` has the wrong type: Received `{type(params["linstage_iterations"])}` expected `list[int]`')
        for e_10 in params["linstage_iterations"]:
            if not isinstance(e_10, int):
                raise StyxValidationError(f'`linstage_iterations` has the wrong type: Received `{type(e_10)}` expected `list[int]`')
    if params.get("linstage_optimiser_first", None) is not None:
        if not isinstance(params["linstage_optimiser_first"], str):
            raise StyxValidationError(f'`linstage_optimiser_first` has the wrong type: Received `{type(params["linstage_optimiser_first"])}` expected `str`')
    if params.get("linstage_optimiser_last", None) is not None:
        if not isinstance(params["linstage_optimiser_last"], str):
            raise StyxValidationError(f'`linstage_optimiser_last` has the wrong type: Received `{type(params["linstage_optimiser_last"])}` expected `str`')
    if params.get("linstage_optimiser_default", None) is not None:
        if not isinstance(params["linstage_optimiser_default"], str):
            raise StyxValidationError(f'`linstage_optimiser_default` has the wrong type: Received `{type(params["linstage_optimiser_default"])}` expected `str`')
    if params.get("linstage_diagnostics_prefix", None) is not None:
        if not isinstance(params["linstage_diagnostics_prefix"], str):
            raise StyxValidationError(f'`linstage_diagnostics_prefix` has the wrong type: Received `{type(params["linstage_diagnostics_prefix"])}` expected `str`')
    if params.get("nl_warp", None) is not None:
        if not isinstance(params["nl_warp"], dict):
            raise StyxValidationError(f'Params object has the wrong type \'{type(params["nl_warp"])}\'')
        if params["nl_warp"].get("warp1", None) is None:
            raise StyxValidationError("`warp1` must not be None")
        if not isinstance(params["nl_warp"]["warp1"], str):
            raise StyxValidationError(f'`warp1` has the wrong type: Received `{type(params["nl_warp"]["warp1"])}` expected `str`')
        if params["nl_warp"].get("warp2", None) is None:
            raise StyxValidationError("`warp2` must not be None")
        if not isinstance(params["nl_warp"]["warp2"], str):
            raise StyxValidationError(f'`warp2` has the wrong type: Received `{type(params["nl_warp"]["warp2"])}` expected `str`')
    if params.get("nl_warp_full", None) is not None:
        if not isinstance(params["nl_warp_full"], str):
            raise StyxValidationError(f'`nl_warp_full` has the wrong type: Received `{type(params["nl_warp_full"])}` expected `str`')
    if params.get("nl_init", None) is not None:
        if not isinstance(params["nl_init"], (pathlib.Path, str)):
            raise StyxValidationError(f'`nl_init` has the wrong type: Received `{type(params["nl_init"])}` expected `InputPathType`')
    if params.get("nl_scale", None) is not None:
        if not isinstance(params["nl_scale"], list):
            raise StyxValidationError(f'`nl_scale` has the wrong type: Received `{type(params["nl_scale"])}` expected `list[float]`')
        for e_11 in params["nl_scale"]:
            if not isinstance(e_11, (float, int)):
                raise StyxValidationError(f'`nl_scale` has the wrong type: Received `{type(e_11)}` expected `list[float]`')
    if params.get("nl_niter", None) is not None:
        if not isinstance(params["nl_niter"], list):
            raise StyxValidationError(f'`nl_niter` has the wrong type: Received `{type(params["nl_niter"])}` expected `list[int]`')
        for e_12 in params["nl_niter"]:
            if not isinstance(e_12, int):
                raise StyxValidationError(f'`nl_niter` has the wrong type: Received `{type(e_12)}` expected `list[int]`')
    if params.get("nl_update_smooth", None) is not None:
        if not isinstance(params["nl_update_smooth"], (float, int)):
            raise StyxValidationError(f'`nl_update_smooth` has the wrong type: Received `{type(params["nl_update_smooth"])}` expected `float`')
    if params.get("nl_disp_smooth", None) is not None:
        if not isinstance(params["nl_disp_smooth"], (float, int)):
            raise StyxValidationError(f'`nl_disp_smooth` has the wrong type: Received `{type(params["nl_disp_smooth"])}` expected `float`')
    if params.get("nl_grad_step", None) is not None:
        if not isinstance(params["nl_grad_step"], (float, int)):
            raise StyxValidationError(f'`nl_grad_step` has the wrong type: Received `{type(params["nl_grad_step"])}` expected `float`')
    if params.get("nl_lmax", None) is not None:
        if not isinstance(params["nl_lmax"], list):
            raise StyxValidationError(f'`nl_lmax` has the wrong type: Received `{type(params["nl_lmax"])}` expected `list[int]`')
        for e_13 in params["nl_lmax"]:
            if not isinstance(e_13, int):
                raise StyxValidationError(f'`nl_lmax` has the wrong type: Received `{type(e_13)}` expected `list[int]`')
    if params.get("diagnostics_image", None) is not None:
        if not isinstance(params["diagnostics_image"], str):
            raise StyxValidationError(f'`diagnostics_image` has the wrong type: Received `{type(params["diagnostics_image"])}` expected `str`')
    if params.get("directions", None) is not None:
        if not isinstance(params["directions"], (pathlib.Path, str)):
            raise StyxValidationError(f'`directions` has the wrong type: Received `{type(params["directions"])}` expected `InputPathType`')
    if params.get("noreorientation", None) is not None:
        if not isinstance(params["noreorientation"], bool):
            raise StyxValidationError(f'`noreorientation` has the wrong type: Received `{type(params["noreorientation"])}` expected `bool`')
    if params.get("mc_weights", None) is not None:
        if not isinstance(params["mc_weights"], list):
            raise StyxValidationError(f'`mc_weights` has the wrong type: Received `{type(params["mc_weights"])}` expected `list[float]`')
        for e_14 in params["mc_weights"]:
            if not isinstance(e_14, (float, int)):
                raise StyxValidationError(f'`mc_weights` has the wrong type: Received `{type(e_14)}` expected `list[float]`')
    if params.get("datatype", None) is not None:
        if not isinstance(params["datatype"], str):
            raise StyxValidationError(f'`datatype` has the wrong type: Received `{type(params["datatype"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[MrregisterConfig]`')
        for e_15 in params["config"]:
            if not isinstance(e_15, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e_15)}\'')
            if e_15.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e_15["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e_15["key"])}` expected `str`')
            if e_15.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e_15["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e_15["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("image1_image2", None) is None:
        raise StyxValidationError("`image1_image2` must not be None")
    if not isinstance(params["image1_image2"], (pathlib.Path, str)):
        raise StyxValidationError(f'`image1_image2` has the wrong type: Received `{type(params["image1_image2"])}` expected `InputPathType`')
    if params.get("contrast1_contrast2", None) is not None:
        if not isinstance(params["contrast1_contrast2"], list):
            raise StyxValidationError(f'`contrast1_contrast2` has the wrong type: Received `{type(params["contrast1_contrast2"])}` expected `list[InputPathType]`')
        for e_16 in params["contrast1_contrast2"]:
            if not isinstance(e_16, (pathlib.Path, str)):
                raise StyxValidationError(f'`contrast1_contrast2` has the wrong type: Received `{type(e_16)}` expected `list[InputPathType]`')

def mrregister_cargs(params: Mrregister, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("mrregister")
    v_0 = params.get("type")
    if v_0 is not None:
        cargs.append("-type")
        cargs.append(v_0)
    v_1 = params.get("transformed")
    if v_1 is not None:
        for item0 in v_1:
            cargs.append("-transformed")
            cargs.append(item0)
    v_2 = params.get("transformed_midway")
    if v_2 is not None:
        for item1 in v_2:
            cargs.append("-transformed_midway")
            cargs.append(item1["image1_transformed"])
            cargs.append(item1["image2_transformed"])
    v_3 = params.get("mask1")
    if v_3 is not None:
        cargs.append("-mask1")
        cargs.append(execution.input_file(v_3))
    v_4 = params.get("mask2")
    if v_4 is not None:
        cargs.append("-mask2")
        cargs.append(execution.input_file(v_4))
    if params.get("nan"):
        cargs.append("-nan")
    v_5 = params.get("rigid")
    if v_5 is not None:
        cargs.append("-rigid")
        cargs.append(v_5)
    v_6 = params.get("rigid_1tomidway")
    if v_6 is not None:
        cargs.append("-rigid_1tomidway")
        cargs.append(v_6)
    v_7 = params.get("rigid_2tomidway")
    if v_7 is not None:
        cargs.append("-rigid_2tomidway")
        cargs.append(v_7)
    v_8 = params.get("rigid_init_translation")
    if v_8 is not None:
        cargs.append("-rigid_init_translation")
        cargs.append(v_8)
    v_9 = params.get("rigid_init_rotation")
    if v_9 is not None:
        cargs.append("-rigid_init_rotation")
        cargs.append(v_9)
    v_10 = params.get("rigid_init_matrix")
    if v_10 is not None:
        cargs.append("-rigid_init_matrix")
        cargs.append(execution.input_file(v_10))
    v_11 = params.get("rigid_scale")
    if v_11 is not None:
        cargs.append("-rigid_scale")
        for item2 in v_11:
            cargs.append(str(item2))
    v_12 = params.get("rigid_niter")
    if v_12 is not None:
        cargs.append("-rigid_niter")
        for item3 in v_12:
            cargs.append(str(item3))
    v_13 = params.get("rigid_metric")
    if v_13 is not None:
        cargs.append("-rigid_metric")
        cargs.append(v_13)
    v_14 = params.get("rigid_metric_diff_estimator")
    if v_14 is not None:
        cargs.append("-rigid_metric.diff.estimator")
        cargs.append(v_14)
    v_15 = params.get("rigid_lmax")
    if v_15 is not None:
        cargs.append("-rigid_lmax")
        for item4 in v_15:
            cargs.append(str(item4))
    v_16 = params.get("rigid_log")
    if v_16 is not None:
        cargs.append("-rigid_log")
        cargs.append(v_16)
    v_17 = params.get("affine")
    if v_17 is not None:
        cargs.append("-affine")
        cargs.append(v_17)
    v_18 = params.get("affine_1tomidway")
    if v_18 is not None:
        cargs.append("-affine_1tomidway")
        cargs.append(v_18)
    v_19 = params.get("affine_2tomidway")
    if v_19 is not None:
        cargs.append("-affine_2tomidway")
        cargs.append(v_19)
    v_20 = params.get("affine_init_translation")
    if v_20 is not None:
        cargs.append("-affine_init_translation")
        cargs.append(v_20)
    v_21 = params.get("affine_init_rotation")
    if v_21 is not None:
        cargs.append("-affine_init_rotation")
        cargs.append(v_21)
    v_22 = params.get("affine_init_matrix")
    if v_22 is not None:
        cargs.append("-affine_init_matrix")
        cargs.append(execution.input_file(v_22))
    v_23 = params.get("affine_scale")
    if v_23 is not None:
        cargs.append("-affine_scale")
        for item5 in v_23:
            cargs.append(str(item5))
    v_24 = params.get("affine_niter")
    if v_24 is not None:
        cargs.append("-affine_niter")
        for item6 in v_24:
            cargs.append(str(item6))
    v_25 = params.get("affine_metric")
    if v_25 is not None:
        cargs.append("-affine_metric")
        cargs.append(v_25)
    v_26 = params.get("affine_metric_diff_estimator")
    if v_26 is not None:
        cargs.append("-affine_metric.diff.estimator")
        cargs.append(v_26)
    v_27 = params.get("affine_lmax")
    if v_27 is not None:
        cargs.append("-affine_lmax")
        for item7 in v_27:
            cargs.append(str(item7))
    v_28 = params.get("affine_log")
    if v_28 is not None:
        cargs.append("-affine_log")
        cargs.append(v_28)
    if params.get("init_translation_unmasked1"):
        cargs.append("-init_translation.unmasked1")
    if params.get("init_translation_unmasked2"):
        cargs.append("-init_translation.unmasked2")
    if params.get("init_rotation_unmasked1"):
        cargs.append("-init_rotation.unmasked1")
    if params.get("init_rotation_unmasked2"):
        cargs.append("-init_rotation.unmasked2")
    v_29 = params.get("init_rotation_search_angles")
    if v_29 is not None:
        cargs.append("-init_rotation.search.angles")
        for item8 in v_29:
            cargs.append(str(item8))
    v_30 = params.get("init_rotation_search_scale")
    if v_30 is not None:
        cargs.append("-init_rotation.search.scale")
        cargs.append(str(v_30))
    v_31 = params.get("init_rotation_search_directions")
    if v_31 is not None:
        cargs.append("-init_rotation.search.directions")
        cargs.append(str(v_31))
    if params.get("init_rotation_search_run_global"):
        cargs.append("-init_rotation.search.run_global")
    v_32 = params.get("init_rotation_search_global_iterations")
    if v_32 is not None:
        cargs.append("-init_rotation.search.global.iterations")
        cargs.append(str(v_32))
    v_33 = params.get("linstage_iterations")
    if v_33 is not None:
        cargs.append("-linstage.iterations")
        for item9 in v_33:
            cargs.append(str(item9))
    v_34 = params.get("linstage_optimiser_first")
    if v_34 is not None:
        cargs.append("-linstage.optimiser.first")
        cargs.append(v_34)
    v_35 = params.get("linstage_optimiser_last")
    if v_35 is not None:
        cargs.append("-linstage.optimiser.last")
        cargs.append(v_35)
    v_36 = params.get("linstage_optimiser_default")
    if v_36 is not None:
        cargs.append("-linstage.optimiser.default")
        cargs.append(v_36)
    v_37 = params.get("linstage_diagnostics_prefix")
    if v_37 is not None:
        cargs.append("-linstage.diagnostics.prefix")
        cargs.append(v_37)
    v_38 = params.get("nl_warp")
    if v_38 is not None:
        cargs.append("-nl_warp")
        cargs.append(v_38["warp1"])
        cargs.append(v_38["warp2"])
    v_39 = params.get("nl_warp_full")
    if v_39 is not None:
        cargs.append("-nl_warp_full")
        cargs.append(v_39)
    v_40 = params.get("nl_init")
    if v_40 is not None:
        cargs.append("-nl_init")
        cargs.append(execution.input_file(v_40))
    v_41 = params.get("nl_scale")
    if v_41 is not None:
        cargs.append("-nl_scale")
        for item10 in v_41:
            cargs.append(str(item10))
    v_42 = params.get("nl_niter")
    if v_42 is not None:
        cargs.append("-nl_niter")
        for item11 in v_42:
            cargs.append(str(item11))
    v_43 = params.get("nl_update_smooth")
    if v_43 is not None:
        cargs.append("-nl_update_smooth")
        cargs.append(str(v_43))
    v_44 = params.get("nl_disp_smooth")
    if v_44 is not None:
        cargs.append("-nl_disp_smooth")
        cargs.append(str(v_44))
    v_45 = params.get("nl_grad_step")
    if v_45 is not None:
        cargs.append("-nl_grad_step")
        cargs.append(str(v_45))
    v_46 = params.get("nl_lmax")
    if v_46 is not None:
        cargs.append("-nl_lmax")
        for item12 in v_46:
            cargs.append(str(item12))
    v_47 = params.get("diagnostics_image")
    if v_47 is not None:
        cargs.append("-diagnostics_image")
        cargs.append(v_47)
    v_48 = params.get("directions")
    if v_48 is not None:
        cargs.append("-directions")
        cargs.append(execution.input_file(v_48))
    if params.get("noreorientation"):
        cargs.append("-noreorientation")
    v_49 = params.get("mc_weights")
    if v_49 is not None:
        cargs.append("-mc_weights")
        for item13 in v_49:
            cargs.append(str(item13))
    v_50 = params.get("datatype")
    if v_50 is not None:
        cargs.append("-datatype")
        cargs.append(v_50)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_51 = params.get("nthreads")
    if v_51 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_51))
    v_52 = params.get("config")
    if v_52 is not None:
        for item14 in v_52:
            cargs.append("-config")
            cargs.append(item14["key"])
            cargs.append(item14["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["image1_image2"]))
    v_53 = params.get("contrast1_contrast2")
    if v_53 is not None:
        for item15 in v_53:
            cargs.append(execution.input_file(item15))
    return cargs

def mrregister_outputs(params: Mrregister, execution: Execution) -> MrregisterOutputs:
    """Build the MrregisterOutputs object for this tool."""
    rigid_v: OutputPathType | None = None
    rigid_1tomidway_v: OutputPathType | None = None
    rigid_2tomidway_v: OutputPathType | None = None
    rigid_log_v: OutputPathType | None = None
    affine_v: OutputPathType | None = None
    affine_1tomidway_v: OutputPathType | None = None
    affine_2tomidway_v: OutputPathType | None = None
    affine_log_v: OutputPathType | None = None
    nl_warp_full_v: OutputPathType | None = None
    root_v: OutputPathType = execution.output_file(".")
    __v0 = params.get("rigid")
    if __v0 is not None:
        rigid_v = execution.output_file((__v0 if __v0 is not None else ""))
    __v1 = params.get("rigid_1tomidway")
    if __v1 is not None:
        rigid_1tomidway_v = execution.output_file((__v1 if __v1 is not None else ""))
    __v2 = params.get("rigid_2tomidway")
    if __v2 is not None:
        rigid_2tomidway_v = execution.output_file((__v2 if __v2 is not None else ""))
    __v3 = params.get("rigid_log")
    if __v3 is not None:
        rigid_log_v = execution.output_file((__v3 if __v3 is not None else ""))
    __v4 = params.get("affine")
    if __v4 is not None:
        affine_v = execution.output_file((__v4 if __v4 is not None else ""))
    __v5 = params.get("affine_1tomidway")
    if __v5 is not None:
        affine_1tomidway_v = execution.output_file((__v5 if __v5 is not None else ""))
    __v6 = params.get("affine_2tomidway")
    if __v6 is not None:
        affine_2tomidway_v = execution.output_file((__v6 if __v6 is not None else ""))
    __v7 = params.get("affine_log")
    if __v7 is not None:
        affine_log_v = execution.output_file((__v7 if __v7 is not None else ""))
    __v8 = params.get("nl_warp_full")
    if __v8 is not None:
        nl_warp_full_v = execution.output_file((__v8 if __v8 is not None else ""))
    return MrregisterOutputs(
        root=root_v,
        rigid=rigid_v,
        rigid_1tomidway=rigid_1tomidway_v,
        rigid_2tomidway=rigid_2tomidway_v,
        rigid_log=rigid_log_v,
        affine=affine_v,
        affine_1tomidway=affine_1tomidway_v,
        affine_2tomidway=affine_2tomidway_v,
        affine_log=affine_log_v,
        nl_warp_full=nl_warp_full_v,
    )

def mrregister_execute(params: Mrregister, runner: Runner | None = None) -> MrregisterOutputs:
    """
    mrregister

    Register two images together using a symmetric rigid, affine or non-linear transformation model.

By default this application will perform an affine, followed by non-linear registration.

FOD registration (with apodised point spread reorientation) will be performed by default if the number of volumes in the 4th dimension equals the number of coefficients in an antipodally symmetric spherical harmonic series (e.g. 6, 15, 28 etc). The -no_reorientation option can be used to force reorientation off if required.

Non-linear registration computes warps to map from both image1->image2 and image2->image1. Similar to Avants (2008) Med Image Anal. 12(1): 26â€“41, registration is performed by matching both the image1 and image2 in a 'midway space'. Warps can be saved as two deformation fields that map directly between image1->image2 and image2->image1, or if using -nl_warp_full as a single 5D file that stores all 4 warps image1->mid->image2, and image2->mid->image1. The 5D warp format stores x,y,z deformations in the 4th dimension, and uses the 5th dimension to index the 4 warps. The affine transforms estimated (to midway space) are also stored as comments in the image header. The 5D warp file can be used to reinitialise subsequent registrations, in addition to transforming images to midway space (e.g. for intra-subject alignment in a 2-time-point longitudinal analysis).

References:

* If FOD registration is being performed:
Raffelt, D.; Tournier, J.-D.; Fripp, J; Crozier, S.; Connelly, A. & Salvado, O. Symmetric diffeomorphic registration of fibre orientation distributions. NeuroImage, 2011, 56(3), 1171-1180

Raffelt, D.; Tournier, J.-D.; Crozier, S.; Connelly, A. & Salvado, O. Reorientation of fiber orientation distributions using apodized point spread functions. Magnetic Resonance in Medicine, 2012, 67, 844-855

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    mrregister_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(MRREGISTER_METADATA)
    execution.params(params)
    args = mrregister_cargs(params, execution)
    out = mrregister_outputs(params, execution)
    execution.run(args)
    return out

def mrregister(
    image1_image2: InputPathType,
    type_: str | None = None,
    transformed: list[str] | None = None,
    transformed_midway: list[MrregisterTransformedMidway] | None = None,
    mask1: InputPathType | None = None,
    mask2: InputPathType | None = None,
    nan: bool = False,
    rigid: str | None = None,
    rigid_1tomidway: str | None = None,
    rigid_2tomidway: str | None = None,
    rigid_init_translation: str | None = None,
    rigid_init_rotation: str | None = None,
    rigid_init_matrix: InputPathType | None = None,
    rigid_scale: list[float] | None = None,
    rigid_niter: list[int] | None = None,
    rigid_metric: str | None = None,
    rigid_metric_diff_estimator: str | None = None,
    rigid_lmax: list[int] | None = None,
    rigid_log: str | None = None,
    affine: str | None = None,
    affine_1tomidway: str | None = None,
    affine_2tomidway: str | None = None,
    affine_init_translation: str | None = None,
    affine_init_rotation: str | None = None,
    affine_init_matrix: InputPathType | None = None,
    affine_scale: list[float] | None = None,
    affine_niter: list[int] | None = None,
    affine_metric: str | None = None,
    affine_metric_diff_estimator: str | None = None,
    affine_lmax: list[int] | None = None,
    affine_log: str | None = None,
    init_translation_unmasked1: bool = False,
    init_translation_unmasked2: bool = False,
    init_rotation_unmasked1: bool = False,
    init_rotation_unmasked2: bool = False,
    init_rotation_search_angles: list[float] | None = None,
    init_rotation_search_scale: float | None = None,
    init_rotation_search_directions: int | None = None,
    init_rotation_search_run_global: bool = False,
    init_rotation_search_global_iterations: int | None = None,
    linstage_iterations: list[int] | None = None,
    linstage_optimiser_first: str | None = None,
    linstage_optimiser_last: str | None = None,
    linstage_optimiser_default: str | None = None,
    linstage_diagnostics_prefix: str | None = None,
    nl_warp: MrregisterNlWarp | None = None,
    nl_warp_full: str | None = None,
    nl_init: InputPathType | None = None,
    nl_scale: list[float] | None = None,
    nl_niter: list[int] | None = None,
    nl_update_smooth: float | None = None,
    nl_disp_smooth: float | None = None,
    nl_grad_step: float | None = None,
    nl_lmax: list[int] | None = None,
    diagnostics_image: str | None = None,
    directions: InputPathType | None = None,
    noreorientation: bool = False,
    mc_weights: list[float] | None = None,
    datatype: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[MrregisterConfig] | None = None,
    help: bool = False,
    version: bool = False,
    contrast1_contrast2: list[InputPathType] | None = None,
    runner: Runner | None = None,
) -> MrregisterOutputs:
    """
    mrregister

    Register two images together using a symmetric rigid, affine or non-linear transformation model.

By default this application will perform an affine, followed by non-linear registration.

FOD registration (with apodised point spread reorientation) will be performed by default if the number of volumes in the 4th dimension equals the number of coefficients in an antipodally symmetric spherical harmonic series (e.g. 6, 15, 28 etc). The -no_reorientation option can be used to force reorientation off if required.

Non-linear registration computes warps to map from both image1->image2 and image2->image1. Similar to Avants (2008) Med Image Anal. 12(1): 26â€“41, registration is performed by matching both the image1 and image2 in a 'midway space'. Warps can be saved as two deformation fields that map directly between image1->image2 and image2->image1, or if using -nl_warp_full as a single 5D file that stores all 4 warps image1->mid->image2, and image2->mid->image1. The 5D warp format stores x,y,z deformations in the 4th dimension, and uses the 5th dimension to index the 4 warps. The affine transforms estimated (to midway space) are also stored as comments in the image header. The 5D warp file can be used to reinitialise subsequent registrations, in addition to transforming images to midway space (e.g. for intra-subject alignment in a 2-time-point longitudinal analysis).

References:

* If FOD registration is being performed:
Raffelt, D.; Tournier, J.-D.; Fripp, J; Crozier, S.; Connelly, A. & Salvado, O. Symmetric diffeomorphic registration of fibre orientation distributions. NeuroImage, 2011, 56(3), 1171-1180

Raffelt, D.; Tournier, J.-D.; Crozier, S.; Connelly, A. & Salvado, O. Reorientation of fiber orientation distributions using apodized point spread functions. Magnetic Resonance in Medicine, 2012, 67, 844-855

    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        image1_image2: input image 1 ('moving') and input image 2 ('template')
        type_: the registration type. Valid choices are: rigid, affine, nonlinear,\
            rigid_affine, rigid_nonlinear, affine_nonlinear, rigid_affine_nonlinear\
            (Default: affine_nonlinear)
        transformed: image1 after registration transformed and regridded to the\
            space of image2. Note that -transformed needs to be repeated for each\
            contrast if multi-contrast registration is used.
        transformed_midway: image1 and image2 after registration transformed and\
            regridded to the midway space. Note that -transformed_midway needs to\
            be repeated for each contrast if multi-contrast registration is used.
        mask1: a mask to define the region of image1 to use for optimisation.
        mask2: a mask to define the region of image2 to use for optimisation.
        nan: use NaN as out of bounds value. (Default: 0.0)
        rigid: the output text file containing the rigid transformation as a 4x4\
            matrix
        rigid_1tomidway: the output text file containing the rigid transformation\
            that aligns image1 to image2 in their common midway space as a 4x4\
            matrix
        rigid_2tomidway: the output text file containing the rigid transformation\
            that aligns image2 to image1 in their common midway space as a 4x4\
            matrix
        rigid_init_translation: initialise the translation and centre of rotation\
            Valid choices are: mass (aligns the centers of mass of both images,\
            default), geometric (aligns geometric image centres) and none.
        rigid_init_rotation: initialise the rotation Valid choices are: search\
            (search for the best rotation using mean squared residuals), moments\
            (rotation based on directions of intensity variance with respect to\
            centre of mass), none (default).
        rigid_init_matrix: initialise either the rigid, affine, or syn registration\
            with the supplied rigid transformation (as a 4x4 matrix in scanner\
            coordinates). Note that this overrides rigid_init_translation and\
            rigid_init_rotation initialisation
        rigid_scale: use a multi-resolution scheme by defining a scale factor for\
            each level using comma separated values (Default: 0.25,0.5,1.0)
        rigid_niter: the maximum number of gradient descent iterations per stage.\
            This can be specified either as a single number for all\
            multi-resolution levels, or a single value for each level. (Default:\
            1000)
        rigid_metric: valid choices are: diff (intensity differences), Default:\
            diff
        rigid_metric_diff_estimator: Valid choices are: l1 (least absolute: |x|),\
            l2 (ordinary least squares), lp (least powers: |x|^1.2), Default: l2
        rigid_lmax: explicitly set the lmax to be used per scale factor in rigid\
            FOD registration. By default FOD registration will use lmax 0,2,4 with\
            default scale factors 0.25,0.5,1.0 respectively. Note that no\
            reorientation will be performed with lmax = 0.
        rigid_log: write gradient descent parameter evolution to log file
        affine: the output text file containing the affine transformation as a 4x4\
            matrix
        affine_1tomidway: the output text file containing the affine transformation\
            that aligns image1 to image2 in their common midway space as a 4x4\
            matrix
        affine_2tomidway: the output text file containing the affine transformation\
            that aligns image2 to image1 in their common midway space as a 4x4\
            matrix
        affine_init_translation: initialise the translation and centre of rotation\
            Valid choices are: mass (aligns the centers of mass of both images),\
            geometric (aligns geometric image centres) and none. (Default: mass)
        affine_init_rotation: initialise the rotation Valid choices are: search\
            (search for the best rotation using mean squared residuals), moments\
            (rotation based on directions of intensity variance with respect to\
            centre of mass), none (Default: none).
        affine_init_matrix: initialise either the affine, or syn registration with\
            the supplied affine transformation (as a 4x4 matrix in scanner\
            coordinates). Note that this overrides affine_init_translation and\
            affine_init_rotation initialisation
        affine_scale: use a multi-resolution scheme by defining a scale factor for\
            each level using comma separated values (Default: 0.25,0.5,1.0)
        affine_niter: the maximum number of gradient descent iterations per stage.\
            This can be specified either as a single number for all\
            multi-resolution levels, or a single value for each level. (Default:\
            1000)
        affine_metric: valid choices are: diff (intensity differences), Default:\
            diff
        affine_metric_diff_estimator: Valid choices are: l1 (least absolute: |x|),\
            l2 (ordinary least squares), lp (least powers: |x|^1.2), Default: l2
        affine_lmax: explicitly set the lmax to be used per scale factor in affine\
            FOD registration. By default FOD registration will use lmax 0,2,4 with\
            default scale factors 0.25,0.5,1.0 respectively. Note that no\
            reorientation will be performed with lmax = 0.
        affine_log: write gradient descent parameter evolution to log file
        init_translation_unmasked1: disregard mask1 for the translation\
            initialisation (affects 'mass')
        init_translation_unmasked2: disregard mask2 for the translation\
            initialisation (affects 'mass')
        init_rotation_unmasked1: disregard mask1 for the rotation initialisation\
            (affects 'search' and 'moments')
        init_rotation_unmasked2: disregard mask2 for the rotation initialisation\
            (affects 'search' and 'moments')
        init_rotation_search_angles: rotation angles for the local search in\
            degrees between 0 and 180. (Default: 2,5,10,15,20)
        init_rotation_search_scale: relative size of the images used for the\
            rotation search. (Default: 0.15)
        init_rotation_search_directions: number of rotation axis for local search.\
            (Default: 250)
        init_rotation_search_run_global: perform a global search. (Default: local)
        init_rotation_search_global_iterations: number of rotations to investigate\
            (Default: 10000)
        linstage_iterations: number of iterations for each registration stage, not\
            to be confused with -rigid_niter or -affine_niter. This can be used to\
            generate intermediate diagnostics images (-linstage.diagnostics.prefix)\
            or to change the cost function optimiser during registration (without\
            the need to repeatedly resize the images). (Default: 1 == no\
            repetition)
        linstage_optimiser_first: Cost function optimisation algorithm to use at\
            first iteration of all stages. Valid choices: bbgd (Barzilai-Borwein\
            gradient descent) or gd (simple gradient descent). (Default: bbgd)
        linstage_optimiser_last: Cost function optimisation algorithm to use at\
            last iteration of all stages (if there are more than one). Valid\
            choices: bbgd (Barzilai-Borwein gradient descent) or gd (simple\
            gradient descent). (Default: bbgd)
        linstage_optimiser_default: Cost function optimisation algorithm to use at\
            any stage iteration other than first or last iteration. Valid choices:\
            bbgd (Barzilai-Borwein gradient descent) or gd (simple gradient\
            descent). (Default: bbgd)
        linstage_diagnostics_prefix: generate diagnostics images after every\
            registration stage
        nl_warp: the non-linear warp output defined as two deformation fields,\
            where warp1 can be used to transform image1->image2 and warp2 to\
            transform image2->image1. The deformation fields also encapsulate any\
            linear transformation estimated prior to non-linear registration.
        nl_warp_full: output all warps used during registration. This saves four\
            different warps that map each image to a midway space and their\
            inverses in a single 5D image file. The 4th image dimension indexes the\
            x,y,z component of the deformation vector and the 5th dimension indexes\
            the field in this order: image1->midway, midway->image1,\
            image2->midway, midway->image2. Where image1->midway defines the field\
            that maps image1 onto the midway space using the reverse convention\
            When linear registration is performed first, the estimated linear\
            transform will be included in the comments of the image header, and\
            therefore the entire linear and non-linear transform can be applied (in\
            either direction) using this output warp file with mrtransform
        nl_init: initialise the non-linear registration with the supplied warp\
            image. The supplied warp must be in the same format as output using the\
            -nl_warp_full option (i.e. have 4 deformation fields with the linear\
            transforms in the image header)
        nl_scale: use a multi-resolution scheme by defining a scale factor for each\
            level using comma separated values (Default: 0.25,0.5,1.0)
        nl_niter: the maximum number of iterations. This can be specified either as\
            a single number for all multi-resolution levels, or a single value for\
            each level. (Default: 50)
        nl_update_smooth: regularise the gradient update field with Gaussian\
            smoothing (standard deviation in voxel units, Default 2.0)
        nl_disp_smooth: regularise the displacement field with Gaussian smoothing\
            (standard deviation in voxel units, Default 1.0)
        nl_grad_step: the gradient step size for non-linear registration (Default:\
            0.5)
        nl_lmax: explicitly set the lmax to be used per scale factor in non-linear\
            FOD registration. By default FOD registration will use lmax 0,2,4 with\
            default scale factors 0.25,0.5,1.0 respectively. Note that no\
            reorientation will be performed with lmax = 0.
        diagnostics_image: write intermediate images for diagnostics purposes
        directions: the directions used for FOD reorientation using apodised point\
            spread functions (Default: 60 directions)
        noreorientation: turn off FOD reorientation. Reorientation is on by default\
            if the number of volumes in the 4th dimension corresponds to the number\
            of coefficients in an antipodally symmetric spherical harmonic series\
            (i.e. 6, 15, 28, 45, 66 etc)
        mc_weights: relative weight of images used for multi-contrast registration.\
            Default: 1.0 (equal weighting)
        datatype: specify output image data type. Valid choices are: float32,\
            float32le, float32be, float64, float64le, float64be, int64, uint64,\
            int64le, uint64le, int64be, uint64be, int32, uint32, int32le, uint32le,\
            int32be, uint32be, int16, uint16, int16le, uint16le, int16be, uint16be,\
            cfloat32, cfloat32le, cfloat32be, cfloat64, cfloat64le, cfloat64be,\
            int8, uint8, bit.
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        contrast1_contrast2: optional list of additional input images used as\
            additional contrasts. Can be used multiple times. contrastX and imageX\
            must share the same coordinate system.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = mrregister_params(
        image1_image2=image1_image2,
        type_=type_,
        transformed=transformed,
        transformed_midway=transformed_midway,
        mask1=mask1,
        mask2=mask2,
        nan=nan,
        rigid=rigid,
        rigid_1tomidway=rigid_1tomidway,
        rigid_2tomidway=rigid_2tomidway,
        rigid_init_translation=rigid_init_translation,
        rigid_init_rotation=rigid_init_rotation,
        rigid_init_matrix=rigid_init_matrix,
        rigid_scale=rigid_scale,
        rigid_niter=rigid_niter,
        rigid_metric=rigid_metric,
        rigid_metric_diff_estimator=rigid_metric_diff_estimator,
        rigid_lmax=rigid_lmax,
        rigid_log=rigid_log,
        affine=affine,
        affine_1tomidway=affine_1tomidway,
        affine_2tomidway=affine_2tomidway,
        affine_init_translation=affine_init_translation,
        affine_init_rotation=affine_init_rotation,
        affine_init_matrix=affine_init_matrix,
        affine_scale=affine_scale,
        affine_niter=affine_niter,
        affine_metric=affine_metric,
        affine_metric_diff_estimator=affine_metric_diff_estimator,
        affine_lmax=affine_lmax,
        affine_log=affine_log,
        init_translation_unmasked1=init_translation_unmasked1,
        init_translation_unmasked2=init_translation_unmasked2,
        init_rotation_unmasked1=init_rotation_unmasked1,
        init_rotation_unmasked2=init_rotation_unmasked2,
        init_rotation_search_angles=init_rotation_search_angles,
        init_rotation_search_scale=init_rotation_search_scale,
        init_rotation_search_directions=init_rotation_search_directions,
        init_rotation_search_run_global=init_rotation_search_run_global,
        init_rotation_search_global_iterations=init_rotation_search_global_iterations,
        linstage_iterations=linstage_iterations,
        linstage_optimiser_first=linstage_optimiser_first,
        linstage_optimiser_last=linstage_optimiser_last,
        linstage_optimiser_default=linstage_optimiser_default,
        linstage_diagnostics_prefix=linstage_diagnostics_prefix,
        nl_warp=nl_warp,
        nl_warp_full=nl_warp_full,
        nl_init=nl_init,
        nl_scale=nl_scale,
        nl_niter=nl_niter,
        nl_update_smooth=nl_update_smooth,
        nl_disp_smooth=nl_disp_smooth,
        nl_grad_step=nl_grad_step,
        nl_lmax=nl_lmax,
        diagnostics_image=diagnostics_image,
        directions=directions,
        noreorientation=noreorientation,
        mc_weights=mc_weights,
        datatype=datatype,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
        contrast1_contrast2=contrast1_contrast2,
    )
    return mrregister_execute(params, runner)

__all__ = [
    "Mrregister",
    "MrregisterOutputs",
    "MRREGISTER_METADATA",
    "mrregister_cargs",
    "mrregister_outputs",
    "mrregister_params",
    "mrregister_execute",
    "mrregister_validate",
    "mrregister",
]