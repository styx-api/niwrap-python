# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

PEAKS2FIXEL_METADATA = Metadata(
    id="peaks2fixel",
    name="peaks2fixel",
    package="mrtrix",
)

class Peaks2fixelConfig(typing.TypedDict):
    key: str
    """temporarily set the value of an MRtrix config file entry."""
    value: str
    """temporarily set the value of an MRtrix config file entry."""

Peaks2fixel = typing.TypedDict(
    "Peaks2fixel",
    {
        "@type": typing.NotRequired[typing.Literal["mrtrix/peaks2fixel"]],
        "dataname": typing.NotRequired[str],
        "info": typing.NotRequired[bool],
        "quiet": typing.NotRequired[bool],
        "debug": typing.NotRequired[bool],
        "force": typing.NotRequired[bool],
        "nthreads": typing.NotRequired[int],
        "config": typing.NotRequired[list[Peaks2fixelConfig]],
        "help": typing.NotRequired[bool],
        "version": typing.NotRequired[bool],
        "directions": InputPathType,
        "fixels": str,
    },
)

@dataclasses.dataclass
class Peaks2fixelOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType
    fixels: OutputPathType
    """the output fixel directory."""

def peaks2fixel_params(
    directions: InputPathType,
    fixels: str,
    dataname: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Peaks2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
) -> Peaks2fixel:
    """
    Build parameters.

    Args:
        directions: the input directions image; each volume corresponds to the x, y\
            & z component of each direction vector in turn.
        fixels: the output fixel directory.
        dataname: the name of the output fixel data file encoding peak amplitudes
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.

    Returns:
        Parameter dictionary.
    """
    params: Peaks2fixel = {
        "@type": "mrtrix/peaks2fixel",
        "directions": directions,
        "fixels": fixels,
        "info": info,
        "quiet": quiet,
        "debug": debug,
        "force": force,
        "help": help,
        "version": version,
    }
    if dataname is not None:
        params["dataname"] = dataname
    if nthreads is not None:
        params["nthreads"] = nthreads
    if config is not None:
        params["config"] = config
    return params

def peaks2fixel_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Peaks2fixel."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("dataname", None) is not None:
        if not isinstance(params["dataname"], str):
            raise StyxValidationError(f'`dataname` has the wrong type: Received `{type(params["dataname"])}` expected `str`')
    if params.get("info", None) is not None:
        if not isinstance(params["info"], bool):
            raise StyxValidationError(f'`info` has the wrong type: Received `{type(params["info"])}` expected `bool`')
    if params.get("quiet", None) is not None:
        if not isinstance(params["quiet"], bool):
            raise StyxValidationError(f'`quiet` has the wrong type: Received `{type(params["quiet"])}` expected `bool`')
    if params.get("debug", None) is not None:
        if not isinstance(params["debug"], bool):
            raise StyxValidationError(f'`debug` has the wrong type: Received `{type(params["debug"])}` expected `bool`')
    if params.get("force", None) is not None:
        if not isinstance(params["force"], bool):
            raise StyxValidationError(f'`force` has the wrong type: Received `{type(params["force"])}` expected `bool`')
    if params.get("nthreads", None) is not None:
        if not isinstance(params["nthreads"], int):
            raise StyxValidationError(f'`nthreads` has the wrong type: Received `{type(params["nthreads"])}` expected `int`')
    if params.get("config", None) is not None:
        if not isinstance(params["config"], list):
            raise StyxValidationError(f'`config` has the wrong type: Received `{type(params["config"])}` expected `list[Peaks2fixelConfig]`')
        for e in params["config"]:
            if not isinstance(e, dict):
                raise StyxValidationError(f'Params object has the wrong type \'{type(e)}\'')
            if e.get("key", None) is None:
                raise StyxValidationError("`key` must not be None")
            if not isinstance(e["key"], str):
                raise StyxValidationError(f'`key` has the wrong type: Received `{type(e["key"])}` expected `str`')
            if e.get("value", None) is None:
                raise StyxValidationError("`value` must not be None")
            if not isinstance(e["value"], str):
                raise StyxValidationError(f'`value` has the wrong type: Received `{type(e["value"])}` expected `str`')
    if params.get("help", None) is not None:
        if not isinstance(params["help"], bool):
            raise StyxValidationError(f'`help` has the wrong type: Received `{type(params["help"])}` expected `bool`')
    if params.get("version", None) is not None:
        if not isinstance(params["version"], bool):
            raise StyxValidationError(f'`version` has the wrong type: Received `{type(params["version"])}` expected `bool`')
    if params.get("directions", None) is None:
        raise StyxValidationError("`directions` must not be None")
    if not isinstance(params["directions"], (pathlib.Path, str)):
        raise StyxValidationError(f'`directions` has the wrong type: Received `{type(params["directions"])}` expected `InputPathType`')
    if params.get("fixels", None) is None:
        raise StyxValidationError("`fixels` must not be None")
    if not isinstance(params["fixels"], str):
        raise StyxValidationError(f'`fixels` has the wrong type: Received `{type(params["fixels"])}` expected `str`')

def peaks2fixel_cargs(params: Peaks2fixel, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("peaks2fixel")
    v_0 = params.get("dataname")
    if v_0 is not None:
        cargs.append("-dataname")
        cargs.append(v_0)
    if params.get("info"):
        cargs.append("-info")
    if params.get("quiet"):
        cargs.append("-quiet")
    if params.get("debug"):
        cargs.append("-debug")
    if params.get("force"):
        cargs.append("-force")
    v_1 = params.get("nthreads")
    if v_1 is not None:
        cargs.append("-nthreads")
        cargs.append(str(v_1))
    v_2 = params.get("config")
    if v_2 is not None:
        for item0 in v_2:
            cargs.append("-config")
            cargs.append(item0["key"])
            cargs.append(item0["value"])
    if params.get("help"):
        cargs.append("-help")
    if params.get("version"):
        cargs.append("-version")
    cargs.append(execution.input_file(params["directions"]))
    cargs.append(params["fixels"])
    return cargs

def peaks2fixel_outputs(params: Peaks2fixel, execution: Execution) -> Peaks2fixelOutputs:
    """Build the Peaks2fixelOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    fixels_v: OutputPathType = execution.output_file(params["fixels"])
    return Peaks2fixelOutputs(
        root=root_v,
        fixels=fixels_v,
    )

def peaks2fixel_execute(params: Peaks2fixel, runner: Runner | None = None) -> Peaks2fixelOutputs:
    """
    peaks2fixel

    Convert peak directions image to a fixel directory.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    peaks2fixel_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(PEAKS2FIXEL_METADATA)
    execution.params(params)
    args = peaks2fixel_cargs(params, execution)
    out = peaks2fixel_outputs(params, execution)
    execution.run(args)
    return out

def peaks2fixel(
    directions: InputPathType,
    fixels: str,
    dataname: str | None = None,
    info: bool = False,
    quiet: bool = False,
    debug: bool = False,
    force: bool = False,
    nthreads: int | None = None,
    config: list[Peaks2fixelConfig] | None = None,
    help: bool = False,
    version: bool = False,
    runner: Runner | None = None,
) -> Peaks2fixelOutputs:
    """
    peaks2fixel

    Convert peak directions image to a fixel directory.



References:



    Author: MRTrix3 Developers

    URL: https://www.mrtrix.org/

    Args:
        directions: the input directions image; each volume corresponds to the x, y\
            & z component of each direction vector in turn.
        fixels: the output fixel directory.
        dataname: the name of the output fixel data file encoding peak amplitudes
        info: display information messages.
        quiet: do not display information messages or progress status;\
            alternatively, this can be achieved by setting the MRTRIX_QUIET\
            environment variable to a non-empty string.
        debug: display debugging messages.
        force: force overwrite of output files (caution: using the same file as\
            input and output might cause unexpected behaviour).
        nthreads: use this number of threads in multi-threaded applications (set to\
            0 to disable multi-threading).
        config: temporarily set the value of an MRtrix config file entry.
        help: display this information page and exit.
        version: display version information and exit.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = peaks2fixel_params(
        directions=directions,
        fixels=fixels,
        dataname=dataname,
        info=info,
        quiet=quiet,
        debug=debug,
        force=force,
        nthreads=nthreads,
        config=config,
        help=help,
        version=version,
    )
    return peaks2fixel_execute(params, runner)

__all__ = [
    "Peaks2fixel",
    "Peaks2fixelOutputs",
    "PEAKS2FIXEL_METADATA",
    "peaks2fixel_cargs",
    "peaks2fixel_outputs",
    "peaks2fixel_params",
    "peaks2fixel_execute",
    "peaks2fixel_validate",
    "peaks2fixel",
]