# This file was auto generated by Styx.
# Do not edit this file directly.

import typing
import pathlib
from styxdefs import *

ANALYZETO4DFP_METADATA = Metadata(
    id="3f5d3d809a30ecb597d74082f936e1be4e243840.boutiques",
    name="analyzeto4dfp",
    package="freesurfer",
    container_image_tag="freesurfer/freesurfer:7.4.1",
)


_Analyzeto4dfpParamsDictNoTag = typing.TypedDict('_Analyzeto4dfpParamsDictNoTag', {
    "analyze_image": InputPathType,
    "rois_scale": bool,
    "flip_x": bool,
    "flip_y": bool,
    "flip_z": bool,
    "endian": typing.NotRequired[str | None],
    "orientation": typing.NotRequired[int | None],
})
Analyzeto4dfpParamsDictTagged = typing.TypedDict('Analyzeto4dfpParamsDictTagged', {
    "@type": typing.Literal["freesurfer/analyzeto4dfp"],
    "analyze_image": InputPathType,
    "rois_scale": bool,
    "flip_x": bool,
    "flip_y": bool,
    "flip_z": bool,
    "endian": typing.NotRequired[str | None],
    "orientation": typing.NotRequired[int | None],
})
Analyzeto4dfpParamsDict = _Analyzeto4dfpParamsDictNoTag | Analyzeto4dfpParamsDictTagged


class Analyzeto4dfpOutputs(typing.NamedTuple):
    """
    Output object returned when calling `Analyzeto4dfpParamsDict(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def analyzeto4dfp_params(
    analyze_image: InputPathType,
    rois_scale: bool = False,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
    endian: str | None = None,
    orientation: int | None = None,
) -> Analyzeto4dfpParamsDictTagged:
    """
    Build parameters.
    
    Args:
        analyze_image: Input ANALYZE image file.
        rois_scale: Apply ROIScaleFactor.
        flip_x: Flip first axis.
        flip_y: Flip second axis.
        flip_z: Flip third axis.
        endian: Output big or little endian (default CPU endian).
        orientation: Supply orientation code (in range [0-5]).
    Returns:
        Parameter dictionary
    """
    params = {
        "@type": "freesurfer/analyzeto4dfp",
        "analyze_image": analyze_image,
        "rois_scale": rois_scale,
        "flip_x": flip_x,
        "flip_y": flip_y,
        "flip_z": flip_z,
    }
    if endian is not None:
        params["endian"] = endian
    if orientation is not None:
        params["orientation"] = orientation
    return params


def analyzeto4dfp_validate(
    params: typing.Any,
) -> None:
    """
    Validate parameters. Throws an error if `params` is not a valid
    `Analyzeto4dfpParamsDict` object.
    
    Args:
        params: The parameters object to validate.
    """
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("analyze_image", None) is None:
        raise StyxValidationError("`analyze_image` must not be None")
    if not isinstance(params["analyze_image"], (pathlib.Path, str)):
        raise StyxValidationError(f'`analyze_image` has the wrong type: Received `{type(params.get("analyze_image", None))}` expected `InputPathType`')
    if params.get("rois_scale", False) is None:
        raise StyxValidationError("`rois_scale` must not be None")
    if not isinstance(params["rois_scale"], bool):
        raise StyxValidationError(f'`rois_scale` has the wrong type: Received `{type(params.get("rois_scale", False))}` expected `bool`')
    if params.get("flip_x", False) is None:
        raise StyxValidationError("`flip_x` must not be None")
    if not isinstance(params["flip_x"], bool):
        raise StyxValidationError(f'`flip_x` has the wrong type: Received `{type(params.get("flip_x", False))}` expected `bool`')
    if params.get("flip_y", False) is None:
        raise StyxValidationError("`flip_y` must not be None")
    if not isinstance(params["flip_y"], bool):
        raise StyxValidationError(f'`flip_y` has the wrong type: Received `{type(params.get("flip_y", False))}` expected `bool`')
    if params.get("flip_z", False) is None:
        raise StyxValidationError("`flip_z` must not be None")
    if not isinstance(params["flip_z"], bool):
        raise StyxValidationError(f'`flip_z` has the wrong type: Received `{type(params.get("flip_z", False))}` expected `bool`')
    if params.get("endian", None) is not None:
        if not isinstance(params["endian"], str):
            raise StyxValidationError(f'`endian` has the wrong type: Received `{type(params.get("endian", None))}` expected `str | None`')
    if params.get("orientation", None) is not None:
        if not isinstance(params["orientation"], int):
            raise StyxValidationError(f'`orientation` has the wrong type: Received `{type(params.get("orientation", None))}` expected `int | None`')
        if not (0 <= params["orientation"] <= 5):
            raise StyxValidationError("Parameter `orientation` must be between 0 and 5 (inclusive)")


def analyzeto4dfp_cargs(
    params: Analyzeto4dfpParamsDict,
    execution: Execution,
) -> list[str]:
    """
    Build command-line arguments from parameters.
    
    Args:
        params: The parameters.
        execution: The execution object for resolving input paths.
    Returns:
        Command-line arguments.
    """
    cargs = []
    cargs.append("analyzeto4dfp")
    cargs.append(execution.input_file(params.get("analyze_image", None)))
    if params.get("rois_scale", False):
        cargs.append("-s")
    if params.get("flip_x", False):
        cargs.append("-x")
    if params.get("flip_y", False):
        cargs.append("-y")
    if params.get("flip_z", False):
        cargs.append("-z")
    if params.get("endian", None) is not None:
        cargs.extend([
            "-@",
            params.get("endian", None)
        ])
    if params.get("orientation", None) is not None:
        cargs.extend([
            "-O",
            str(params.get("orientation", None))
        ])
    return cargs


def analyzeto4dfp_outputs(
    params: Analyzeto4dfpParamsDict,
    execution: Execution,
) -> Analyzeto4dfpOutputs:
    """
    Build outputs object containing output file paths and possibly stdout/stderr.
    
    Args:
        params: The parameters.
        execution: The execution object for resolving input paths.
    Returns:
        Outputs object.
    """
    ret = Analyzeto4dfpOutputs(
        root=execution.output_file("."),
    )
    return ret


def analyzeto4dfp_execute(
    params: Analyzeto4dfpParamsDict,
    runner: Runner | None = None,
) -> Analyzeto4dfpOutputs:
    """
    analyzeto4dfp
    
    Convert ANALYZE image format to 4dfp format with various options.
    
    Author: FreeSurfer Developers
    
    URL: https://github.com/freesurfer/freesurfer
    
    Args:
        params: The parameters.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `Analyzeto4dfpOutputs`).
    """
    analyzeto4dfp_validate(params)
    runner = runner or get_global_runner()
    execution = runner.start_execution(ANALYZETO4DFP_METADATA)
    params = execution.params(params)
    cargs = analyzeto4dfp_cargs(params, execution)
    ret = analyzeto4dfp_outputs(params, execution)
    execution.run(cargs)
    return ret


def analyzeto4dfp(
    analyze_image: InputPathType,
    rois_scale: bool = False,
    flip_x: bool = False,
    flip_y: bool = False,
    flip_z: bool = False,
    endian: str | None = None,
    orientation: int | None = None,
    runner: Runner | None = None,
) -> Analyzeto4dfpOutputs:
    """
    analyzeto4dfp
    
    Convert ANALYZE image format to 4dfp format with various options.
    
    Author: FreeSurfer Developers
    
    URL: https://github.com/freesurfer/freesurfer
    
    Args:
        analyze_image: Input ANALYZE image file.
        rois_scale: Apply ROIScaleFactor.
        flip_x: Flip first axis.
        flip_y: Flip second axis.
        flip_z: Flip third axis.
        endian: Output big or little endian (default CPU endian).
        orientation: Supply orientation code (in range [0-5]).
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `Analyzeto4dfpOutputs`).
    """
    params = analyzeto4dfp_params(
        analyze_image=analyze_image,
        rois_scale=rois_scale,
        flip_x=flip_x,
        flip_y=flip_y,
        flip_z=flip_z,
        endian=endian,
        orientation=orientation,
    )
    return analyzeto4dfp_execute(params, runner)


__all__ = [
    "ANALYZETO4DFP_METADATA",
    "Analyzeto4dfpOutputs",
    "Analyzeto4dfpParamsDict",
    "Analyzeto4dfpParamsDictTagged",
    "analyzeto4dfp",
    "analyzeto4dfp_execute",
    "analyzeto4dfp_params",
]
