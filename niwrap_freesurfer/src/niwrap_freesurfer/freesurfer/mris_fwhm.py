# This file was auto generated by Styx.
# Do not edit this file directly.

import typing
import pathlib
from styxdefs import *

MRIS_FWHM_METADATA = Metadata(
    id="a1a32f8d7f7e5d30241888e0d9b40c74ddb0107e.boutiques",
    name="mris_fwhm",
    package="freesurfer",
    container_image_tag="freesurfer/freesurfer:7.4.1",
)


_MrisFwhmParamsDictNoTag = typing.TypedDict('_MrisFwhmParamsDictNoTag', {
    "input_file": InputPathType,
    "subject": str,
    "hemi": str,
    "surf": typing.NotRequired[str | None],
    "label_file": typing.NotRequired[InputPathType | None],
    "cortex_flag": bool,
    "mask_file": typing.NotRequired[InputPathType | None],
    "x_matrix": typing.NotRequired[InputPathType | None],
    "detrend_order": typing.NotRequired[float | None],
    "smooth_only_flag": bool,
    "no_detrend_flag": bool,
    "sqr_flag": bool,
    "sum_file": typing.NotRequired[str | None],
    "dat_file": typing.NotRequired[str | None],
    "ar1dat_file": typing.NotRequired[str | None],
    "ar1vol": typing.NotRequired[str | None],
    "fwhmmap": typing.NotRequired[str | None],
    "prune_flag": bool,
    "no_prune_flag": bool,
    "out_mask": typing.NotRequired[str | None],
    "varnorm_flag": bool,
    "fwhm": typing.NotRequired[float | None],
    "niters_only": typing.NotRequired[str | None],
    "output_file": str,
    "sd": typing.NotRequired[str | None],
    "synth_flag": bool,
    "synth_frames": typing.NotRequired[float | None],
    "threads": typing.NotRequired[float | None],
    "debug_flag": bool,
    "checkopts_flag": bool,
    "version_flag": bool,
})
MrisFwhmParamsDictTagged = typing.TypedDict('MrisFwhmParamsDictTagged', {
    "@type": typing.Literal["freesurfer/mris_fwhm"],
    "input_file": InputPathType,
    "subject": str,
    "hemi": str,
    "surf": typing.NotRequired[str | None],
    "label_file": typing.NotRequired[InputPathType | None],
    "cortex_flag": bool,
    "mask_file": typing.NotRequired[InputPathType | None],
    "x_matrix": typing.NotRequired[InputPathType | None],
    "detrend_order": typing.NotRequired[float | None],
    "smooth_only_flag": bool,
    "no_detrend_flag": bool,
    "sqr_flag": bool,
    "sum_file": typing.NotRequired[str | None],
    "dat_file": typing.NotRequired[str | None],
    "ar1dat_file": typing.NotRequired[str | None],
    "ar1vol": typing.NotRequired[str | None],
    "fwhmmap": typing.NotRequired[str | None],
    "prune_flag": bool,
    "no_prune_flag": bool,
    "out_mask": typing.NotRequired[str | None],
    "varnorm_flag": bool,
    "fwhm": typing.NotRequired[float | None],
    "niters_only": typing.NotRequired[str | None],
    "output_file": str,
    "sd": typing.NotRequired[str | None],
    "synth_flag": bool,
    "synth_frames": typing.NotRequired[float | None],
    "threads": typing.NotRequired[float | None],
    "debug_flag": bool,
    "checkopts_flag": bool,
    "version_flag": bool,
})
MrisFwhmParamsDict = _MrisFwhmParamsDictNoTag | MrisFwhmParamsDictTagged


class MrisFwhmOutputs(typing.NamedTuple):
    """
    Output object returned when calling `MrisFwhmParamsDict(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""


def mris_fwhm_params(
    input_file: InputPathType,
    subject: str,
    hemi: str,
    output_file: str,
    surf: str | None = None,
    label_file: InputPathType | None = None,
    cortex_flag: bool = False,
    mask_file: InputPathType | None = None,
    x_matrix: InputPathType | None = None,
    detrend_order: float | None = None,
    smooth_only_flag: bool = False,
    no_detrend_flag: bool = False,
    sqr_flag: bool = False,
    sum_file: str | None = None,
    dat_file: str | None = None,
    ar1dat_file: str | None = None,
    ar1vol: str | None = None,
    fwhmmap: str | None = None,
    prune_flag: bool = False,
    no_prune_flag: bool = False,
    out_mask: str | None = None,
    varnorm_flag: bool = False,
    fwhm: float | None = None,
    niters_only: str | None = None,
    sd: str | None = None,
    synth_flag: bool = False,
    synth_frames: float | None = None,
    threads: float | None = None,
    debug_flag: bool = False,
    checkopts_flag: bool = False,
    version_flag: bool = False,
) -> MrisFwhmParamsDictTagged:
    """
    Build parameters.
    
    Args:
        input_file: Input data file.
        subject: Subject whose surface the input is defined on.
        hemi: Hemifield that the input is defined on. Legal values are lh and\
            rh.
        output_file: Output file to save the processed data.
        surf: Surface name to compute AR1 on. Default is white.
        label_file: Label file to be used as a mask.
        cortex_flag: Use hemi.cortex.label as a mask.
        mask_file: Mask file. Compute AR1 only over voxels in the given mask.
        x_matrix: Detrend data with the matrix in x.mat.
        detrend_order: Order of polynomial detrending.
        smooth_only_flag: Only smooth the data, implies --no-detrend.
        no_detrend_flag: Turn off polynomial detrending.
        sqr_flag: Compute square of input before smoothing.
        sum_file: Prints ascii summary to sumfile.
        dat_file: File for FWHM data.
        ar1dat_file: File containing ar1mean and ar1std.
        ar1vol: Save spatial AR1 as an overlay.
        fwhmmap: Save vertex-wise spatial FWHM as an overlay.
        prune_flag: Remove any voxel that is zero in any subject (after any\
            inversion).
        no_prune_flag: Do not prune (default).
        out_mask: File to save the final mask.
        varnorm_flag: Normalize the variance across space within any mask.
        fwhm: Smooth input by the specified FWHM in mm.
        niters_only: File that reports the number of iterations needed to\
            achieve the specified FWHM.
        sd: Subjects directory.
        synth_flag: Synthesize input with white gaussian noise.
        synth_frames: Number of frames for synthesized input.
        threads: Number of threads to use.
        debug_flag: Turn on debugging.
        checkopts_flag: Don't run anything, just check options and exit.
        version_flag: Print out version and exit.
    Returns:
        Parameter dictionary
    """
    params = {
        "@type": "freesurfer/mris_fwhm",
        "input_file": input_file,
        "subject": subject,
        "hemi": hemi,
        "cortex_flag": cortex_flag,
        "smooth_only_flag": smooth_only_flag,
        "no_detrend_flag": no_detrend_flag,
        "sqr_flag": sqr_flag,
        "prune_flag": prune_flag,
        "no_prune_flag": no_prune_flag,
        "varnorm_flag": varnorm_flag,
        "output_file": output_file,
        "synth_flag": synth_flag,
        "debug_flag": debug_flag,
        "checkopts_flag": checkopts_flag,
        "version_flag": version_flag,
    }
    if surf is not None:
        params["surf"] = surf
    if label_file is not None:
        params["label_file"] = label_file
    if mask_file is not None:
        params["mask_file"] = mask_file
    if x_matrix is not None:
        params["x_matrix"] = x_matrix
    if detrend_order is not None:
        params["detrend_order"] = detrend_order
    if sum_file is not None:
        params["sum_file"] = sum_file
    if dat_file is not None:
        params["dat_file"] = dat_file
    if ar1dat_file is not None:
        params["ar1dat_file"] = ar1dat_file
    if ar1vol is not None:
        params["ar1vol"] = ar1vol
    if fwhmmap is not None:
        params["fwhmmap"] = fwhmmap
    if out_mask is not None:
        params["out_mask"] = out_mask
    if fwhm is not None:
        params["fwhm"] = fwhm
    if niters_only is not None:
        params["niters_only"] = niters_only
    if sd is not None:
        params["sd"] = sd
    if synth_frames is not None:
        params["synth_frames"] = synth_frames
    if threads is not None:
        params["threads"] = threads
    return params


def mris_fwhm_validate(
    params: typing.Any,
) -> None:
    """
    Validate parameters. Throws an error if `params` is not a valid
    `MrisFwhmParamsDict` object.
    
    Args:
        params: The parameters object to validate.
    """
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_file", None) is None:
        raise StyxValidationError("`input_file` must not be None")
    if not isinstance(params["input_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_file` has the wrong type: Received `{type(params.get("input_file", None))}` expected `InputPathType`')
    if params.get("subject", None) is None:
        raise StyxValidationError("`subject` must not be None")
    if not isinstance(params["subject"], str):
        raise StyxValidationError(f'`subject` has the wrong type: Received `{type(params.get("subject", None))}` expected `str`')
    if params.get("hemi", None) is None:
        raise StyxValidationError("`hemi` must not be None")
    if not isinstance(params["hemi"], str):
        raise StyxValidationError(f'`hemi` has the wrong type: Received `{type(params.get("hemi", None))}` expected `str`')
    if params.get("surf", None) is not None:
        if not isinstance(params["surf"], str):
            raise StyxValidationError(f'`surf` has the wrong type: Received `{type(params.get("surf", None))}` expected `str | None`')
    if params.get("label_file", None) is not None:
        if not isinstance(params["label_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`label_file` has the wrong type: Received `{type(params.get("label_file", None))}` expected `InputPathType | None`')
    if params.get("cortex_flag", False) is None:
        raise StyxValidationError("`cortex_flag` must not be None")
    if not isinstance(params["cortex_flag"], bool):
        raise StyxValidationError(f'`cortex_flag` has the wrong type: Received `{type(params.get("cortex_flag", False))}` expected `bool`')
    if params.get("mask_file", None) is not None:
        if not isinstance(params["mask_file"], (pathlib.Path, str)):
            raise StyxValidationError(f'`mask_file` has the wrong type: Received `{type(params.get("mask_file", None))}` expected `InputPathType | None`')
    if params.get("x_matrix", None) is not None:
        if not isinstance(params["x_matrix"], (pathlib.Path, str)):
            raise StyxValidationError(f'`x_matrix` has the wrong type: Received `{type(params.get("x_matrix", None))}` expected `InputPathType | None`')
    if params.get("detrend_order", None) is not None:
        if not isinstance(params["detrend_order"], (float, int)):
            raise StyxValidationError(f'`detrend_order` has the wrong type: Received `{type(params.get("detrend_order", None))}` expected `float | None`')
    if params.get("smooth_only_flag", False) is None:
        raise StyxValidationError("`smooth_only_flag` must not be None")
    if not isinstance(params["smooth_only_flag"], bool):
        raise StyxValidationError(f'`smooth_only_flag` has the wrong type: Received `{type(params.get("smooth_only_flag", False))}` expected `bool`')
    if params.get("no_detrend_flag", False) is None:
        raise StyxValidationError("`no_detrend_flag` must not be None")
    if not isinstance(params["no_detrend_flag"], bool):
        raise StyxValidationError(f'`no_detrend_flag` has the wrong type: Received `{type(params.get("no_detrend_flag", False))}` expected `bool`')
    if params.get("sqr_flag", False) is None:
        raise StyxValidationError("`sqr_flag` must not be None")
    if not isinstance(params["sqr_flag"], bool):
        raise StyxValidationError(f'`sqr_flag` has the wrong type: Received `{type(params.get("sqr_flag", False))}` expected `bool`')
    if params.get("sum_file", None) is not None:
        if not isinstance(params["sum_file"], str):
            raise StyxValidationError(f'`sum_file` has the wrong type: Received `{type(params.get("sum_file", None))}` expected `str | None`')
    if params.get("dat_file", None) is not None:
        if not isinstance(params["dat_file"], str):
            raise StyxValidationError(f'`dat_file` has the wrong type: Received `{type(params.get("dat_file", None))}` expected `str | None`')
    if params.get("ar1dat_file", None) is not None:
        if not isinstance(params["ar1dat_file"], str):
            raise StyxValidationError(f'`ar1dat_file` has the wrong type: Received `{type(params.get("ar1dat_file", None))}` expected `str | None`')
    if params.get("ar1vol", None) is not None:
        if not isinstance(params["ar1vol"], str):
            raise StyxValidationError(f'`ar1vol` has the wrong type: Received `{type(params.get("ar1vol", None))}` expected `str | None`')
    if params.get("fwhmmap", None) is not None:
        if not isinstance(params["fwhmmap"], str):
            raise StyxValidationError(f'`fwhmmap` has the wrong type: Received `{type(params.get("fwhmmap", None))}` expected `str | None`')
    if params.get("prune_flag", False) is None:
        raise StyxValidationError("`prune_flag` must not be None")
    if not isinstance(params["prune_flag"], bool):
        raise StyxValidationError(f'`prune_flag` has the wrong type: Received `{type(params.get("prune_flag", False))}` expected `bool`')
    if params.get("no_prune_flag", False) is None:
        raise StyxValidationError("`no_prune_flag` must not be None")
    if not isinstance(params["no_prune_flag"], bool):
        raise StyxValidationError(f'`no_prune_flag` has the wrong type: Received `{type(params.get("no_prune_flag", False))}` expected `bool`')
    if params.get("out_mask", None) is not None:
        if not isinstance(params["out_mask"], str):
            raise StyxValidationError(f'`out_mask` has the wrong type: Received `{type(params.get("out_mask", None))}` expected `str | None`')
    if params.get("varnorm_flag", False) is None:
        raise StyxValidationError("`varnorm_flag` must not be None")
    if not isinstance(params["varnorm_flag"], bool):
        raise StyxValidationError(f'`varnorm_flag` has the wrong type: Received `{type(params.get("varnorm_flag", False))}` expected `bool`')
    if params.get("fwhm", None) is not None:
        if not isinstance(params["fwhm"], (float, int)):
            raise StyxValidationError(f'`fwhm` has the wrong type: Received `{type(params.get("fwhm", None))}` expected `float | None`')
    if params.get("niters_only", None) is not None:
        if not isinstance(params["niters_only"], str):
            raise StyxValidationError(f'`niters_only` has the wrong type: Received `{type(params.get("niters_only", None))}` expected `str | None`')
    if params.get("output_file", None) is None:
        raise StyxValidationError("`output_file` must not be None")
    if not isinstance(params["output_file"], str):
        raise StyxValidationError(f'`output_file` has the wrong type: Received `{type(params.get("output_file", None))}` expected `str`')
    if params.get("sd", None) is not None:
        if not isinstance(params["sd"], str):
            raise StyxValidationError(f'`sd` has the wrong type: Received `{type(params.get("sd", None))}` expected `str | None`')
    if params.get("synth_flag", False) is None:
        raise StyxValidationError("`synth_flag` must not be None")
    if not isinstance(params["synth_flag"], bool):
        raise StyxValidationError(f'`synth_flag` has the wrong type: Received `{type(params.get("synth_flag", False))}` expected `bool`')
    if params.get("synth_frames", None) is not None:
        if not isinstance(params["synth_frames"], (float, int)):
            raise StyxValidationError(f'`synth_frames` has the wrong type: Received `{type(params.get("synth_frames", None))}` expected `float | None`')
    if params.get("threads", None) is not None:
        if not isinstance(params["threads"], (float, int)):
            raise StyxValidationError(f'`threads` has the wrong type: Received `{type(params.get("threads", None))}` expected `float | None`')
    if params.get("debug_flag", False) is None:
        raise StyxValidationError("`debug_flag` must not be None")
    if not isinstance(params["debug_flag"], bool):
        raise StyxValidationError(f'`debug_flag` has the wrong type: Received `{type(params.get("debug_flag", False))}` expected `bool`')
    if params.get("checkopts_flag", False) is None:
        raise StyxValidationError("`checkopts_flag` must not be None")
    if not isinstance(params["checkopts_flag"], bool):
        raise StyxValidationError(f'`checkopts_flag` has the wrong type: Received `{type(params.get("checkopts_flag", False))}` expected `bool`')
    if params.get("version_flag", False) is None:
        raise StyxValidationError("`version_flag` must not be None")
    if not isinstance(params["version_flag"], bool):
        raise StyxValidationError(f'`version_flag` has the wrong type: Received `{type(params.get("version_flag", False))}` expected `bool`')


def mris_fwhm_cargs(
    params: MrisFwhmParamsDict,
    execution: Execution,
) -> list[str]:
    """
    Build command-line arguments from parameters.
    
    Args:
        params: The parameters.
        execution: The execution object for resolving input paths.
    Returns:
        Command-line arguments.
    """
    cargs = []
    cargs.append("mris_fwhm")
    cargs.extend([
        "--i",
        execution.input_file(params.get("input_file", None))
    ])
    cargs.extend([
        "--subject",
        params.get("subject", None)
    ])
    cargs.extend([
        "--hemi",
        params.get("hemi", None)
    ])
    if params.get("surf", None) is not None:
        cargs.extend([
            "--surf",
            params.get("surf", None)
        ])
    if params.get("label_file", None) is not None:
        cargs.extend([
            "--label",
            execution.input_file(params.get("label_file", None))
        ])
    if params.get("cortex_flag", False):
        cargs.append("--cortex")
    if params.get("mask_file", None) is not None:
        cargs.extend([
            "--mask",
            execution.input_file(params.get("mask_file", None))
        ])
    if params.get("x_matrix", None) is not None:
        cargs.extend([
            "--X",
            execution.input_file(params.get("x_matrix", None))
        ])
    if params.get("detrend_order", None) is not None:
        cargs.extend([
            "--detrend",
            str(params.get("detrend_order", None))
        ])
    if params.get("smooth_only_flag", False):
        cargs.append("--smooth-only")
    if params.get("no_detrend_flag", False):
        cargs.append("--no-detrend")
    if params.get("sqr_flag", False):
        cargs.append("--sqr")
    if params.get("sum_file", None) is not None:
        cargs.extend([
            "--sum",
            params.get("sum_file", None)
        ])
    if params.get("dat_file", None) is not None:
        cargs.extend([
            "--dat",
            params.get("dat_file", None)
        ])
    if params.get("ar1dat_file", None) is not None:
        cargs.extend([
            "--ar1dat",
            params.get("ar1dat_file", None)
        ])
    if params.get("ar1vol", None) is not None:
        cargs.extend([
            "--ar1",
            params.get("ar1vol", None)
        ])
    if params.get("fwhmmap", None) is not None:
        cargs.extend([
            "--fwhm-map",
            params.get("fwhmmap", None)
        ])
    if params.get("prune_flag", False):
        cargs.append("--prune")
    if params.get("no_prune_flag", False):
        cargs.append("--no-prune")
    if params.get("out_mask", None) is not None:
        cargs.extend([
            "--out-mask",
            params.get("out_mask", None)
        ])
    if params.get("varnorm_flag", False):
        cargs.append("--varnorm")
    if params.get("fwhm", None) is not None:
        cargs.extend([
            "--fwhm",
            str(params.get("fwhm", None))
        ])
    if params.get("niters_only", None) is not None:
        cargs.extend([
            "--niters-only",
            params.get("niters_only", None)
        ])
    cargs.extend([
        "--o",
        params.get("output_file", None)
    ])
    if params.get("sd", None) is not None:
        cargs.extend([
            "--sd",
            params.get("sd", None)
        ])
    if params.get("synth_flag", False):
        cargs.append("--synth")
    if params.get("synth_frames", None) is not None:
        cargs.extend([
            "--synth-frames",
            str(params.get("synth_frames", None))
        ])
    if params.get("threads", None) is not None:
        cargs.extend([
            "--threads",
            str(params.get("threads", None))
        ])
    if params.get("debug_flag", False):
        cargs.append("--debug")
    if params.get("checkopts_flag", False):
        cargs.append("--checkopts")
    if params.get("version_flag", False):
        cargs.append("--version")
    return cargs


def mris_fwhm_outputs(
    params: MrisFwhmParamsDict,
    execution: Execution,
) -> MrisFwhmOutputs:
    """
    Build outputs object containing output file paths and possibly stdout/stderr.
    
    Args:
        params: The parameters.
        execution: The execution object for resolving input paths.
    Returns:
        Outputs object.
    """
    ret = MrisFwhmOutputs(
        root=execution.output_file("."),
    )
    return ret


def mris_fwhm_execute(
    params: MrisFwhmParamsDict,
    runner: Runner | None = None,
) -> MrisFwhmOutputs:
    """
    mris_fwhm
    
    Smooths surface data and/or estimates FWHM.
    
    Author: FreeSurfer Developers
    
    URL: https://github.com/freesurfer/freesurfer
    
    Args:
        params: The parameters.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `MrisFwhmOutputs`).
    """
    mris_fwhm_validate(params)
    runner = runner or get_global_runner()
    execution = runner.start_execution(MRIS_FWHM_METADATA)
    params = execution.params(params)
    cargs = mris_fwhm_cargs(params, execution)
    ret = mris_fwhm_outputs(params, execution)
    execution.run(cargs)
    return ret


def mris_fwhm(
    input_file: InputPathType,
    subject: str,
    hemi: str,
    output_file: str,
    surf: str | None = None,
    label_file: InputPathType | None = None,
    cortex_flag: bool = False,
    mask_file: InputPathType | None = None,
    x_matrix: InputPathType | None = None,
    detrend_order: float | None = None,
    smooth_only_flag: bool = False,
    no_detrend_flag: bool = False,
    sqr_flag: bool = False,
    sum_file: str | None = None,
    dat_file: str | None = None,
    ar1dat_file: str | None = None,
    ar1vol: str | None = None,
    fwhmmap: str | None = None,
    prune_flag: bool = False,
    no_prune_flag: bool = False,
    out_mask: str | None = None,
    varnorm_flag: bool = False,
    fwhm: float | None = None,
    niters_only: str | None = None,
    sd: str | None = None,
    synth_flag: bool = False,
    synth_frames: float | None = None,
    threads: float | None = None,
    debug_flag: bool = False,
    checkopts_flag: bool = False,
    version_flag: bool = False,
    runner: Runner | None = None,
) -> MrisFwhmOutputs:
    """
    mris_fwhm
    
    Smooths surface data and/or estimates FWHM.
    
    Author: FreeSurfer Developers
    
    URL: https://github.com/freesurfer/freesurfer
    
    Args:
        input_file: Input data file.
        subject: Subject whose surface the input is defined on.
        hemi: Hemifield that the input is defined on. Legal values are lh and\
            rh.
        output_file: Output file to save the processed data.
        surf: Surface name to compute AR1 on. Default is white.
        label_file: Label file to be used as a mask.
        cortex_flag: Use hemi.cortex.label as a mask.
        mask_file: Mask file. Compute AR1 only over voxels in the given mask.
        x_matrix: Detrend data with the matrix in x.mat.
        detrend_order: Order of polynomial detrending.
        smooth_only_flag: Only smooth the data, implies --no-detrend.
        no_detrend_flag: Turn off polynomial detrending.
        sqr_flag: Compute square of input before smoothing.
        sum_file: Prints ascii summary to sumfile.
        dat_file: File for FWHM data.
        ar1dat_file: File containing ar1mean and ar1std.
        ar1vol: Save spatial AR1 as an overlay.
        fwhmmap: Save vertex-wise spatial FWHM as an overlay.
        prune_flag: Remove any voxel that is zero in any subject (after any\
            inversion).
        no_prune_flag: Do not prune (default).
        out_mask: File to save the final mask.
        varnorm_flag: Normalize the variance across space within any mask.
        fwhm: Smooth input by the specified FWHM in mm.
        niters_only: File that reports the number of iterations needed to\
            achieve the specified FWHM.
        sd: Subjects directory.
        synth_flag: Synthesize input with white gaussian noise.
        synth_frames: Number of frames for synthesized input.
        threads: Number of threads to use.
        debug_flag: Turn on debugging.
        checkopts_flag: Don't run anything, just check options and exit.
        version_flag: Print out version and exit.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `MrisFwhmOutputs`).
    """
    params = mris_fwhm_params(
        input_file=input_file,
        subject=subject,
        hemi=hemi,
        surf=surf,
        label_file=label_file,
        cortex_flag=cortex_flag,
        mask_file=mask_file,
        x_matrix=x_matrix,
        detrend_order=detrend_order,
        smooth_only_flag=smooth_only_flag,
        no_detrend_flag=no_detrend_flag,
        sqr_flag=sqr_flag,
        sum_file=sum_file,
        dat_file=dat_file,
        ar1dat_file=ar1dat_file,
        ar1vol=ar1vol,
        fwhmmap=fwhmmap,
        prune_flag=prune_flag,
        no_prune_flag=no_prune_flag,
        out_mask=out_mask,
        varnorm_flag=varnorm_flag,
        fwhm=fwhm,
        niters_only=niters_only,
        output_file=output_file,
        sd=sd,
        synth_flag=synth_flag,
        synth_frames=synth_frames,
        threads=threads,
        debug_flag=debug_flag,
        checkopts_flag=checkopts_flag,
        version_flag=version_flag,
    )
    return mris_fwhm_execute(params, runner)


__all__ = [
    "MRIS_FWHM_METADATA",
    "MrisFwhmOutputs",
    "MrisFwhmParamsDict",
    "MrisFwhmParamsDictTagged",
    "mris_fwhm",
    "mris_fwhm_execute",
    "mris_fwhm_params",
]
