# This file was auto generated by Styx.
# Do not edit this file directly.

import typing
import pathlib
from styxdefs import *

MRI_PAINT_METADATA = Metadata(
    id="dec6a4dce4882cf1b15d956692f1b6e908fbc2be.boutiques",
    name="mri_paint",
    package="freesurfer",
    container_image_tag="freesurfer/freesurfer:7.4.1",
)


_MriPaintParamsDictNoTag = typing.TypedDict('_MriPaintParamsDictNoTag', {
    "input_volume": InputPathType,
    "input_surface": InputPathType,
    "registration_file": InputPathType,
    "output_float_file": str,
    "image_offset": typing.NotRequired[float | None],
    "paint_surf_coords": bool,
})
MriPaintParamsDictTagged = typing.TypedDict('MriPaintParamsDictTagged', {
    "@type": typing.Literal["freesurfer/mri_paint"],
    "input_volume": InputPathType,
    "input_surface": InputPathType,
    "registration_file": InputPathType,
    "output_float_file": str,
    "image_offset": typing.NotRequired[float | None],
    "paint_surf_coords": bool,
})
MriPaintParamsDict = _MriPaintParamsDictNoTag | MriPaintParamsDictTagged


class MriPaintOutputs(typing.NamedTuple):
    """
    Output object returned when calling `MriPaintParamsDict(...)`.
    """
    root: OutputPathType
    """Output root folder. This is the root folder for all outputs."""
    output_float: OutputPathType
    """The output .float file resulting from the painting process"""


def mri_paint_params(
    input_volume: InputPathType,
    input_surface: InputPathType,
    registration_file: InputPathType,
    output_float_file: str,
    image_offset: float | None = None,
    paint_surf_coords: bool = False,
) -> MriPaintParamsDictTagged:
    """
    Build parameters.
    
    Args:
        input_volume: The input volume file.
        input_surface: The input surface file.
        registration_file: The registration file.
        output_float_file: The output .float file.
        image_offset: Set offset to use.
        paint_surf_coords: Paint using surface coordinates.
    Returns:
        Parameter dictionary
    """
    params = {
        "@type": "freesurfer/mri_paint",
        "input_volume": input_volume,
        "input_surface": input_surface,
        "registration_file": registration_file,
        "output_float_file": output_float_file,
        "paint_surf_coords": paint_surf_coords,
    }
    if image_offset is not None:
        params["image_offset"] = image_offset
    return params


def mri_paint_validate(
    params: typing.Any,
) -> None:
    """
    Validate parameters. Throws an error if `params` is not a valid
    `MriPaintParamsDict` object.
    
    Args:
        params: The parameters object to validate.
    """
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("input_volume", None) is None:
        raise StyxValidationError("`input_volume` must not be None")
    if not isinstance(params["input_volume"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_volume` has the wrong type: Received `{type(params.get("input_volume", None))}` expected `InputPathType`')
    if params.get("input_surface", None) is None:
        raise StyxValidationError("`input_surface` must not be None")
    if not isinstance(params["input_surface"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_surface` has the wrong type: Received `{type(params.get("input_surface", None))}` expected `InputPathType`')
    if params.get("registration_file", None) is None:
        raise StyxValidationError("`registration_file` must not be None")
    if not isinstance(params["registration_file"], (pathlib.Path, str)):
        raise StyxValidationError(f'`registration_file` has the wrong type: Received `{type(params.get("registration_file", None))}` expected `InputPathType`')
    if params.get("output_float_file", None) is None:
        raise StyxValidationError("`output_float_file` must not be None")
    if not isinstance(params["output_float_file"], str):
        raise StyxValidationError(f'`output_float_file` has the wrong type: Received `{type(params.get("output_float_file", None))}` expected `str`')
    if params.get("image_offset", None) is not None:
        if not isinstance(params["image_offset"], (float, int)):
            raise StyxValidationError(f'`image_offset` has the wrong type: Received `{type(params.get("image_offset", None))}` expected `float | None`')
    if params.get("paint_surf_coords", False) is None:
        raise StyxValidationError("`paint_surf_coords` must not be None")
    if not isinstance(params["paint_surf_coords"], bool):
        raise StyxValidationError(f'`paint_surf_coords` has the wrong type: Received `{type(params.get("paint_surf_coords", False))}` expected `bool`')


def mri_paint_cargs(
    params: MriPaintParamsDict,
    execution: Execution,
) -> list[str]:
    """
    Build command-line arguments from parameters.
    
    Args:
        params: The parameters.
        execution: The execution object for resolving input paths.
    Returns:
        Command-line arguments.
    """
    cargs = []
    cargs.append("mri_paint")
    cargs.append(execution.input_file(params.get("input_volume", None)))
    cargs.append(execution.input_file(params.get("input_surface", None)))
    cargs.append(execution.input_file(params.get("registration_file", None)))
    cargs.append(params.get("output_float_file", None))
    if params.get("image_offset", None) is not None:
        cargs.extend([
            "-imageoffset",
            str(params.get("image_offset", None))
        ])
    if params.get("paint_surf_coords", False):
        cargs.append("-S")
    return cargs


def mri_paint_outputs(
    params: MriPaintParamsDict,
    execution: Execution,
) -> MriPaintOutputs:
    """
    Build outputs object containing output file paths and possibly stdout/stderr.
    
    Args:
        params: The parameters.
        execution: The execution object for resolving input paths.
    Returns:
        Outputs object.
    """
    ret = MriPaintOutputs(
        root=execution.output_file("."),
        output_float=execution.output_file(params.get("output_float_file", None)),
    )
    return ret


def mri_paint_execute(
    params: MriPaintParamsDict,
    runner: Runner | None = None,
) -> MriPaintOutputs:
    """
    mri_paint
    
    This program will paint average Talairach stats onto a surface.
    
    Author: FreeSurfer Developers
    
    URL: https://github.com/freesurfer/freesurfer
    
    Args:
        params: The parameters.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `MriPaintOutputs`).
    """
    mri_paint_validate(params)
    runner = runner or get_global_runner()
    execution = runner.start_execution(MRI_PAINT_METADATA)
    params = execution.params(params)
    cargs = mri_paint_cargs(params, execution)
    ret = mri_paint_outputs(params, execution)
    execution.run(cargs)
    return ret


def mri_paint(
    input_volume: InputPathType,
    input_surface: InputPathType,
    registration_file: InputPathType,
    output_float_file: str,
    image_offset: float | None = None,
    paint_surf_coords: bool = False,
    runner: Runner | None = None,
) -> MriPaintOutputs:
    """
    mri_paint
    
    This program will paint average Talairach stats onto a surface.
    
    Author: FreeSurfer Developers
    
    URL: https://github.com/freesurfer/freesurfer
    
    Args:
        input_volume: The input volume file.
        input_surface: The input surface file.
        registration_file: The registration file.
        output_float_file: The output .float file.
        image_offset: Set offset to use.
        paint_surf_coords: Paint using surface coordinates.
        runner: Command runner.
    Returns:
        NamedTuple of outputs (described in `MriPaintOutputs`).
    """
    params = mri_paint_params(
        input_volume=input_volume,
        input_surface=input_surface,
        registration_file=registration_file,
        output_float_file=output_float_file,
        image_offset=image_offset,
        paint_surf_coords=paint_surf_coords,
    )
    return mri_paint_execute(params, runner)


__all__ = [
    "MRI_PAINT_METADATA",
    "MriPaintOutputs",
    "MriPaintParamsDict",
    "MriPaintParamsDictTagged",
    "mri_paint",
    "mri_paint_execute",
    "mri_paint_params",
]
