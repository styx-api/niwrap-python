# This file was auto generated by Styx.
# Do not edit this file directly.

import dataclasses
import pathlib
import typing

from styxdefs import Execution, InputPathType, Metadata, Runner, StyxValidationError, OutputPathType, get_global_runner

DCM2NIIX_METADATA = Metadata(
    id="dcm2niix",
    name="dcm2niix",
    package="dcm2niix",
)

Dcm2niixAdjacent = typing.Literal["y"] | typing.Literal["n"]

Dcm2niixBids = typing.Literal["y"] | typing.Literal["n"] | typing.Literal["o"]

Dcm2niixExportFormat = typing.Literal["y"] | typing.Literal["n"] | typing.Literal["o"] | typing.Literal["j"] | typing.Literal["b"]

Dcm2niixDefaults = typing.Literal["y"] | typing.Literal["n"] | typing.Literal["o"] | typing.Literal["i"]

Dcm2niixMerge2d = typing.Literal["n"] | typing.Literal["y"] | typing.Literal[0] | typing.Literal[1] | typing.Literal[2]

Dcm2niixSearchMode = typing.Literal["y"] | typing.Literal["l"] | typing.Literal["n"]

Dcm2niixVerbose = typing.Literal[0] | typing.Literal[1] | typing.Literal[2]

Dcm2niixCrop3d = typing.Literal["y"] | typing.Literal["n"] | typing.Literal["i"]

Dcm2niixCompression = typing.Literal["y"] | typing.Literal["o"] | typing.Literal["i"] | typing.Literal["n"] | typing.Literal[3]

Dcm2niix = typing.TypedDict(
    "Dcm2niix",
    {
        "@type": typing.NotRequired[typing.Literal["dcm2niix/dcm2niix"]],
        "compression_level": typing.NotRequired[float],
        "adjacent": typing.NotRequired[Dcm2niixAdjacent],
        "bids": typing.NotRequired[Dcm2niixBids],
        "bids_anon": typing.NotRequired[Dcm2niixAdjacent],
        "comment": typing.NotRequired[str],
        "depth": typing.NotRequired[float],
        "export_format": typing.NotRequired[Dcm2niixExportFormat],
        "filename": typing.NotRequired[str],
        "defaults": typing.NotRequired[Dcm2niixDefaults],
        "ignore_derived": typing.NotRequired[Dcm2niixAdjacent],
        "scaling": typing.NotRequired[Dcm2niixBids],
        "merge_2d": typing.NotRequired[Dcm2niixMerge2d],
        "series_number": typing.NotRequired[str],
        "output_dir": typing.NotRequired[str],
        "philips_scaling": typing.NotRequired[Dcm2niixAdjacent],
        "search_mode": typing.NotRequired[Dcm2niixSearchMode],
        "rename": typing.NotRequired[Dcm2niixAdjacent],
        "single_file": typing.NotRequired[Dcm2niixAdjacent],
        "update_check": typing.NotRequired[bool],
        "verbose": typing.NotRequired[Dcm2niixVerbose],
        "conflict_behavior": typing.NotRequired[Dcm2niixVerbose],
        "crop_3d": typing.NotRequired[Dcm2niixCrop3d],
        "compression": typing.NotRequired[Dcm2niixCompression],
        "endian": typing.NotRequired[Dcm2niixBids],
        "progress": typing.NotRequired[Dcm2niixAdjacent],
        "ignore_trigger": typing.NotRequired[bool],
        "terse": typing.NotRequired[bool],
        "xml": typing.NotRequired[bool],
        "input_dir": InputPathType,
    },
)

@dataclasses.dataclass
class Dcm2niixOutputs:
    """Output paths produced by the tool."""
    root: OutputPathType

def dcm2niix_params(
    input_dir: InputPathType,
    compression_level: float | None = None,
    adjacent: Dcm2niixAdjacent | None = None,
    bids: Dcm2niixBids | None = None,
    bids_anon: Dcm2niixAdjacent | None = None,
    comment: str | None = None,
    depth: float | None = None,
    export_format: Dcm2niixExportFormat | None = None,
    filename: str | None = None,
    defaults: Dcm2niixDefaults | None = None,
    ignore_derived: Dcm2niixAdjacent | None = None,
    scaling: Dcm2niixBids | None = None,
    merge_2d: Dcm2niixMerge2d | None = None,
    series_number: str | None = None,
    output_dir: str | None = ".",
    philips_scaling: Dcm2niixAdjacent | None = None,
    search_mode: Dcm2niixSearchMode | None = None,
    rename: Dcm2niixAdjacent | None = None,
    single_file: Dcm2niixAdjacent | None = None,
    update_check: bool = False,
    verbose: Dcm2niixVerbose | None = None,
    conflict_behavior: Dcm2niixVerbose | None = None,
    crop_3d: Dcm2niixCrop3d | None = None,
    compression: Dcm2niixCompression | None = None,
    endian: Dcm2niixBids | None = None,
    progress: Dcm2niixAdjacent | None = None,
    ignore_trigger: bool = False,
    terse: bool = False,
    xml: bool = False,
) -> Dcm2niix:
    """
    Build parameters.

    Args:
        input_dir: Input folder containing DICOM files. Will be searched\
            recursively based on depth parameter.
        compression_level: gz compression level (1=fastest..9=smallest)
        adjacent: Adjacent DICOMs (images from same series always in same folder)\
            for faster conversion
        bids: Generate BIDS sidecar JSON files (o=only: no NIfTI)
        bids_anon: Anonymize BIDS sidecar files by removing personal information
        comment: Comment stored in NIfTI aux_file (up to 24 characters)
        depth: Directory search depth for DICOM files in sub-folders
        export_format: Export format: NRRD (y), MGH (o), JSON/JNIfTI (j), or\
            BJNIfTI (b)
        filename: Output filename template (%a=antenna, %b=basename, %c=comments,\
            %d=description, %e=echo number, %f=folder name, %g=accession number,\
            %i=ID of patient, %j=seriesInstanceUID, %k=studyInstanceUID,\
            %m=manufacturer, %n=name of patient, %o=mediaObjectInstanceUID,\
            %p=protocol, %r=instance number, %s=series number, %t=time,\
            %u=acquisition number, %v=vendor, %x=study ID; %z=sequence name)
        defaults: Generate defaults file (o=only: reset and write defaults;\
            i=ignore: reset defaults)
        ignore_derived: Ignore derived, localizer and 2D images
        scaling: Losslessly scale 16-bit integers (y=scale, n=no but uint16->int16,\
            o=original)
        merge_2d: Merge 2D slices from same series regardless of echo, exposure,\
            etc. (0=no, 1=yes, 2=auto)
        series_number: Only convert specified series CRC number (can be used up to\
            16 times)
        output_dir: Output directory (omit to save to input folder)
        philips_scaling: Use Philips precise float (not display) scaling
        search_mode: Search mode (y=show number of DICOMs, l=list DICOMs, n=no)
        rename: Rename instead of convert DICOMs
        single_file: Single file mode, do not convert other images in folder
        update_check: Check for newer versions
        verbose: Verbose output (0=no, 1=yes, 2=logorrheic)
        conflict_behavior: Write behavior for name conflicts (0=skip, 1=overwrite,\
            2=add suffix)
        crop_3d: Crop 3D acquisitions (i=ignore: neither crop nor rotate)
        compression: gz compress images (y=pigz, o=optimal pigz, i=internal:zlib,\
            n=no, 3=no,3D)
        endian: Byte order (y=big-end, n=little-end, o=optimal/native)
        progress: Slicer format progress information
        ignore_trigger: Disregard values in 0018,1060 and 0020,9153
        terse: Omit filename post-fixes (can cause overwrites)
        xml: Output Slicer format features

    Returns:
        Parameter dictionary.
    """
    params: Dcm2niix = {
        "@type": "dcm2niix/dcm2niix",
        "input_dir": input_dir,
        "update_check": update_check,
        "ignore_trigger": ignore_trigger,
        "terse": terse,
        "xml": xml,
    }
    if compression_level is not None:
        params["compression_level"] = compression_level
    if adjacent is not None:
        params["adjacent"] = adjacent
    if bids is not None:
        params["bids"] = bids
    if bids_anon is not None:
        params["bids_anon"] = bids_anon
    if comment is not None:
        params["comment"] = comment
    if depth is not None:
        params["depth"] = depth
    if export_format is not None:
        params["export_format"] = export_format
    if filename is not None:
        params["filename"] = filename
    if defaults is not None:
        params["defaults"] = defaults
    if ignore_derived is not None:
        params["ignore_derived"] = ignore_derived
    if scaling is not None:
        params["scaling"] = scaling
    if merge_2d is not None:
        params["merge_2d"] = merge_2d
    if series_number is not None:
        params["series_number"] = series_number
    if output_dir is not None:
        params["output_dir"] = output_dir
    if philips_scaling is not None:
        params["philips_scaling"] = philips_scaling
    if search_mode is not None:
        params["search_mode"] = search_mode
    if rename is not None:
        params["rename"] = rename
    if single_file is not None:
        params["single_file"] = single_file
    if verbose is not None:
        params["verbose"] = verbose
    if conflict_behavior is not None:
        params["conflict_behavior"] = conflict_behavior
    if crop_3d is not None:
        params["crop_3d"] = crop_3d
    if compression is not None:
        params["compression"] = compression
    if endian is not None:
        params["endian"] = endian
    if progress is not None:
        params["progress"] = progress
    return params

def dcm2niix_validate(params: typing.Any) -> None:
    """Validate untrusted parameters. Raises StyxValidationError if `params` is not a valid Dcm2niix."""
    if params is None or not isinstance(params, dict):
        raise StyxValidationError(f'Params object has the wrong type \'{type(params)}\'')
    if params.get("compression_level", None) is not None:
        if not isinstance(params["compression_level"], (float, int)):
            raise StyxValidationError(f'`compression_level` has the wrong type: Received `{type(params["compression_level"])}` expected `float`')
        if not (1 <= params["compression_level"] <= 9):
            raise StyxValidationError("Parameter `compression_level` must be between 1 and 9 (inclusive)")
    if params.get("adjacent", None) is not None:
        if not isinstance(params["adjacent"], str):
            raise StyxValidationError(f'`adjacent` has the wrong type: Received `{type(params["adjacent"])}` expected `Dcm2niixAdjacent`')
        if params["adjacent"] not in ["y", "n"]:
            raise StyxValidationError("Parameter `adjacent` must be one of [\"y\", \"n\"]")
    if params.get("bids", None) is not None:
        if not isinstance(params["bids"], str):
            raise StyxValidationError(f'`bids` has the wrong type: Received `{type(params["bids"])}` expected `Dcm2niixBids`')
        if params["bids"] not in ["y", "n", "o"]:
            raise StyxValidationError("Parameter `bids` must be one of [\"y\", \"n\", \"o\"]")
    if params.get("bids_anon", None) is not None:
        if not isinstance(params["bids_anon"], str):
            raise StyxValidationError(f'`bids_anon` has the wrong type: Received `{type(params["bids_anon"])}` expected `Dcm2niixAdjacent`')
        if params["bids_anon"] not in ["y", "n"]:
            raise StyxValidationError("Parameter `bids_anon` must be one of [\"y\", \"n\"]")
    if params.get("comment", None) is not None:
        if not isinstance(params["comment"], str):
            raise StyxValidationError(f'`comment` has the wrong type: Received `{type(params["comment"])}` expected `str`')
    if params.get("depth", None) is not None:
        if not isinstance(params["depth"], (float, int)):
            raise StyxValidationError(f'`depth` has the wrong type: Received `{type(params["depth"])}` expected `float`')
        if not (0 <= params["depth"] <= 9):
            raise StyxValidationError("Parameter `depth` must be between 0 and 9 (inclusive)")
    if params.get("export_format", None) is not None:
        if not isinstance(params["export_format"], str):
            raise StyxValidationError(f'`export_format` has the wrong type: Received `{type(params["export_format"])}` expected `Dcm2niixExportFormat`')
        if params["export_format"] not in ["y", "n", "o", "j", "b"]:
            raise StyxValidationError("Parameter `export_format` must be one of [\"y\", \"n\", \"o\", \"j\", \"b\"]")
    if params.get("filename", None) is not None:
        if not isinstance(params["filename"], str):
            raise StyxValidationError(f'`filename` has the wrong type: Received `{type(params["filename"])}` expected `str`')
    if params.get("defaults", None) is not None:
        if not isinstance(params["defaults"], str):
            raise StyxValidationError(f'`defaults` has the wrong type: Received `{type(params["defaults"])}` expected `Dcm2niixDefaults`')
        if params["defaults"] not in ["y", "n", "o", "i"]:
            raise StyxValidationError("Parameter `defaults` must be one of [\"y\", \"n\", \"o\", \"i\"]")
    if params.get("ignore_derived", None) is not None:
        if not isinstance(params["ignore_derived"], str):
            raise StyxValidationError(f'`ignore_derived` has the wrong type: Received `{type(params["ignore_derived"])}` expected `Dcm2niixAdjacent`')
        if params["ignore_derived"] not in ["y", "n"]:
            raise StyxValidationError("Parameter `ignore_derived` must be one of [\"y\", \"n\"]")
    if params.get("scaling", None) is not None:
        if not isinstance(params["scaling"], str):
            raise StyxValidationError(f'`scaling` has the wrong type: Received `{type(params["scaling"])}` expected `Dcm2niixBids`')
        if params["scaling"] not in ["y", "n", "o"]:
            raise StyxValidationError("Parameter `scaling` must be one of [\"y\", \"n\", \"o\"]")
    if params.get("merge_2d", None) is not None:
        if not isinstance(params["merge_2d"], (float, int)):
            raise StyxValidationError(f'`merge_2d` has the wrong type: Received `{type(params["merge_2d"])}` expected `Dcm2niixMerge2d`')
        if params["merge_2d"] not in ["n", "y", 0, 1, 2]:
            raise StyxValidationError("Parameter `merge_2d` must be one of [\"n\", \"y\", 0, 1, 2]")
    if params.get("series_number", None) is not None:
        if not isinstance(params["series_number"], str):
            raise StyxValidationError(f'`series_number` has the wrong type: Received `{type(params["series_number"])}` expected `str`')
    if params.get("output_dir", None) is not None:
        if not isinstance(params["output_dir"], str):
            raise StyxValidationError(f'`output_dir` has the wrong type: Received `{type(params["output_dir"])}` expected `str`')
    if params.get("philips_scaling", None) is not None:
        if not isinstance(params["philips_scaling"], str):
            raise StyxValidationError(f'`philips_scaling` has the wrong type: Received `{type(params["philips_scaling"])}` expected `Dcm2niixAdjacent`')
        if params["philips_scaling"] not in ["y", "n"]:
            raise StyxValidationError("Parameter `philips_scaling` must be one of [\"y\", \"n\"]")
    if params.get("search_mode", None) is not None:
        if not isinstance(params["search_mode"], str):
            raise StyxValidationError(f'`search_mode` has the wrong type: Received `{type(params["search_mode"])}` expected `Dcm2niixSearchMode`')
        if params["search_mode"] not in ["y", "l", "n"]:
            raise StyxValidationError("Parameter `search_mode` must be one of [\"y\", \"l\", \"n\"]")
    if params.get("rename", None) is not None:
        if not isinstance(params["rename"], str):
            raise StyxValidationError(f'`rename` has the wrong type: Received `{type(params["rename"])}` expected `Dcm2niixAdjacent`')
        if params["rename"] not in ["y", "n"]:
            raise StyxValidationError("Parameter `rename` must be one of [\"y\", \"n\"]")
    if params.get("single_file", None) is not None:
        if not isinstance(params["single_file"], str):
            raise StyxValidationError(f'`single_file` has the wrong type: Received `{type(params["single_file"])}` expected `Dcm2niixAdjacent`')
        if params["single_file"] not in ["y", "n"]:
            raise StyxValidationError("Parameter `single_file` must be one of [\"y\", \"n\"]")
    if params.get("update_check", None) is not None:
        if not isinstance(params["update_check"], bool):
            raise StyxValidationError(f'`update_check` has the wrong type: Received `{type(params["update_check"])}` expected `bool`')
    if params.get("verbose", None) is not None:
        if not isinstance(params["verbose"], (float, int)):
            raise StyxValidationError(f'`verbose` has the wrong type: Received `{type(params["verbose"])}` expected `Dcm2niixVerbose`')
        if params["verbose"] not in [0, 1, 2]:
            raise StyxValidationError("Parameter `verbose` must be one of [0, 1, 2]")
    if params.get("conflict_behavior", None) is not None:
        if not isinstance(params["conflict_behavior"], (float, int)):
            raise StyxValidationError(f'`conflict_behavior` has the wrong type: Received `{type(params["conflict_behavior"])}` expected `Dcm2niixVerbose`')
        if params["conflict_behavior"] not in [0, 1, 2]:
            raise StyxValidationError("Parameter `conflict_behavior` must be one of [0, 1, 2]")
    if params.get("crop_3d", None) is not None:
        if not isinstance(params["crop_3d"], str):
            raise StyxValidationError(f'`crop_3d` has the wrong type: Received `{type(params["crop_3d"])}` expected `Dcm2niixCrop3d`')
        if params["crop_3d"] not in ["y", "n", "i"]:
            raise StyxValidationError("Parameter `crop_3d` must be one of [\"y\", \"n\", \"i\"]")
    if params.get("compression", None) is not None:
        if not isinstance(params["compression"], (float, int)):
            raise StyxValidationError(f'`compression` has the wrong type: Received `{type(params["compression"])}` expected `Dcm2niixCompression`')
        if params["compression"] not in ["y", "o", "i", "n", 3]:
            raise StyxValidationError("Parameter `compression` must be one of [\"y\", \"o\", \"i\", \"n\", 3]")
    if params.get("endian", None) is not None:
        if not isinstance(params["endian"], str):
            raise StyxValidationError(f'`endian` has the wrong type: Received `{type(params["endian"])}` expected `Dcm2niixBids`')
        if params["endian"] not in ["y", "n", "o"]:
            raise StyxValidationError("Parameter `endian` must be one of [\"y\", \"n\", \"o\"]")
    if params.get("progress", None) is not None:
        if not isinstance(params["progress"], str):
            raise StyxValidationError(f'`progress` has the wrong type: Received `{type(params["progress"])}` expected `Dcm2niixAdjacent`')
        if params["progress"] not in ["y", "n"]:
            raise StyxValidationError("Parameter `progress` must be one of [\"y\", \"n\"]")
    if params.get("ignore_trigger", None) is not None:
        if not isinstance(params["ignore_trigger"], bool):
            raise StyxValidationError(f'`ignore_trigger` has the wrong type: Received `{type(params["ignore_trigger"])}` expected `bool`')
    if params.get("terse", None) is not None:
        if not isinstance(params["terse"], bool):
            raise StyxValidationError(f'`terse` has the wrong type: Received `{type(params["terse"])}` expected `bool`')
    if params.get("xml", None) is not None:
        if not isinstance(params["xml"], bool):
            raise StyxValidationError(f'`xml` has the wrong type: Received `{type(params["xml"])}` expected `bool`')
    if params.get("input_dir", None) is None:
        raise StyxValidationError("`input_dir` must not be None")
    if not isinstance(params["input_dir"], (pathlib.Path, str)):
        raise StyxValidationError(f'`input_dir` has the wrong type: Received `{type(params["input_dir"])}` expected `InputPathType`')

def dcm2niix_cargs(params: Dcm2niix, execution: Execution) -> list[str]:
    """Build command-line arguments from parameters."""
    cargs: list[str] = []
    cargs.append("dcm2niix")
    v_0 = params.get("compression_level")
    if v_0 is not None:
        cargs.append("-")
        cargs.append(str(v_0))
    v_1 = params.get("adjacent")
    if v_1 is not None:
        cargs.append("-a")
        cargs.append(str(v_1))
    v_2 = params.get("bids")
    if v_2 is not None:
        cargs.append("-b")
        cargs.append(str(v_2))
    v_3 = params.get("bids_anon")
    if v_3 is not None:
        cargs.append("-ba")
        cargs.append(str(v_3))
    v_4 = params.get("comment")
    if v_4 is not None:
        cargs.append("-c")
        cargs.append(v_4)
    v_5 = params.get("depth")
    if v_5 is not None:
        cargs.append("-d")
        cargs.append(str(v_5))
    v_6 = params.get("export_format")
    if v_6 is not None:
        cargs.append("-e")
        cargs.append(str(v_6))
    v_7 = params.get("filename")
    if v_7 is not None:
        cargs.append("-f")
        cargs.append(v_7)
    v_8 = params.get("defaults")
    if v_8 is not None:
        cargs.append("-g")
        cargs.append(str(v_8))
    v_9 = params.get("ignore_derived")
    if v_9 is not None:
        cargs.append("-i")
        cargs.append(str(v_9))
    v_10 = params.get("scaling")
    if v_10 is not None:
        cargs.append("-l")
        cargs.append(str(v_10))
    v_11 = params.get("merge_2d")
    if v_11 is not None:
        cargs.append("-m")
        cargs.append(str(v_11))
    v_12 = params.get("series_number")
    if v_12 is not None:
        cargs.append("-n")
        cargs.append(v_12)
    v_13 = params.get("output_dir")
    if v_13 is not None:
        cargs.append("-o")
        cargs.append(v_13)
    v_14 = params.get("philips_scaling")
    if v_14 is not None:
        cargs.append("-p")
        cargs.append(str(v_14))
    v_15 = params.get("search_mode")
    if v_15 is not None:
        cargs.append("-q")
        cargs.append(str(v_15))
    v_16 = params.get("rename")
    if v_16 is not None:
        cargs.append("-r")
        cargs.append(str(v_16))
    v_17 = params.get("single_file")
    if v_17 is not None:
        cargs.append("-s")
        cargs.append(str(v_17))
    if params.get("update_check"):
        cargs.append("-u")
    v_18 = params.get("verbose")
    if v_18 is not None:
        cargs.append("-v")
        cargs.append(str(v_18))
    v_19 = params.get("conflict_behavior")
    if v_19 is not None:
        cargs.append("-w")
        cargs.append(str(v_19))
    v_20 = params.get("crop_3d")
    if v_20 is not None:
        cargs.append("-x")
        cargs.append(str(v_20))
    v_21 = params.get("compression")
    if v_21 is not None:
        cargs.append("-z")
        cargs.append(str(v_21))
    v_22 = params.get("endian")
    if v_22 is not None:
        cargs.append("--big-endian")
        cargs.append(str(v_22))
    v_23 = params.get("progress")
    if v_23 is not None:
        cargs.append("--progress")
        cargs.append(str(v_23))
    if params.get("ignore_trigger"):
        cargs.append("--ignore_trigger_times")
    if params.get("terse"):
        cargs.append("--terse")
    if params.get("xml"):
        cargs.append("--xml")
    cargs.append(execution.input_file(params["input_dir"]))
    return cargs

def dcm2niix_outputs(params: Dcm2niix, execution: Execution) -> Dcm2niixOutputs:
    """Build the Dcm2niixOutputs object for this tool."""
    root_v: OutputPathType = execution.output_file(".")
    return Dcm2niixOutputs(
        root=root_v,
    )

def dcm2niix_execute(params: Dcm2niix, runner: Runner | None = None) -> Dcm2niixOutputs:
    """
    dcm2niix

    Chris Rorden's dcm2niiX - DICOM to NIfTI converter. Converts DICOM files to NIfTI format with optional BIDS sidecar generation.

    Author: Chris Rorden

    Args:
        params: The parameters.
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    dcm2niix_validate(params)
    runner = runner if runner is not None else get_global_runner()
    execution = runner.start_execution(DCM2NIIX_METADATA)
    execution.params(params)
    args = dcm2niix_cargs(params, execution)
    out = dcm2niix_outputs(params, execution)
    execution.run(args)
    return out

def dcm2niix(
    input_dir: InputPathType,
    compression_level: float | None = None,
    adjacent: Dcm2niixAdjacent | None = None,
    bids: Dcm2niixBids | None = None,
    bids_anon: Dcm2niixAdjacent | None = None,
    comment: str | None = None,
    depth: float | None = None,
    export_format: Dcm2niixExportFormat | None = None,
    filename: str | None = None,
    defaults: Dcm2niixDefaults | None = None,
    ignore_derived: Dcm2niixAdjacent | None = None,
    scaling: Dcm2niixBids | None = None,
    merge_2d: Dcm2niixMerge2d | None = None,
    series_number: str | None = None,
    output_dir: str | None = ".",
    philips_scaling: Dcm2niixAdjacent | None = None,
    search_mode: Dcm2niixSearchMode | None = None,
    rename: Dcm2niixAdjacent | None = None,
    single_file: Dcm2niixAdjacent | None = None,
    update_check: bool = False,
    verbose: Dcm2niixVerbose | None = None,
    conflict_behavior: Dcm2niixVerbose | None = None,
    crop_3d: Dcm2niixCrop3d | None = None,
    compression: Dcm2niixCompression | None = None,
    endian: Dcm2niixBids | None = None,
    progress: Dcm2niixAdjacent | None = None,
    ignore_trigger: bool = False,
    terse: bool = False,
    xml: bool = False,
    runner: Runner | None = None,
) -> Dcm2niixOutputs:
    """
    dcm2niix

    Chris Rorden's dcm2niiX - DICOM to NIfTI converter. Converts DICOM files to NIfTI format with optional BIDS sidecar generation.

    Author: Chris Rorden

    Args:
        input_dir: Input folder containing DICOM files. Will be searched\
            recursively based on depth parameter.
        compression_level: gz compression level (1=fastest..9=smallest)
        adjacent: Adjacent DICOMs (images from same series always in same folder)\
            for faster conversion
        bids: Generate BIDS sidecar JSON files (o=only: no NIfTI)
        bids_anon: Anonymize BIDS sidecar files by removing personal information
        comment: Comment stored in NIfTI aux_file (up to 24 characters)
        depth: Directory search depth for DICOM files in sub-folders
        export_format: Export format: NRRD (y), MGH (o), JSON/JNIfTI (j), or\
            BJNIfTI (b)
        filename: Output filename template (%a=antenna, %b=basename, %c=comments,\
            %d=description, %e=echo number, %f=folder name, %g=accession number,\
            %i=ID of patient, %j=seriesInstanceUID, %k=studyInstanceUID,\
            %m=manufacturer, %n=name of patient, %o=mediaObjectInstanceUID,\
            %p=protocol, %r=instance number, %s=series number, %t=time,\
            %u=acquisition number, %v=vendor, %x=study ID; %z=sequence name)
        defaults: Generate defaults file (o=only: reset and write defaults;\
            i=ignore: reset defaults)
        ignore_derived: Ignore derived, localizer and 2D images
        scaling: Losslessly scale 16-bit integers (y=scale, n=no but uint16->int16,\
            o=original)
        merge_2d: Merge 2D slices from same series regardless of echo, exposure,\
            etc. (0=no, 1=yes, 2=auto)
        series_number: Only convert specified series CRC number (can be used up to\
            16 times)
        output_dir: Output directory (omit to save to input folder)
        philips_scaling: Use Philips precise float (not display) scaling
        search_mode: Search mode (y=show number of DICOMs, l=list DICOMs, n=no)
        rename: Rename instead of convert DICOMs
        single_file: Single file mode, do not convert other images in folder
        update_check: Check for newer versions
        verbose: Verbose output (0=no, 1=yes, 2=logorrheic)
        conflict_behavior: Write behavior for name conflicts (0=skip, 1=overwrite,\
            2=add suffix)
        crop_3d: Crop 3D acquisitions (i=ignore: neither crop nor rotate)
        compression: gz compress images (y=pigz, o=optimal pigz, i=internal:zlib,\
            n=no, 3=no,3D)
        endian: Byte order (y=big-end, n=little-end, o=optimal/native)
        progress: Slicer format progress information
        ignore_trigger: Disregard values in 0018,1060 and 0020,9153
        terse: Omit filename post-fixes (can cause overwrites)
        xml: Output Slicer format features
        runner: Command runner (defaults to global runner).

    Returns:
        Tool outputs (paths to files produced by the tool).
    """
    params = dcm2niix_params(
        input_dir=input_dir,
        compression_level=compression_level,
        adjacent=adjacent,
        bids=bids,
        bids_anon=bids_anon,
        comment=comment,
        depth=depth,
        export_format=export_format,
        filename=filename,
        defaults=defaults,
        ignore_derived=ignore_derived,
        scaling=scaling,
        merge_2d=merge_2d,
        series_number=series_number,
        output_dir=output_dir,
        philips_scaling=philips_scaling,
        search_mode=search_mode,
        rename=rename,
        single_file=single_file,
        update_check=update_check,
        verbose=verbose,
        conflict_behavior=conflict_behavior,
        crop_3d=crop_3d,
        compression=compression,
        endian=endian,
        progress=progress,
        ignore_trigger=ignore_trigger,
        terse=terse,
        xml=xml,
    )
    return dcm2niix_execute(params, runner)

__all__ = [
    "Dcm2niix",
    "Dcm2niixOutputs",
    "DCM2NIIX_METADATA",
    "dcm2niix_cargs",
    "dcm2niix_outputs",
    "dcm2niix_params",
    "dcm2niix_execute",
    "dcm2niix_validate",
    "dcm2niix",
]